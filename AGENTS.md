# AGENTS.md — english-learning-clean（Clean Rewrite · Implementation Baseline V1）

## 项目一句话

成人英语学习应用「英语客厅」的 **Clean Rewrite**：以 Implementation Baseline V1（用户主导的更高智力讨论收敛，2026-09-20 交回）为唯一规范权威，从零实现。**规范优先级：本仓 `docs/` 六 canonical 文档 > `behavioral_baselines/` 行为基线资产 > 其他一切**（旧仓 `D:\测试1` 整体为 exploration archive，其全部设计仅作历史参考）。实现栈已裁：**Python 内核 + SQLite**（app.db 可变 / content.db 只读 / SecretStore；客户端边界已裁 `DEC-…d7937fd7.12`：进程内 Python API，交互面推迟 Phase 8 末后）。

## 🔴 dmcp 段（新会话续接第一入口）

- **workspace_id**：`ws-7585bd9c-f6ec-4c73-be14-d2e635a37033`（**schema 5**；旧工作区 `ws-db58afd2-…` 已随旧仓归档，别用）
- **续接顺序**：①读本文件 → ②`design_status` 刷基 → ③`design_get` 首要五件（**Phase 9 / p9-4 已毕，下一动 = p9-5（末刀）**；**现役先读**：`PLAN-OPI-92170eff-…25`（**Phase 9 母计划**：`p9-0` **已毕** / `p9-1`（迁移 0018 五表 + 端口/适配 + 删除面 + 钉搬迁）**已毕** / `p9-2`（普通 chat 走 GUARDED_STREAM + 记录面推进 + 保守部分交付）**已毕** / `p9-3`（PreDeliveryGuard 七条 + Guard 外中断入口 + 保守取消与 handoff + 教学面接线）**已毕** / `p9-4`（ExposureEstimate 派生与 ACK 精化 + 两 crash 窗口保守和解 + 恰好一次曝光）**已毕** / `p9-5-stress-and-e2e`（**下一动 = 末刀**）；关系：p9-1 → {p9-2,p9-3,p9-4} → p9-5、p9-2 MAY_PARALLEL p9-3、p9-0 MAY_PARALLEL p9-1）/ `VAL-OPI-92170eff-…27`（相位验收八组）/ `DEC-OPI-92170eff-…23`（开工 + 前沿裁决 R1–R8）＋ 外评采纳 `DEC-OPI-92170eff-…21` ＋ **p9-1 三件**：`TASK-OPI-e26b27a7-…3`（任务书 A–E 五面 + R1–R12 预裁）/ 切片 `VAL-OPI-e26b27a7-…5`（验收七组，盖 **`VR-OPI-e26b27a7-…7` PASS 于 `26024e2`**）/ 处置 `DEC-OPI-e26b27a7-…9`（执行者 8 异议逐条判定 + 总控起草缺陷自认 5 项 + 机制事实三条）；＋ **p9-2 三件**：`TASK-OPI-e26b27a7-…12`（任务书 A–E 五面 + R1–R10 预裁）/ 切片 `VAL-OPI-e26b27a7-…14`（验收七组，盖 **`VR-OPI-e26b27a7-…16` PASS 于 `cb254c0`**）/ 处置 `DEC-OPI-e26b27a7-…18`（**含首交整刀拒收的完整留痕**：拒收→仓外快照→回退→合规重交→评审→处置）；＋ **p9-3 三件**：`TASK-OPI-e26b27a7-…20`（任务书 A–E 五面 + R1–R10 预裁 + 七条载体表）/ 切片 `VAL-OPI-e26b27a7-…22`（验收七组，盖 **`VR-OPI-e26b27a7-…24` PASS 于 `6596da0`**）/ 处置 `DEC-OPI-e26b27a7-…25`（评审 MEDIUM-1 当刀修 + 两条 carrier 收窄裁定 + 两次训诫留痕）；＋ **p9-4 三件**：`TASK-OPI-e26b27a7-…27`（任务书 A–E 五面 + R1–R9 预裁 + 派生表 + 两 crash 窗口映射）/ 切片 `VAL-OPI-e26b27a7-…29`（验收七组，盖 **`VR-OPI-e26b27a7-…31` PASS 于 `dabf8b2`**）/ 处置 `DEC-OPI-e26b27a7-…32`（评审 MEDIUM F2 = 本刀自己造成的既有断言强度降级，以**父/子对照变异**确证并恢复）；**以下 Phase 8 入口为历史**：`PLAN-OPI-0f76024b-4634-422f-ab0a-7f05bb786df0.5`（Phase 8 母计划六工作项：`p8-0-cp2-planner-durable` / `p8-1-gate-automatic-open` / `p8-2-session-budget-continuation` / `p8-3-ledger-durable-review-events` **均已 COMPLETE** / `p8-4-automatic-integration-stress` **已 COMPLETE** / `p8-5-rollout-gate` **已 COMPLETE（本会话收口）**）/ `VAL-OPI-0f76024b-…7`（相位验收八组；④ 记 `OQ-024A` 无载体 ⇒ 60 例 gate benchmark 为可执行替代）/ `DEC-OPI-0f76024b-…3`（开工 + 前沿裁决 R1–R7；R5/R6 仍有效）＋ `DEC-OPI-0f76024b-…1`（Phase 7 外评采纳）＋ **最近六刀的三件**：p8-5 = `TASK-OPI-92170eff-…10` / `VAL-OPI-92170eff-…12` / 处置 `DEC-OPI-92170eff-…16`（盖切片 `VR-OPI-92170eff-…14` + **相位验收 `VR-OPI-92170eff-…18`** PASS 于 `9506c56`）、p8-4 = `TASK-OPI-92170eff-…3` / `VAL-OPI-92170eff-…5` / 处置 `DEC-OPI-92170eff-…8`（盖 `VR-OPI-92170eff-…7` PASS 于 `08dda87`）、p8-3 = `TASK-OPI-6d40862d-…9` / `VAL-OPI-6d40862d-…11` / `DEC-OPI-6d40862d-…13`（盖 `VR-OPI-6d40862d-…15`）、p8-2 = `…1` / `…3` / `DEC-…5`（盖 `VR-…7`）、p8-1 = `TASK-OPI-64c88704-…17` / `VAL-…19` / `DEC-…21`（盖 `VR-…23`）、p8-0 = `TASK-OPI-64c88704-…7` / `VAL-…9` / `DEC-…11`（盖 `VR-…13`）；**以下为历史链**）：`PLAN-OPI-2394139d-206d-4b01-bd61-c9789866c287.8`（**Phase 7 母计划**：p7-1 kernel+trace **已毕** / p7-2 candidates+scope+priority **已毕** / **p7-3 frontier+ledger+coverage 已毕** / **p7-4 shadow-mode+OQ-023A 已毕**；p7-1..3 皆 MUST_PRECEDE p7-4——**四工作项均已毕 + 相位验收已盖章**（`VR-OPI-44647873-…38` PASS 于 `5395694`）**+ 收口 `DEC-OPI-44647873-…39` ⇒ PHASE 7 = COMPLETE（工程）；下一动 = Phase 8 开工第 0 步**（`plan_build` 铸 Phase 8 母计划：Planner → Gate → 自动教学面）**——外评已采纳 `DEC-OPI-0f76024b-…1`：`PHASE 7 COMPLETE = YES` 无 reopen、**PHASE 8 = GO**、**真实 rollout = HOLD**（原因 = 语料 readiness，非 Planner）；议程见遗留账 ⑩）＋`VAL-OPI-2394139d-…10`（相位验收八组；⑤ 记 **`OQ-023A` 无载体 ⇒ 43 例可执行替代**）＋**p7-1 三件**（`TASK-OPI-fc9d8ca7-…4` / `VAL-OPI-fc9d8ca7-…2` / 盖 **`VR-OPI-fc9d8ca7-…8` PASS** 于 `d876ed5`）＋**p7-2 三件**（`TASK-OPI-fc9d8ca7-…13` / `VAL-OPI-fc9d8ca7-…11` / 盖 **`VR-OPI-fc9d8ca7-…20` PASS** 于 `53b330e`）＋**p7-3 三件**（`TASK-OPI-44647873-…20` / `VAL-OPI-44647873-…18` / 盖 **`VR-OPI-44647873-…23` PASS** 于 `b7b29f3`；交付 `821b247` + 处置修 `51a9868` + 收口 `b7b29f3`）＋**p7-3 前沿裁决** `DEC-OPI-44647873-…16`（R1 前沿=第 4 步幸存者集 / 台账纯核不落表 ⇒ 零迁移 / 覆盖阶梯与曝光窗为声明常数 / UNKNOWN 不转义）+ **p7-3 处置** `DEC-OPI-44647873-…25`（3 LOW 已修 + 4 INFO 入册；含总控起草缺陷自认两项）＋`DEC-OPI-b99560d4-12a8-4ff5-8450-5049d9d97106.10`（**Phase 6 收口 + 门二四查预告**；**待补铸 DEC 已于第十五会话全部补铸**（门二 `DEC-OPI-44647873-…4` / 外评采纳 `…7` / P7-0 `…10` / p7-1 处置 `…12` / p7-2 处置 `…14`）——`decision_add` 契约已恢复）＋**门二四件** `PLAN-OPI-b99560d4-…14`（门二计划）/`TASK-OPI-b99560d4-…19`（门二任务书——**四查取证与裁决 R1–R15 全文在此**）/`VAL-OPI-b99560d4-…26`（门二验收，**取代 `VAL-…b99560d4.…17`**；其 ⑤ 已记 profile 腿的**有据收窄**）+`VR-OPI-b99560d4-…28`（**PASS 于 `4ce43ed`**）＋`DEC-OPI-b99560d4-12a8-4ff5-8450-5049d9d97106.10`（**Phase 6 收口**：四刀链 + 同题扩版相位 VAL 取代 `VAL-…a68fd9eb.…44` + 遗留账七项接管 + **门二四查预告**——**本会话末先读**）＋`DEC-OPI-5a0be06d-5af8-4124-9550-88fda7b22587.23`（**p6-3 处置**：评审 ACCEPT-WITH-FINDINGS 无 HIGH / L-1·L-2·INFO-1·INFO-2 已修（`4a941a1`）/ L-3 预算留痕 + 「今后任务书写预算带须写明计数口径」/ **流程书序修正**（处置→盖章）/ **总控任务书笔误承认**——**收口前先读**）＋`DEC-OPI-5a0be06d-…15`（**p6-3 前沿裁决 R1–R12**：所有权 user_config / 0013 / §9 九列·四类型·三 scope / **active 独立转移面** / 读面时间窗与目标腿 / **22 处断钉预写** / R10 迁移常量收口）＋`DEC-OPI-5a0be06d-…9`（**p6-2 处置**：due 四步派生的读法边界、F-2 DEFER 触发条件、通道纪律第四段）＋`DEC-OPI-5a0be06d-…2`（**p6-2 前沿裁决 R1–R15**：零迁移 / 内容寻址 version / `ScheduleView(as_of)` / `SessionBudgetView` 留 Phase 8 / **`PlannerConstraint` 排位 p6-3 归 user_config**（已兑现））＋`VAL-OPI-b99560d4-…6`（**相位验收七组 = 同题扩版**——取代 `VAL-OPI-a68fd9eb-…44`（其 ⑤「migrations 增至 0011」句已假）：⑤ 改 **0013/v13** + 新增 ⑥ `PlannerConstraint` §9 组，路径集扩入 README.md/AGENTS.md；盖章 `VR-OPI-b99560d4-…8` PASS）；Phase 6 三份计划: `PLAN-OPI-a68fd9eb-…42`（p6-0/p6-1/p6-2，**全毕**）/`PLAN-OPI-5a0be06d-…13`（p6-3，**已毕**）；背靠 `DEC-OPI-4d516e4f-9575-4cbf-8d08-6bf19a95bd25.32`（**外评采纳：表述收窄 + 三重生产硬门 +「禁用 _seed()」**——仍生效）→ ④`git log --oneline` → ⑤按下一动读文档（**PHASE 9 进行中：p9-0 / p9-1 / p9-2 / p9-3 / p9-4 均已 COMPLETE（p9-4 = 交付 `8df2dbd` + 处置 `dabf8b2`，head **0018_delivery_records / v18**，读数 3433/0/0）**；**下一动 = p9-5（末刀，路径集仅 `tests/`）**——读 `IMPLEMENTATION_PLAN.md` §10 的 Acceptance 四条 + 外评**九条承诺清单**（见「当前状态」与 `DEC-OPI-92170eff-…21`）+ `RUNTIME_ARCHITECTURE.md` §13/§14/§17/§18/§22/§23 与 `STATE_MACHINES.md` §13；本刀 = 承诺清单逐条可证/诚实登记 + E2E（普通 chat GUARDED_STREAM 与 teaching BUFFERED_VALIDATED 全链各一条真库链路）+ 不回归（全量 + 四口径 + 60/43 重放）；**其后 = Phase 9 相位验收**（`VAL-OPI-92170eff-…27` 八组在实际最终提交上盖章）+ 相位收口 + 外评；真实 rollout **仍 HOLD**（SessionBudget 三分与内容补课为开闸前置））。详见「当前状态」与 ㉓**；

（历史：p8-4 = COMPLETE（第十八会话）——七面全部落地；交付 `08dda87`（含处置），评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM，读数 2929/0/0；详见「当前状态」p8-4 块与 ⑲）**p8-3（台账落表 + 删除面 + ReviewEvent 语义）= COMPLETE**（交付 `ff769e9`+`280adc1` + 处置 `ddbfd90`，盖 `VR-OPI-6d40862d-…15` PASS 于 `ddbfd90`；三件 = `TASK-OPI-6d40862d-…9` / `VAL-OPI-6d40862d-…11` / 处置 `DEC-OPI-6d40862d-…13`）；**p8-2 = COMPLETE**（交付 `14041d0` + 处置 `2f0b8e3`，盖 `VR-OPI-6d40862d-…7` PASS）；**p8-1 = COMPLETE**（交付 `b43d4bc` + 处置 `325e7a6`，盖 `VR-OPI-64c88704-…23` PASS）；**p8-0 = COMPLETE**（交付 `42d1b16` + 处置 `0479cbf`，盖 `VR-OPI-64c88704-…13` PASS）；**Phase 8 = 开工第 0 步已完成**（`DEC-OPI-0f76024b-…3` / `PLAN-…5` / `VAL-…7`）；四查必答面见 ⑩ 的五项 + 迁移面预写 + ⑭/⑯/⑰/⑱；**p8-4 的四查面（供参考）**：`RUNTIME_ARCHITECTURE.md` §4 步 11–20（prompt/Provider/Validator/Guard/Delivery/CP3a/terminalization/CP4）+ §6 CP3a 与 §13/§14/§15（delivery 与曝光语义）+ §22/§23（恢复与崩溃窗口的 Moment 半）+ `DATA_MODEL §22`（Delivery Data）/§15（TeachingMoment 的 lifecycle 与 delivery 转移）+ `IMPLEMENTATION_PLAN §9`（§7.2 自动路径）与 §12（rollout）+ p8-3 的 `teaching_presented` 事件面（`elc/planner/ledger_store.py` 的写面 + `planning_ledger_event` 五词）与 `ConversationPriorityView` 的生产者（RA §4 步 3））：**Phase 8** 读 `docs/IMPLEMENTATION_PLAN.md` §9（及 §12）/`docs/RUNTIME_ARCHITECTURE.md` §CP2 与 10A–10B 与崩溃窗口/`docs/DATA_MODEL.md` §14·§14.1·§15·§18/`docs/DOMAIN_MODEL.md` §13·§14/`docs/STATE_MACHINES.md` 的 Teaching 与 Gate 转移面/`behavioral_baselines/gate/*`（BF-03 v1.1 + 60 例 benchmark + 参考实现 + 决策表）。**Phase 7** 读 `docs/DOMAIN_MODEL.md` §10/§10.1/§11/§12/§13、`docs/DATA_MODEL.md` §14/§24.14、`docs/IMPLEMENTATION_PLAN.md` §8 与 `behavioral_baselines/planner/*`（BF-02 §5 的 `feature_assembly_status`/`snapshot_status`/missing_authorities 与 DEFERRED 路径）。派单协议同下：四查+VAL+TASK 同铸；**四查必答：列名/词表/判据照 canonical 原文逐字；本刀若新增迁移，stamp 钉搬迁必须预先写明**（p6-1 16 处 / p6-3 22 处的判决性实验先例；自 p6-3 起 head/stamp 值集中在 `tests/conftest.py` 常量，边际成本已降）；读数口径按 `DEC-…a68fd9eb.32`；内容补课未落地前不得假设任何 target readiness ≥ R2；禁 `_seed()`/fixture 供给；**VAL 一律带实际触及路径集**）
- **执行者遗留处理先例**（已发生三次：P3-1A/P3-2/P4-4 前均因 API 余额中断留下半成品）：开工前必查 git status——不干净时先取证（快照/sha256/差异分析），按「吸收+逐文件复核+修正+补齐+留痕」或「拒收重交」二选一，绝不静默覆盖；**P4-4 先例**：遗留半成品 3 条失败被判测试侧，评审以 as-found 原件复跑逐条复核成立
- **当前状态**（2026-09-25 本会话续；rev 185）：**p9-4 = COMPLETE（Phase 9 第五刀）：交付 `8df2dbd` + 处置修 `dabf8b2`；盖切片 `VR-OPI-e26b27a7-…31` PASS 于 `dabf8b2`；三件 = `TASK-OPI-e26b27a7-…27`（任务书 A–E 五面 + R1–R9 预裁 + 派生表 + 两 crash 窗口映射）/ `VAL-OPI-e26b27a7-…29`（验收七组）/ 处置 `DEC-OPI-e26b27a7-…32`；读数 3433/0/0（phase3 280 / phase6 666 / phase7 420 / phase8 667 / phase9 401 / deletion 120；非 phase6·7 不含 deletion 2227）+ mypy 154 文件 0 error；ruff clean、compileall 0；**零迁移（head 0018_delivery_records / v18）**；评审 ACCEPT-WITH-FINDINGS 无 HIGH（**1 MEDIUM + 3 LOW + 3 INFO**；**9 RED / 2 NOT-RED**）；执行者**零通道违规**；下一动 = p9-5（末刀：stress + E2E + 外评九条承诺清单）**。**交付面**：`src/elc/runtime/exposure_reconciliation.py`（新 521 行 SQL-free/零时钟/纯：`exposure_estimate_of` 派生（终态词 × 前缀/全文 × 尝试/未尝试 ⇒ §22 六列，partial ⇒ 绝不 FULL）、`ACK_CERTAINTY_LADDER`（`UNKNOWN`<`SERVER_SENT_UNCONFIRMED`<`CONFIRMED_RENDERED`）、`refine_with_ack` 只升不降、`derivation_reason` 确定性）+ `delivery_records.refine_exposure_estimate` 协议 + `delivery_store` 实装（单调守卫）+ `planner/ledger_store.has_ledger_event` + `exposure.py` 第三读 + controller（两交付面初估写入单点 `_write_initial_estimate`（端口缺席不模拟）/ `accept_render_ack`（不持 guard，写 ACK + 只升精化，零 mastery）/**两 crash 窗口和解**：普通 loop 的 `DELIVERING` 调停 + 启动线 `reconcile_delivering_residue`；`abort_undelivered_openings`（D② 先于 sweep）+ `StartupRecoveryOutcome` 两新字段）+ `tests/phase9/` 三新文件（**+82 例**）。**本刀 DEFER/INFO（登记）**：F3 `presented` 谓词今日不可达（已补强制世界一例；触发 = 教学动作改走流式面）；F5 `_ensure_exposure_once` 的 found 分支返回 slot 期望词而非 durable 行 `event` 列（触发 = 下次触碰该面）；F6 `UNKNOWN` certainty 现役调用点不可达；F7 预算带须写明「src 行数 vs 含测试」。**（以下为 p9-3 收口态，保留为历史）**：**p9-3 = COMPLETE（Phase 9 第四刀）：交付 `90bb577` + 处置修 `6596da0`；盖切片 `VR-OPI-e26b27a7-…24` PASS 于 `6596da0`；三件 = `TASK-OPI-e26b27a7-…20`（任务书 A–E 五面 + R1–R10 预裁 + **七条 hard invalidation 的 V1 载体表**）/ `VAL-OPI-e26b27a7-…22`（验收七组）/ 处置 `DEC-OPI-e26b27a7-…25`；读数 3351/0/0（phase3 280 / phase6 666 / phase7 420 / phase8 667 / phase9 319 / deletion 120；非 phase6·7 不含 deletion 2145）+ mypy 153 文件 0 error；ruff clean、compileall 0；**零迁移（head 0018_delivery_records / v18）**；评审 ACCEPT-WITH-FINDINGS 无 HIGH（**1 MEDIUM + 3 LOW + 6 INFO**；13 组变异 **11 RED / 1 等价 / 1 转 LOW**）；下一动 = p9-4（teaching-exposure-reconciliation，末刀 p9-5 前）**。**交付面**：`src/elc/runtime/pre_delivery_guard.py`（新 363 行 SQL-free/零时钟：七条件 `CONVERSATION_INACTIVE`/`ACTION_CANCELLED`/`ACTION_SUPERSEDED`/`TEACHING_LOCK_INVALID`/`NEW_TARGET_SUPPRESSED`/`JUST_CHAT_HARD_SWITCH`/`LINEAGE_MISMATCH`、`PreDeliveryGuardFacts` 七字段各 `bool|None`、`guard_verdict` 三分规则（True⇒INVALIDATE+码 / None⇒`UNCHECKED_*` 不判无效 / 全 False⇒VALID）、decision 只用 §21.1 两词、**3^7=2187 穷举零失配**）+ `src/elc/runtime/interrupt_transport.py`（新 87 行：`InterruptAwareTransport` 包装既有 transport，用 p9-2 的 `STOPPED(reason)` 缝 ⇒ `guarded_stream.py` **零 diff**）+ `conversation/` 三只读面与待决谓词（「待决」= 命名 leg 仍未终态；行不删）+ controller（Guard 外 durable 入口 `request_interrupt`（持锁时 0.001s 成功）+ Guard 内 `_reconcile_pending_interrupt`（**三 owner**：begin_turn/request_teaching/respond_to_teaching）+ `_invalidate_teaching_delivery` + guard 行落盘（**VALID 也写**，`_joined_note` 合并失败注记））+ `tests/phase9/` 四新文件（**+77 例**，含处置新增 `test_p9_3_teaching_guard.py` 830 行/5 例）。**本刀 INFO/风险六条（登记）**：INFO-1 中断与末块竞争时全文送达也记 `CANCELLED`+`SENT_PARTIAL`（无测试钉 ⇒ Revisit = P9-4 曝光对账首次读该行）；INFO-2 理由码次序（实现 cancelled→superseded，canonical §15 列举序相反）；INFO-3 同 action 二判 `CONFLICT`（判决已驱动行为、今日不可达）；INFO-4 `_cancel_streamed_delivery` 前缀臂「恒 CANCELLED_BY_USER」是真空不变式；INFO-5 `_delivery_interrupted` 读失败 ⇒ False（继续发，方向保守）；INFO-6 预算口径；小风险两条：`_joined_note` 两笔写失败同时发生无专属用例 / resume+目标抑制的新行为未专门测试。**两处 carrier 读法收窄（已裁定采纳，见 `DEC-…25`）**：lineage 腿改为「教学 action 比对其 moment 的 cycle」（依据 `STATE_MACHINES.md:391`）；lock 腿对 `PERSONA_RESUME` 读 False（交付在 `terminalize_moment` 之后、lock 已释放）。**训诫留痕两次**：p9-3 交付刀 1 次仓内 Bash（`sed -i` 改测试文件，<2 次不拒收）+ 评审刀 2 次仓外 Bash 回写 probe。**（以下为 p9-2 收口态，保留为历史）**：**p9-2 = COMPLETE（Phase 9 第三刀）：交付 `ab8f683`（**重交**，as-found 纯吸收自被拒首交 `ce771e0`：提交面逐字节相同）+ 处置修 `cb254c0`；盖切片 `VR-OPI-e26b27a7-…16` PASS 于 `cb254c0`；三件 = `TASK-OPI-e26b27a7-…12`（任务书 A–E 五面 + R1–R10 预裁）/ `VAL-OPI-e26b27a7-…14`（验收七组）/ 处置 `DEC-OPI-e26b27a7-…18`（**含首交拒收全程留痕**）；读数 3274/0/0（phase3 280 / phase6 666 / phase7 420 / phase8 667 / phase9 242 / deletion 120；非 phase6·7 不含 deletion 2068）+ mypy 151 文件 0 error；ruff clean、compileall 0；**零迁移（head 0018_delivery_records / v18）**；评审 ACCEPT-WITH-FINDINGS 无 HIGH（**3 MEDIUM + 1 LOW + 4 INFO**；**16 组变异 13 RED / 1 等价 / 2 真缺口**）；下一动 = p9-3（barge-in/cancel/supersede），p9-4（teaching exposure reconciliation）**可并行****。**首交拒收（本仓立规首次真执行）**：首交 `ce771e0`（3270/0/0、六文件 +3105）因执行者**自报 2 次同类通道违规**（Bash heredoc `cat >>` 两次追加仓内测试文件）⇒ 按 p8-4 立规「同一刀内 ≥2 次同类违规即整刀拒收、无裁量」判**整刀拒收**：仓外快照 `D:\_p92_rejected\ce771e0.patch` → `git reset --hard b0eb0c7`（被拒提交仍可按 hash 取回）→ 重交刀以 **Write/Edit** 从被拒提交只读取内容重落（`controller.py` 全 +389 以 8 次 Edit 外科插入；重交自报**零次仓内 Bash 写入**）⇒ **提交面 sha256 逐文件自证纯吸收**。**交付面**：`src/elc/runtime/guarded_stream.py`（新 495 行 SQL-free/零时钟：`StreamStep` 三形 / `StreamTransport` 两面 / `SMALL_BUFFER_CHUNKS=1` / `STREAM_STEP_BUDGET=10_000` / `DELIVERY_MODE_BY_ACTION` 六词表 + `delivery_mode_of` fail-closed / `guard_verdict` 复用 `ResponseValidator.decide` 同规则集同版本对**累积文本**判定 / `run_guarded_stream` 顺序 take→guard→emit→record / `LocalSingleChunkTransport` V1 单块替身）+ `runtime/controller.py`（+389 **零删除**：`StreamedDelivery` / 两注入点 `delivery_records=`·`stream_transport=`（默认 None）/ 普通 persona 段改走 `finalize_streamed_delivery`（`SENDING`→`SENT_PARTIAL`×n→终态，**以 durable `sent_prefix` 逐字**落 transcript）/ `finalize_delivery` **一字未动**（teaching 零放松））+ `runtime/types.py`（+14：`TurnCompletion.delivery_state`/`delivery_failure_reason`）+ `tests/phase9/` 三新文件（**+87 例**；其中 4 例为处置新增）。**本刀新增 DEFER 五条（F4–F8，各带触发条件）**：F4 `SMALL_BUFFER_CHUNKS` 为纯声明（结构性质非可调参数）；F5 SQL-free 钉只覆盖导入集（触发 = 下次触碰 `guarded_stream.py` 时并入语句文本扫描）；F6 `finalize_streamed_delivery` 未写「调用方持 guard」（姊妹面写了）；F7 已修（注册句收窄为「推进为」）；F8 M12 属等价变异非缺口。**（以下为 p9-1 收口态，保留为历史）**：**p9-1 = COMPLETE（Phase 9 第二刀）：交付 `fc2e818`（五面）+ 处置修 `26024e2`；盖切片 `VR-OPI-e26b27a7-…7` PASS 于 `26024e2`；三件 = `TASK-OPI-e26b27a7-…3`（任务书 A–E 五面 + R1–R12 预裁）/ `VAL-OPI-e26b27a7-…5`（验收七组）/ 处置 `DEC-OPI-e26b27a7-…9`；读数 3187/0/0（phase3 280 / phase6 666 / phase7 420 / phase8 667 / phase9 155 / deletion 120；非 phase6·7 不含 deletion 1981）+ mypy 150 文件 0 error；ruff clean、compileall 0；**head `0018_delivery_records`（v18）；本刀新增迁移 0018**；评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM（2 LOW + 6 INFO；**12 组变异全 RED、零 NOT-RED**）；下一动 = p9-2（guarded-stream-normal-persona）**。**交付面**：`migrations/0018_delivery_records.sql`（302 行；五表逐列照 §22/§21.1：`server_delivery_record` 7 / `client_render_ack` 6 / `exposure_estimate` 6 / `validator_result` **7** / `pre_delivery_guard_result` **6**；CHECK 恰三处（§21.1 两处词表 + `final_rendered` 的 (0,1)）；五个 `action_id` FK → `generation_action_intent`；`assistant_turn_id` 无 FK；stamp 两条 '18'）+ `src/elc/runtime/delivery_records.py`（610 行 SQL-free 端口：四新行形状 + `ValidatorResult` 复用 `elc.persona.types` 的 + 唯一列映射 + 10 条带 Revisit 的声明读法）+ `src/elc/platform/db/delivery_store.py`（631 行：单短事务 / 写事务内 epoch fence / **零时钟**（instant 全来自记录）/ 重放·冲突 / `server_delivery_record` 四条推进不变量）+ 删除面同刀（三清单（位于 `generation_action_intent` 之前）+ `_SURFACE_SELECT`·`_DELETE_BY_ROWID` 各五条 + `_conversation` 五条腿）+ 钉搬迁 8 文件 / 7 组（head 0017→0018 / v17→v18）+ `tests/phase9/` 三新文件（+87 例 = 25+44+18）。**本刀新增 DEFER 五条（F4–F8，各带触发条件）**：F4 断言冗余（非错）；F5 `delivery_store.py:52`「no identifier assembly」措辞不精确（同句式已在 `planner_store`/`projection_store`/`decision_cycle_store`/`generation_store`；改会牵连 db 层口径）；**F6 registry 同名不同形**（`registry.py:160` 的 `validator_result` 绑 `runtime.types.ValidatorResultRecord`，durable 行是 `persona.types.ValidatorResult`；触发 = 触碰该条目或该形状的刀）；**F7 `validator_result.created_at` NOT NULL 而 `persona/validator.py:56` 默认 None ⇒ None 判 `VALIDATION_FAILED`**（触发 = p9-2/p9-4 写者自带 instant）；F8 `final_rendered` 的 (0,1) CHECK 为已登记读法。**（以下为 p9-0 收口态，保留为历史）**：**p9-0 = COMPLETE（Phase 9 首刀）：交付 `77fa549`（含处置）；盖切片 `VR-OPI-92170eff-…33` PASS 于 `77fa549`；处置 `DEC-OPI-92170eff-…34`；读数 3100/0/0（phase3 280 / phase6 666 / phase7 420 / phase8 667 / phase9 68 / deletion 120；非 phase6·7 不含 deletion 1894）+ mypy 148；零迁移（head 0017/v17）；评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM（3 LOW + 4 INFO；12 组变异全 RED）；下一动 = p9-1（迁移 0018 五表）**。**三件**：`TASK-OPI-92170eff-…29`（任务书 A–D 四裁决）+ `VAL-OPI-92170eff-…31`（切片验收五组）+ 处置 DEC。**交付面**：`src/elc/planner/trace_document.py`（452 行 SQL-free，versioned structured JSON `ft1`）+ 形状寄居 `platform/types.py`（断环）+ `records.py`/`planner_store.py` 的写读重放单点 + `automatic_teaching/turn.py` 生产装配真接 trace + `gate.py` 的 `ContinuationFacts.terminalizing_action`（**= p8-1 DEFER F11 收口**；评审 200 组网格证 HEAD 恰 4 组与冻结参考不符、本刀恰好关掉那 4 组）+ `persona/commands.py` 的门三**区域框架**（边界标记+长度前缀+行终止符转义；评审在 HEAD 精确树上实测 pre-cut 可伪出第二个 `[contract]` 头行、本刀后消失）+ `scheduler/review_record.py` 的 writer 契约段 + **anchor 裁置请求** + `tests/phase9/` 六文件（+68 例）。**PHASE 9 = 开工第 0 步已完成（三件已铸：开工 `DEC-OPI-92170eff-…23`（R1–R8：迁移 0018 / P9-0 carryover / GUARDED_STREAM 本地形态 / ExposureEstimate 与台账单向 / PreDeliveryGuard 七条 / rollout 仍 HOLD / 挂账不变 / 不变量）/ 母计划 `PLAN-OPI-92170eff-…25`（六工作项 + 八关系）/ 相位验收 `VAL-OPI-92170eff-…27`（八组））；**p9-0（carryover + trust boundary）= COMPLETE（交付 `77fa549` + 处置 `DEC-OPI-92170eff-…34`，盖切片 `VR-OPI-92170eff-…33` PASS 于 `77fa549`；三件 = `TASK-…29` / `VAL-…31` / 处置 DEC）；下一动 = p9-1（canonical-delivery-records：迁移 0018 五表，逐列照 §22/§21.1）**；**p8-5（`rollout-gate`，RISK_SPIKE）= COMPLETE ⇒ PHASE 8 = COMPLETE（六工作项 p8-0..p8-5 全毕；收口 `DEC-OPI-92170eff-…19`）；PHASE 8 外评已采纳（`DEC-OPI-92170eff-…21`，基于远程 `b098e8a`）：`PHASE 8 COMPLETE = YES`（无 reopen）/ `PHASE 9 IMPLEMENTATION = GO` / `REAL AUTOMATIC-TEACHING ROLLOUT = CONTINUE HOLD`；两个 MEDIUM（durable `factor_trace` 完整度、`ContinuationFacts.terminalizing_action`）逐条取证成立并归 **P9-0 Carryover**；Phase 9 六刀切片（P9-0 Carryover+Trust Boundary / P9-1 Canonical Delivery Records / P9-2 GUARDED_STREAM / P9-3 Barge-in·cancel·supersede / P9-4 teaching exposure reconciliation / P9-5 Stress·E2E）采纳为提议（位次由开工第 0 步 `plan_build` 定），详见 ㉒**：**交付 `9506c56`；盖切片 `VR-OPI-92170eff-…14` + 相位验收 `VR-OPI-92170eff-…18` PASS 于 `9506c56`；处置 `DEC-OPI-92170eff-…16`；sync SYNCED 163 对象 / rev 166 / CST `CST-247cee34-…`**。**三件**：`TASK-OPI-92170eff-…10`（任务书 A–G 七裁决）+ `VAL-OPI-92170eff-…12`（切片验收六组）+ 处置 DEC。**交付面**：`src/elc/teaching/rollout.py`（1130 行，SQL-free：`RolloutStage`（IP §12 四词，未声明 fail-closed 最保守）+ `stage_allows_automatic` + `automatic_teaching_enabled_of`（阶段腿 × §5.1 模式腿，无第三源）+ `GATE_ROWS`（BF-02 §10 首 fence 逐字）+ 门槛检查器（真语料实测 `VERDICT: HOLD`：四行 usable 0、14 targets / 14 without a level）+ 六项观察指标读数（定义/读法/载体诚实声明/无载体项代理/Revisit）+ HOLD 判定与开闸条件（回退手段照 IP §12，零开闸声称））；接线合成式 = `Planner 组装值 ∧ stage_allows_automatic(wiring.rollout_stage)`（两腿承重、默认 None deny、无第二次读模半）；`gate.py`/`automatic_teaching.py`/`budget.py` 三处 Revisit 与主语收口（纯注释）；测试 = `tests/phase8/` 5 个新文件（+103 例）+ p8_4 两文件增补（AST 布线钉扩别名面）。**独立评审（新上下文 executor-pro 只读、仓外 15 组变异）**：**ACCEPT-WITH-FINDINGS，无 HIGH**（1 MEDIUM + 1 LOW + 7 INFO；13 RED、**2 组 0-RED = 两条 findings**）——评审把残件回植实跑复现 6/6、三重独立腿核 `GATE_ROWS`（自写解析器 + 与 kernel 四 floor 常量双向相等 + 真语料探针）、并发证据在仓外副本复跑；**处置**：MEDIUM-1（skip/reject 两半不求和无断言承重，变异 M5 全仓 0-RED）补生产顺序断言 + 变异自证 1 RED；LOW-1（AST 布线钉 M13/M14 别名形状 0-RED）扩 `_wiring_names` + 变异自证 2 RED；INFO-1/5/6/7 收口 + INFO-2/3/4 登记。**执行者遗留先例（第 6 次）**：首派 `executor-pro` 报 `Insufficient Balance`（第 7 次执行者侧中断）留 11 文件半成品（`rollout.py` 1117 行 + 5 测试文件；**CR=0 全 LF 无字节指纹**）→ 总控快照（`D:\_p85_residue_snapshot\`）+ 亲跑 3024/6 → 续接刀判「**6 条全测试侧缺陷**（§10 块解析过宽 / budget 断言不可能为真 / `elc.platform.db` 闭包先于本刀 / 大小写笔误 / `model_fields` 必抛 / `NameError`）」逐条改写 ⇒ 3031/0/0。**通道**：半成品与两刀均无仓内 Bash 落盘；**评审侧自报 2 次口径越界**（Bash 内联 python 回写仓外 probe / `git show > 仓外`）⇒ 训诫留痕。**总控起草缺陷自认 1 项**：任务书 §0 第 1 条失败名写错（实名 `test_the_four_floors_are_bf_02_section_10s_block`）。**读数为总控亲跑**（`9506c56`）：**3032/0/0**；`tests/phase3` **280** / `phase6` **666** / `phase7` **420** / `deletion` **120** 逐值不变、`tests/phase8` **667**；非 phase6·7 不含 deletion = **1826**；ruff clean、mypy **147** 文件 0 error、compileall 0；**head `0017_ledger_event_provenance`（v17）；零迁移**；60 例 gate 重放与 43 例 planner stress 全绿；冻结面零改动。**（以下为 p8-5 开工前的状态记录，保留为历史）**：**p8-4（`automatic-integration-stress`）= COMPLETE**：**交付 `08dda87`（含处置；已推送）；盖 `VR-OPI-92170eff-…7` PASS 于 `08dda87`；处置 `DEC-OPI-92170eff-…8`；sync SYNCED 159 对象 / rev 162 / CST `CST-880fb120-…`**。**三件**：`TASK-OPI-92170eff-…3`（任务书 A–H 八裁决 + 迁移钉预写清单 + 预算两口径）+ `VAL-OPI-92170eff-…5`（切片验收八组）+ 处置 DEC。**交付面**：`src/elc/runtime/automatic_turn.py`（新 1050 行，SQL-free：真源装配 PlanningRequest + `generate_candidates` 供给（**可选 `supply` 注入点** = ALLOW 验收缝，与 p8-1 `supply_of` 同口径）+ `run_shadow` + §13 `ConversationPriorityView` 生产者 + `record_leg_failure`）+ `src/elc/runtime/exposure.py`（新 274 行：RA §20 四词写手（`teaching_presented`/`hint_presented`/`reveal_presented`/`user_skip`），单点，确定性 event id）+ `runtime/controller.py`（`automatic_teaching: AutomaticTurnWiring | None = None` **默认不可见** + `_run_automatic_leg`（读回 `get_action_for_turn`）+ `_finish_automatic_open`（CP2 后重入续交同 action + 推 AWAITING_USER）+ `_with_exposure`/`_record_skip_exposure`）+ `migrations/0017_ledger_event_provenance.sql`（`planning_ledger_event ADD COLUMN moment_id TEXT`，nullable **无 FK**、纯 ALTER；stamp 17）+ `ledger_store` 的 moment_id 写读面 + **replay 判定含列** + `deletion` 的 **CONVERSATION 清引用腿**（`_clear_ledger_provenance_legs`，§27 先例非删行；counted in `cleared_provenance_legs`）+ `planner/scope.py` 的 `natural_break_available_of`（共享读法；shadow 行为零改动、`sh2` 不动）+ 20 tracked 改动 + 11 新文件 + `tests/phase8/` 8 个新文件（**+142 例**）。**独立评审（新上下文 executor-pro 只读、仓外 11 组变异）**：**ACCEPT-WITH-FINDINGS，无 HIGH、无 MEDIUM**（3 LOW + 5 INFO；**10/11 变异 RED、1 NOT-RED = F1**）——硬证据含「默认不可见」的 **47 表跨副本逐表比对**（唯一差异 = 0017 新列）、G7 在 HEAD 副本上以 user-initiated 路径独立复现、replay 三连探针、钉搬迁 11 处无放松（含一处**收紧**：`p8_3_deletion` 的 `("DELETE",)`→`("DELETE","UPDATE")`）。**处置（`08dda87`）**：F1 修（读回腿两例 + 仓外变异自证 **2 RED**）/ F2 修（四处 replay 措辞收窄为真事实 + 补 `ledger_event/ledger_failure` 断言）/ F3 只登记（`review_event.teaching_moment_id` 无 moment 腿 ⇒ CONVERSATION 删除 FK 拒删；今日无生产写者）+ 一条钉现状 / F4–F8 登记。**顺带修先于本刀存在的删除面缺陷（G7，评审独立复现）**：`gate_execution_status` 的 OPEN 行 `moment_id=NULL`（p3-1A 注释「an OPEN binds the DecisionCycle」）使 CONVERSATION 清扫漏行 ⇒ `decision_cycle` FK 拒删 ⇒ **任何开过教学的会话都删不掉**；修复 = 一片同键清扫（语句类不变）+ 3 例。**通道违规裁量（本刀）**：续接刀自报 **6 次同类**（Bash/Python 写 4 个**自建**测试文件 ⇒ CRLF 中间态、自行归正为 LF）+ 处置刀自报 **3 次仓外**（Bash heredoc patch 仓外诊断脚本）⇒ **训诫留痕 + 立规：下一刀（p8-5）起同一刀内 ≥2 次同类违规即整刀拒收（恢复 p7-0 判例）、无裁量**；总控自认**取证方法错误**（Git-Bash `grep -c $'\r'` 被当空 pattern ⇒ 报的是行数而非 CR 数，误判 3 个 as-found 新文件为 CRLF 并写进派单——已撤回；**今后 CR 计数一律用 python 字节计数或 `git ls-files --eol`**）。**总控起草/取证缺陷自认 5 项**：① 任务书 §3 钉清单不完整（漏 `tests/architecture/test_platform_db_infra.py` 2 条与 `test_p6_3_invariants.py` 名单族）；② 数字面「约 36 条迁移钉」实为 31 条 + architecture 2 条；③ 预算口径自相矛盾（「`git diff --stat`」与一个只有含 untracked 才成立的下界）；④ **取证方法错误**（见上，最严重一项）；⑤ 处置任务书 F1 的断言②③与代码事实不符（读回失败在 CP2 之后 ⇒ CP2 五件已 durable、leg-failure 被 P8-0 重放规则拒 ⇒ 不落；执行者驳回并按实测断言 + 登记 torn state）。**执行者侧故障留痕**：首派 `executor-pro` 报 `Insufficient Balance`（**第 6 次执行者侧中断**）**但在失败前留下 12 文件半成品**（第 5 次「执行者遗留先例」：总控仓外快照 + 修 2 处阻断缺陷（`controller.py:53` 拼行 SyntaxError / `automatic_turn.py` 缺 `AUTOMATIC_OPEN_SLOT`）+ 诊断基线 2745/42（**失败全为钉面、无一条核心行为测试失败**）→ 续接刀吸收）。**读数为总控亲跑**（`08dda87`）：**2929/0/0**；`tests/phase3` **280** / `phase6` **666** / `phase7` **420** / `deletion` **120** 逐值不变、`tests/phase8` **564**；非 phase6·7 不含 deletion = **1723**（1581+142）；ruff clean、mypy **146** 文件 0 error、compileall 0；**head `0017_ledger_event_provenance`（v17）**；60 例 gate 重放与 43 例 planner stress 全绿；`behavioral_baselines/`、`docs/`、`0001–0016`、`tests/architecture/` 零改动。**（以下为 p8-4 开工前的状态记录，保留为历史）**：**p8-3（台账落表 + 删除面 + ReviewEvent 语义）= COMPLETE**：**交付 `ff769e9` + 补 `280adc1` + 处置修 `ddbfd90`；盖 `VR-OPI-6d40862d-…15` PASS 于 `ddbfd90`；处置 `DEC-OPI-6d40862d-…13`；sync SYNCED 156 对象 / rev 159**。**三件**：`TASK-OPI-6d40862d-…9`（任务书 A–H 八裁决 + 迁移钉预写 + 预算 60–140）+ `VAL-OPI-6d40862d-…11`（切片验收七组）+ 处置 DEC。**交付面**：`migrations/0016_planning_ledger.sql`（三表：`planning_ledger`（§14 标量列 + 判别列 `ledger_key_type`）/ `coverage_obligation`（§14 的 11 字段逐字）/ `planning_ledger_event`（`RA §20` 五事件词，CHECK 限词，append-first）；stamp 16；纯 DDL）+ `src/elc/planner/ledger_store.py`（**单短事务**写面：重放判定/基座 `Counter` 多重集校验/键面校验/三表 upsert；`upsert_obligation` 独立面；epoch fence）+ `src/elc/scheduler/review_record.py`（`event_type` 四词声明 + 只读 `engaged`/`evidence_group_id` 的成败语义；**schema 不落词**）+ **删除面同刀落**（三表进 `SWEPT_TABLES`/`LEARNING_TARGET`/`LEARNING_HISTORY` + 六条字面量 + `_learning_target` 新腿）+ `PLANNING_LEDGER_STORAGE` 翻牌 `DURABLE_TABLES_V1`（旧成员保留）+ 三处文案收口 + `tests/phase8/` 五个新文件（+140 例）+ 7 处迁移钉与 1 处模块清单钉。**独立评审（新上下文 executor-pro 只读、仓外 14 组变异）**：**ACCEPT-WITH-FINDINGS，无 HIGH、无 MEDIUM**（2 LOW + 5 INFO；**14/14 变异全 RED、零漏网**）——自写解析器逐字比 §14 与 `PRAGMA`；跨第二连接证同短事务；多重集（自造非时刻序基座）非序列；三 scope 真删 + 家族腿真守 + 零孤儿；`review_outcome_of` 20 格全中；纯核**逐方法体 AST 相同 + 行为面 30 项逐值全等**；四项执行者异议**全判成立**（含**键面第三种形状优于我给的两种**：评审证明复合键必致静默丢行）；并指出 **F1/F2 两处本刀造成的过期表述未扫**。**处置（`ddbfd90`）**：F1 修（`deletion/__init__.py` 的「has no table in app.db」改真：S43/S46/S48 的 ledger 腿已真兑现、**唯 S41 仍登记**）+ 正钉；F2 修（`ledger.py` 两处 `NO_TABLE_V1` 引用改指现役 `ledger_store`/`DURABLE_TABLES_V1`，**只改 docstring**，`ast.unparse` + 注释流自证）+ 正钉；INFO-1/INFO-3 修；INFO-2 确认（§19 第三腿的不可满足是**决定**）；**INFO-4/5/6 登记**（`ledger_store` 缺冷启动钉 / commit message 措辞不准 / 0016 头部 rollup 论证需收窄）。**总控起草缺陷自认四项（本会话累计 11 项）**：钉面漏第 7 处 / 「除这 6 处外不得改任何既有测试」不可满足（词表唯一诚实家是新模块 ⇒ 模块清单钉被迫 +1 名）/ 键面只给两种而第三种更好 / 把两列 rollup 当 `planning_ledger` 的标量列（核心里它们是**行无关字段**）。**读数为总控亲跑**（`ddbfd90`）：**2787/0/0**；`tests/phase3` **280** / `phase6` **666** / `phase7` **420** / `deletion` **120** 逐值不变、`tests/phase8` **422**；非 phase6·7 不含 deletion = **1581**；ruff clean、mypy **144** 文件 0 error、compileall 0；**head `0016_planning_ledger`（v16）**；`behavioral_baselines/`、`docs/`、`tests/architecture/`、`gate.py` 零改动；**纯核剥 docstring 后代码同一**（总控亲验）。**（以下为 p8-3 开工前的状态记录，保留为历史）**：**p8-2（会话预算视图 + 权威化）= COMPLETE**：**交付 `14041d0` + 处置修 `2f0b8e3`；盖 `VR-OPI-6d40862d-…7` PASS 于 `2f0b8e3`；处置 `DEC-OPI-6d40862d-…5`；sync SYNCED 153 对象 / rev 156；**CI 四门 SUCCESS（run `35879933209` / `35881072478` / `35881198788` / `35891404119` / `35920614523`）**。**三件**：`TASK-OPI-6d40862d-…1`（任务书 A–G 七裁决 + 逐字段派生裁定 + 预算 40–110）+ `VAL-OPI-6d40862d-…3`（切片验收七组）+ 处置 DEC。**交付面**：`SessionBudgetView`（`DATA_MODEL §5.2` 十字段逐字、frozen、无默认；`src/elc/teaching/types.py`）+ `src/elc/teaching/budget.py`（**纯派生**：零 SQL/零时钟/零 IO；逐字段带 basis+Revisit；两条**声明常数** `COOLDOWN_WINDOW_SECONDS=1800.0` / `RECENT_TEACHING_WINDOW_SECONDS=3600.0`）+ `TeachingController.get_session_budget_view(conversation_id, as_of)`（**纯读**；策略腿经注入窄端口——§5.1 策略按 user 读而 §3 的 Conversation 无 `user_id` ⇒ user 腿只能在端口上，已独立核实）+ `TeachingController.count_automatic_probe_moments` + `src/elc/runtime/automatic_controls.py`（三控制纯映射：budget ← remaining（`None` ⇒ False）、cooldown ← `>0`、protected ← `flow_priority is FlowPriority.PROTECTED`）+ `TeachingControlFacts.derived(...)` 接入 p8-1 单元 + `shadow.py`/`planner/controller.py` 的 `natural_break_available` **去参**（改由 `PlanningRequest.conversation_priority_view` 派生、无视图 fail-closed）+ registry 入册 `session_budget_view`（OWNER_TEACHING，`version_field="policy_version"`）+ `tests/phase8/` 两个新文件（**+104 例**）。**独立评审（新上下文 executor-pro 只读、仓外 21 组变异）**：**ACCEPT-WITH-FINDINGS，无 HIGH、无 MEDIUM**（4 LOW + 7 INFO；19 组 RED、**1 组真 0-RED = F1**、1 组等价变异）——评审自读 §5.2 逐字比对；**零迁移 + 纯读**以真 app.db 实测（`total_changes` 74→74、44 表逐表不变）；逐字段派生五项边界自测；`natural_break_available` 三态 + 无旁路（手工传关键字两处 TypeError）；`stress_suite` 43/43、60 例 gate 重放不回归。**处置（`2f0b8e3`）**：F1（真 0-RED：忙会话发明疲劳信号 ⇒ 全量 2643 全绿）**修** + 仓外变异自证（唯一 RED 恰为新例）；F2 事实更正（Gate 不读那两个计数，实测 0/0 命中）；F3 `SessionBudgetView` **入册** registry；F4 `shadow.py` 两句过期理由改写；F5/F6 过宽表述收窄；F7 `frontier.py` 模块散文同句已过期⇒**只登记不改**（条目与谓词有意冻结）；F8–F11 登记；F12 工作树 11 个**空目录**以 `rmdir` 清掉；**两条裁定**：①`SHADOW_MODE_MODEL_VERSION` **bump 为 `sh2`**（该常量自述「随读数移动」⇒ 保持 `sh1` 即规则与事实矛盾；同步 4 处、phase7 仍 420）+ ②**显式授权**处置刀修 `gate.py` 三句过期散文（**仅注释、零行为**，`git diff -U0` 每行以 `#` 开头、**剥注释后 AST 同一**）。**总控起草缺陷自认三项（本会话累计 7 项）**：漏 `PlannerService.run_shadow` 的第三处转发面 / `sh1` 消费者枚举漏第 5 处（姐妹 stamp 注释）/ 「只改值」与 phase8 断言钉旧事实冲突。**评审侧自报通道违规 1 次**（仓外探针用 Bash heredoc + `write_text` 写 `.py`）⇒ 训诫留痕。**读数为总控亲跑**（`2f0b8e3`）：**2645/0/0**；`tests/phase3` **280** / `phase6` **666** / `phase7` **420** / `deletion` **120** 逐值不变、`tests/phase8` **280**；非 phase6·7 不含 deletion = **1439**；ruff clean、mypy **142** 文件 0 error、compileall 0；**三刀零迁移**（head 仍 0015/v15）；`behavioral_baselines/`+`docs/`+`0001–0015`+`tests/architecture/`+`tests/conftest.py`+`runtime/controller.py` 零改动；`gate.py` 剥注释 AST 同一；`src/` 下已只剩 `elc/`。**（以下为 p8-2 开工前的状态记录，保留为历史）**：**p8-1（Gate 自动面）= COMPLETE**：**交付 `b43d4bc` + 处置修 `325e7a6`；盖 `VR-OPI-64c88704-…23` PASS 于 `325e7a6`；处置 `DEC-OPI-64c88704-…21`；sync SYNCED 150 对象 / rev 153；**CI 四门 SUCCESS（run `35879933209` / `35881072478` / `35881198788` / `35891404119`，Ubuntu ruff→mypy→compileall→pytest）**。**三件**：`TASK-OPI-64c88704-…17`（任务书 A–G 七裁决 + 单条预写钉 + 预算 50–130）+ `VAL-OPI-64c88704-…19`（切片验收七组）+ 处置 DEC。**交付面**：`src/elc/teaching/gate.py` **只增**两自动 profile——`AutomaticOpenFacts`/`decide_automatic_open`（`OPEN`+`AUTOMATIC`，要求 planner SUCCEEDED+SELECT；deny 面含 `USER_INTENT_BLOCK`(intent≠OPEN)/`AUTO_TEACH_DISABLED`/`HARD_PROTECTED_FLOW`/`AUTO_SESSION_BUDGET_EXHAUSTED`/`HARD_COOLDOWN_ACTIVE`/锁冲突）+ `AutoContinuationFacts`/`decide_auto_continuation`（`AUTO_CONTINUE`；`AUTO_TEACH_DISABLED` **仅当** `moment_consent_class=AUTO_OPENED`；**不受** budget/cooldown；两条 hard cap 带 terminalizing 豁免）+ `AUTOMATIC_OPEN_FACT_KEYS`(8)/`AUTO_CONTINUATION_FACT_KEYS`(7) + 13/15 条可发理由集 + `moment_consent_class_of`（派生读法单点）；**既有两 USER_INITIATED profile 判决级零改动**（评审以 `git archive` 抽旧版并跑 462 例枚举复核：五元组零差异；唯多故障输入的 `GateInputError` **文本**因检查序统一而变，23/231 组、异常类相同、无测试断言）；`src/elc/runtime/automatic_teaching.py`（**558→779 行**，SQL-free）：自动决策单元——planner 半先 durable（p8-0 store）→ 非 SUCCEEDED/SELECT 不进 Gate → `SELECT` ⇒ Gate 自动面 → `ALLOW` 走**既有五件原子单元**（`commit_cp2_open`，`source=AUTOMATIC`）/ `DENY` 只落两件 / `DEGRADED` 只落状态行；`lock_state` 读 durable `active_teaching_lock`；**重放短路**（同 cycle 已有 Gate 事实 ⇒ 纯重放、不问 Gate、不写行，见下）；`tests/phase8/` 三个新文件 + phase3 一条钉（**60 例 benchmark 仓内全量重放 60/60**、自动面 55 例、自动单元 16 例）。**独立评审（新上下文 executor-pro 只读，仓外变异）**：**ACCEPT-WITH-FINDINGS，无 HIGH**（2 MEDIUM + 4 LOW + 6 INFO；**13 组变异 RED、4 组 0-RED** 全部归入 findings）——A 面（最高风险：既有校验被抽成只读 Protocol 共享帮助器）用旧/新双跑 68 例 + 462 组枚举证明判决零差异；B 面自动 OPEN 随机 2 万例零差异、AUTO_CONTINUE 2 万例 468 差异**全为两类蓄意 fail-closed**；C 面三条独立比对腿（自建升级映射 vs 冻结 artifact / repo profile vs 参考 / vs benchmark expected）**均 0 失配**；D 面 12 探针含**跨第二连接在单元中段读同一文件库**（teaching 五表 0 行而 planner 半已 durable）；13/17 变异 RED。**处置（`325e7a6`）**：**F1（MEDIUM）修**——原实现在同 cycle **已有 durable Gate 事实**时仍重算判决 ⇒ 返回值与 durable 相反（ALLOW 后以 `automatic_teaching_enabled=False` 重入报 DENY 而 durable 仍 ALLOW+moment），反方向落 store 抛裸 sqlite UNIQUE 文本 ⇒ 加**重放短路**（`_durable_gate_replay`：durable Gate 事实优先、不问 Gate 不写行；三点合 + 撕裂 SUCCEEDED 以明语 CONFLICT 拒）+ 双向用例 + 「不再调 Gate」兜底桩 + DENY 臂 planner 半 durable 断言；**F2（MEDIUM）修（仅声明）**——「无 durable 权威」漏了仓内既有推导（`feature_assembly.py:254` 的 `TEACHING_FREQUENCY_TO_PROFILE` ← §5.1 durable `teaching_frequency`，经 `TeachingPolicyPort` 进 Planner trace）⇒ 已点名并写明「接线的刀必须传 Planner 组装出的同一值」+「LOUNGE 只抬阈值、不能替代 `AUTO_TEACH_DISABLED`」；**F3/F5/F6（LOW）修**（措辞收窄 + 同族过期表述第三/四处 + `controller.py:213` 指路）；**F4（LOW）修**（`lock_state` 改读 durable 锁 + critical 事实可表达 UNKNOWN ⇒ Gate-DEGRADED 臂从「删掉也没人发现」转为可达 + 变异自证 RED）；**F7/F9/F10（INFO）修**（白名单钉 / 死代码 / 60 例重放守卫）；**F8/F11/F12 登记**（F11 = 新暴露的既有缺口：`ContinuationFacts` 无 `terminalizing_action` ⇒ 冻结参考 §8 豁免在 USER_REQUESTED_CONTINUE 路不可达，触发=下次触碰该 profile；F12 = store 裸 sqlite CONFLICT 文本，触发=触碰 `teaching/store.py`）。**通道违规（本切片两次，均训诫留痕）**：交付刀 1 次（Bash heredoc 写仓内 phase8 测试后 Edit 还原、blob 全 LF 无痕）、处置刀 1 次（`printf >>` 写**仓外副本** .py 变异）⇒ **口径收紧（本切片起）：凡变异/诊断脚本（.py 含仓外副本）一律 Write/Edit 落盘；本切片内第三次违规一律整刀拒收**。**总控起草缺陷自认四项**：①任务书把 benchmark 的 `OPEN+USER_INITIATED` 例数误写 19（实测 **8**）；②F7 要求改 phase3 钉文件与「新用例只落 phase8」自相矛盾；③「SQL-free」措辞不精确（被强制的是直接 import/调用面；传递闭包带 sqlite3 是先于本刀的事实）；④「两处过期表述」实为同族四处（F5/F6 在自述之外，今后派单须要求「同族措辞一次扫全」）。**读数为总控亲跑**（`325e7a6`）：**2541/0/0**；`tests/phase3` **280** / `phase6` **666** / `phase7` **420** / `deletion` **120** 逐值不变（phase3 的 +1 = 新钉）；`tests/phase8` **176**；非 phase6·7 不含 deletion = **1335**；ruff clean、mypy **140** 文件 0 error、compileall 0；**两刀均零迁移**（head 仍 0015/v15）；`behavioral_baselines/`+`docs/`+`0001–0015`+`tests/conftest.py` 零改动；六文件 blob 行尾全 LF。**机制事实（新）**：**Write/Edit 保留目标文件既有行尾**（受控实验：LF 标本编辑后 CRLF=0；CRLF 标本 181 行 CRLF 不变）⇒ 仓内既有 CRLF 工作树文件（如 `teaching/controller.py`，属既有 32 个 CRLF 文件）经 Edit 修改**不会整文件翻行尾**（实测 diff 3/2 行、blob 仍 LF）。**（以下为 p8-1 开工前的状态记录，保留为历史）**：**p8-0（CP2 durability）= COMPLETE**：**交付 `42d1b16`（23 文件 +4099/−27）+ 处置修 `0479cbf`（6 文件 +309/−21）；盖 `VR-OPI-64c88704-…13` PASS 于 `0479cbf`；处置 `DEC-OPI-64c88704-…11`；sync SYNCED 147 对象 / rev 150；**CI 四门 SUCCESS（run `35879933209` / `35881072478` / `35881198788`，Ubuntu ruff→mypy→compileall→pytest）**。**三件**：`TASK-OPI-64c88704-…7`（任务书：A–G 七裁决 + 迁移钉预写清单 6+2+登记类 + 预算带 40–110）+ `VAL-OPI-64c88704-…9`（切片验收七组）+ 处置 DEC。**交付面**：`migrations/0015_planner_records.sql`（§14 四表逐列：`planner_evaluation` 8 列 / `planner_decision` 7 列 / `planner_execution_status` 4 列 / `runtime_decision_outcome` 5 列；三处 CHECK 词表逐字；FK 到 `decision_cycle`/`turn_record`/`planner_evaluation`；stamp 15；**纯 DDL**）+ `src/elc/planner/records.py`（**SQL-free 端口** `PlannerRecordStore` + `PlannerCycleRecords` + **唯一列映射表**（`factor_trace`↔`reason_trace`、`policy_profile_version`↔`policy_version`、`ranked_candidate_ids`↔`ranked_candidates` 的 id 序）+ **9 条带 Revisit 的声明读法**）+ `src/elc/platform/db/planner_store.py`（**CP2 单短事务**：status+evaluation+decision(仅 SUCCEEDED)+反向引用 UPDATE+outcome UPSERT；epoch fence；**重放不重写**、矛盾即 CONFLICT；五读面）+ `src/elc/platform/types.py` 两记录（`PlannerEvaluationRecord` / `RuntimeDecisionOutcome`）+ 删除面四表三 scope（CONVERSATION/LEARNING_HISTORY/ALL_USER_DATA，children-first 在 `decision_cycle` 之前）+ registry 两条目（`planner_evaluation`=OWNER_PLANNER/`planner_execution_status`=OWNER_RUNTIME，`runtime_decision_outcome` 按裁定不入册并就地登记理由）+ `tests/phase8/` **92 例**（真内核链路：真 conversation/CP0 → 真 DecisionCycle 单元行 → `run_shadow` 真内核 → 真落库；三值失败路径零 decision；原子性回滚；崩溃窗口重放；三 scope 清扫）。**独立评审（新上下文 executor-pro 只读，仓外变异）**：**ACCEPT-WITH-FINDINGS，无 HIGH**（1 MEDIUM + 3 LOW + 5 INFO；**12 组变异 11 RED、m7 = 0-RED**）——A 面自写解析器逐列比对 §14；B 面 13 探针在真链路上证「降级零 decision/反向引用 NULL」；C 面**跨第二连接**实测单事务成批可见 + 中期/晚期失败双向回滚；E 面 `decision_cycle` 对 `planner_store` 仅 UPDATE、四表全树唯二写者；G 面 **1158 条既有测试 id 集逐行相同**；H 面 m7 暴露 F1（重放状态臂无测试）。**处置（`0479cbf`）**：F1（MEDIUM）加两条断言式测试（状态词臂 + error_code 臂，含「哪条臂拒绝」的消息判别）并**仓外变异自证 2 RED**；F2 kernel.py judgement 12 加「Superseded in part (P8-0)」取代段；F3 registry 补两条目；F4 登记重放 `reason_codes` 的不对称 + 一条钉；F5/F6/F7 头注精度与 `factor_trace` 的 Revisit；F8 两元素字节钉纵深；F9（裸 `str` 状态抛裸 `AttributeError`）**DEFER**（触发=下次触碰 `planner_store.py`）。**总控起草缺陷自认两项**：① 任务书 F5/F6 子句误称 `PRE_0015_DIGESTS` 钉着 0015 摘要（该常量只含 0013/0014；**执行者据实驳回并按正确处置执行**）；② 裁决 E 措辞过宽（「SQL 只在 platform/db」非全仓架构——被强制执行的是 **Gate 2 的 runtime 零 SQL**；`DISPATCH_PREFIXES` 钉的是**一次 run 新增**的模块集而非包闭包，`import elc.planner` 在父提交即已含 `elc.runtime`/`elc.platform.db`/`sqlite3`）。**执行者侧故障留痕**：首次派单 `executor-pro` 报 `Insufficient Balance`（第 5 次执行者侧中断）⇒ 同刀重派 `executor-flash`（任务书已把裁决前置）。**读数为总控亲跑**（`0479cbf`）：**2456/0/0**；`tests/phase6` **666** / `tests/phase7` **420** / `tests/deletion` **120** 逐值不变；`tests/phase8` **92**、非 phase6/7 不含 deletion = **1250**；ruff clean、mypy **139** 文件 0 error、compileall exit 0；迁移 head **0015_planner_records（v15，本刀零新迁移于 0015 之后）**；`behavioral_baselines/`+`docs/`+`0001–0014` 零改动；六文件 blob 行尾全 LF。**（以下为 p8-0 开工前的状态记录，保留为历史）**：**PHASE 8 = 开工第 0 步完成（三件已铸）**：**开工 DEC `DEC-OPI-0f76024b-…3`**（前沿裁决 R1–R7：R1 CP2 落库与形状（§14 四表 + 0006 预留反向引用 + 崩溃窗口）/ R2 Gate 自动面**只增不改**（既有两 USER_INITIATED profile 语义零改动；`OQ-024A` 无载体 ⇒ 60 例 gate benchmark 为可执行替代）/ R3 `SessionBudgetView` 与 `natural_break_available` 权威化 / R4 台账落表连删除面语句 / R5 rollout HOLD 与门槛 / R6 迁移面预写 / R7 挂账不变）+ **母计划 `PLAN-OPI-0f76024b-…5`**（六工作项：`p8-0-cp2-planner-durable` / `p8-1-gate-automatic-open` / `p8-2-session-budget-continuation` / `p8-3-ledger-durable-review-events` / `p8-4-automatic-integration-stress` / `p8-5-rollout-gate`（RISK_SPIKE）；八关系：p8-0 → p8-1 → {p8-2, p8-3} → p8-4 → p8-5，p8-2 MAY_PARALLEL p8-3）+ **相位验收 `VAL-OPI-0f76024b-…7`**（八组：CP2 durability / Gate 自动面 / 自动路径与 10B 原子性 / `OQ-024A` 60 例替代 / SessionBudget 与 natural_break / 台账落表与删除面 / 不回归与门 / 诚实性与 rollout HOLD）。**实现面现状（开工取证）**：`decision_cycle`（0006，**已预留 `planner_decision_id`**）、`gate_decision`、`gate_execution_status`、`teaching_moment`、`active_teaching_lock`、`generation_action_intent` **均已有表**；**§14 四记录无表**（⇒ p8-0 的迁移）；`gate.py:52–55` 自述自动分支属 Phase 8 且未实现；`runtime/controller.py:611` 载 CP2 现归 teaching controller（user-initiated 面）。**PHASE 7 = COMPLETE**（见下）；**p7-4**：交付 `f66cc2d`（8 文件 +2484/−29：新增 `src/elc/planner/shadow.py`（IMPLEMENTATION_PLAN §8 影子运行：`PlanningRequest` 装配 → `kernel.plan()` → 影子记录 would_have_selected/why/execution_status/runtime_decision_outcome；**零派发**——不 import 派发面、不建 `TeachingMoment`、不写 store、不产文本，四条钉含真 app.db 与冷启动溯源）与 `stress_suite.py`（冻结 43 例仓内重放：判定语义逐层复刻 `run_stress.py`；**三条已知分歧显式建模**——S04/S42 由适配层边界拒（BF-02 §4「Kernel 接收到重复 `canonical_key` 属于 input contract error」）、S20 由运行器**先读** §5 两词；error 6 例**分类断言**；§20 向量按**输入**读 + 合成 §5.2 行入册）+ `PlannerService.run_shadow` 实装（`plan`/`get_planner_evaluation` 仍诚实拒绝）+ 33 例）→ **首轮执行者被 API 余额中断**（第 4 次「执行者遗留先例」）→ 总控仓外快照 + 定位**未还原变异**（`stress_suite.py` 的 `MUTATION-3`）→ 续接刀**吸收** → 处置刀 `5e33d0d`（4 LOW 已修 + F5/F6 留痕 + F7/F8 登记）→ **独立评审（新上下文 executor-pro 只读）ACCEPT-WITH-FINDINGS，无 HIGH/MEDIUM**（4 LOW + 4 INFO；**16 组变异 + 3 组篡改用例**，4 组 0-RED 已逐条转 findings；**并与冻结参考实现逐例交叉验证 43/43 一致 + 36 个计分例 utility 逐值相等**；as-found 重建逐数复现 8 failed/2351 passed）→ 总控亲跑 **2364/0/0**（非 phase6/7 不含 deletion = **1158 逐值不变**；phase7 = **420**；mypy **137**/0；零迁移；冻结面零改；43 例重放 exit 0）→ 盖 **`VR-OPI-44647873-…35` PASS 于 `5e33d0d`（首提即过）** → sync SYNCED（**139 对象**）。**p7-4 的裁决/处置 DEC**：`DEC-OPI-44647873-…29`（影子=纯记录零派发 / 43 例走独立适配层 / 三分歧显式建模）+ `DEC-OPI-44647873-…36`（含**两条既有测试钉搬迁的定谳**：架构门 `PlannerService` 整类毕业 + phase6 mention 钉增列 `planner/shadow.py`，均纯新增且有行级论证；**通道口径裁定**「段①禁的是用 Bash 落盘源码，只读内联 heredoc 不构成违规」；通道合规字节层仍 **UNCERTAIN**；**总控起草缺陷一项**：任务书 mypy 基线误写 133（实际 137））。**p7-3（frontier + ledger + coverage）= COMPLETE（前一会话）**：交付 `821b247`（新增 `src/elc/planner/frontier.py` 571 行 + `ledger.py` 1253 行：**七元并集**与 14 源词映射（声明读法）+ **次序裁决落地**（§19 位置；前沿成员 = kernel 第 4 步幸存者集；**硬资格被排除者不是成员**；`KernelStep` 十一步零改动）+ `PlanningLedger` 列集与 `coverage_obligations[]` **十一字段**逐字（「不记录 mastery」）+ 五事件词表（**`SELECT != exposure`**、**`CoverageDebt 不因 selection 自动偿还`** 两句各带断言）+ 暂停不累计（`UNAVAILABLE_IN_CURRENT_RUNTIME`）+ `overexposure`/`coverage_service_state` 读台账（阶梯与曝光窗为**声明常数**）+ `PlannerEvaluation.frontier_candidate_ids` **追加式**落 `DATA_MODEL §14` 列 + `candidates.py` 台账腿（**不传台账时字面量与 `SourceGap` 逐字不变**））→ 执行者**自报 1 次通道违规**（Bash heredoc + `io.open(...,'w')` 回写 `tests/phase7/test_p7_3_frontier.py`）＝同一刀第 1 次 ⇒ **训诫留痕**；评审 G 面判 **UNCERTAIN**（10 文件全 LF 无 BOM ⇒ 字节无痕、机制不可判，与 p7-2 同型）；处置刀与收口刀 **0 次违规** → **独立评审（新上下文 executor-pro 只读）ACCEPT-WITH-FINDINGS，无 HIGH/MEDIUM**（**3 LOW + 4 INFO**；**13 组变异全断言式 RED、无 0-RED**；B 面自写解析器现读 canonical `FAILS: 0`；C 面仓外探针真链路 PASS；E 面旧/新树 **1158 子集测试 id 逐行相同**；H 面 kernel 效用/Pareto/tie-break 零改动）→ 处置修 **`51a9868`**（F1 列块引文 §13→§14 共 14 处 / F2 六处显式 `Revisit` / F3 「两半皆完备」收窄为真（构造器只保互斥、覆盖性是 `frontier_of` 的属性）+ 2 条正向钉（变异自证 10 RED / 2 RED）/ F4 位置前提登记）+ 收口 **`b7b29f3`**（异议-1 同族引文滑移，差 5 字节）——**零行为改动有 AST 佐证**（剥 docstring 后 `ast.dump` 同一）→ 总控亲跑 **2328/0/0**（非 phase6/7 不含 deletion = **1158 逐值不变**；phase7 = **384** = 299+83+2；phase6 = 666；deletion = 120）、ruff clean、mypy **135**/0、compileall 0、**零迁移**、冻结面零改、`tests/phase7` 改动面 = **纯新增**（605/919/520 行、零删除）→ 在 `b7b29f3` 盖 **`VR-OPI-44647873-…23` PASS（首提即过）** → sync SYNCED（**135 对象**）。**p7-3 的裁决/处置 DEC**：前沿裁决 `DEC-OPI-44647873-…16`（R1 位置=§19 且成员=第 4 步幸存者；R3 台账 = 纯核 + 显式登记「无表」（依据：Phase 7 无 exposure 写者 + `SECURITY_PRIVACY_DELETION_CONTRACT` 把 PlanningLedger user history 列为可删内容）⇒ 零迁移；R4–R6 计提/偿还/读数；R7 UNKNOWN 不转义）+ 处置 `DEC-OPI-44647873-…25`（含**总控起草缺陷自认两项**：任务书 F1 明文与自证口径冲突、F2 示例节号滑移）。**第十五会话补铸的历史 DEC（此前 `decision_add` 拒收，本会话已恢复）**：门二 `DEC-OPI-44647873-…4` / 外评采纳 `…7` / P7-0 `…10`（含「首交同刀两次违规整刀拒收 → 重交 as-found 吸收 → 通道合规 UNCERTAIN」三项定谳）/ p7-1 处置 `…12` / p7-2 处置 `…14`。**p7-2（候选供给侧）= COMPLETE（前一会话）**：交付 `4c6afb7`（`src/elc/planner/{candidates,scope,supply}.py`：**Track A 六源 / Track B 八源**逐字（`PRODUCT_CONTRACT:351–382`）+ 单入口「两 lane 进同一 Planner」+ `UserIntentScope` 解析（`TARGETED` 真起范围约束）+ `ConversationPriorityView`（§13 三字段；**只作视图，Gate 留授权**）+ prerequisite resolver + readiness 门（**缺/UNKNOWN ⇒ 不可用或 `missing_authorities`，绝不当 R0/0**）+ 76 例；`40ffa5c` 补 §19 vs §10.1 次序差异登记）→ 执行者**自报 1 次通道违规**（Bash `io.open(...,'w')` 回写 `candidates.py`；评审按行尾推断：**文本模式整体回写必留 CRLF，而实测全 LF** ⇒ 「写入动作自报属实但字节无痕、机制 UNCERTAIN」，**第 1 次＝训诫留痕**）→ **独立评审 ACCEPT-WITH-FINDINGS 无 HIGH**（**F1 MEDIUM = 前置解析器与真实 `prerequisites_of` 面契约不符**：真面 `from_node OR to_node` 双向且不筛 `edge_type`，实现假定只返入边 ⇒ 目标自身是别人的前置时伪 BLOCKED/UNKNOWN；今日 `edges: []` 故潜伏；其测试假图单向 ⇒ **掩蔽**了该缺陷；F2 引文精度 / F3 §11 两形状折叠 / F4 GS22 跨源 merge 永不触发（已登记）/ F5/F6 INFO；8 组变异 7 RED）→ 处置修 **`53b330e`**（F1 新 `_is_a_prerequisite_of`（`edge_type ∧ to_node`）+ skip 计数 + 3 条真面语义探针 + **变异自证 2 RED** / F2 三引文校正到资产真值 / F3 保留 `UNKNOWN` 可见（+2 探针、变异自证 2 RED）/ F5 删无人读字段 / F6 字段来源登记 / F4 revisit 补 GS22 条件）→ 总控亲跑 **2243/0/0**（非 phase6/7 不含 deletion = **1158 逐值不变**；phase7 = **299**；mypy **133**/0；零迁移；冻结面零改）→ 在 `53b330e` 盖 **`VR-OPI-fc9d8ca7-…20` PASS** → sync SYNCED（**126 对象**）。**环境事实（评审侧，须留痕）**：p7-2 评审上下文里 **dmcp `design_get` 对任何对象返回 `OBJECT_NOT_FOUND`**（连 `design_status` 自列的旧 id 亦然）⇒ 评审只能以 canonical 原文 + 仓库实测为准；**p7-2 的 dmcp 侧正文（TASK/VAL）未被该评审读到**。**p7-1（Planner kernel + trace）= COMPLETE**：交付 `f79a36d`（`src/elc/planner/kernel.py` 2160 行：§10.1 十一步按序 + 硬规则 + BF-02 §12 权重 + Pareto/tie-break + 可审计 trace；三个新测试文件 +110 例）→ 执行者**自报 1 次通道违规**（Bash heredoc 删两行未用 import；**独立指纹证实**：同刀新建文件全 LF 而该文件 CRLF ⇒ **「新建文件 CRLF = Windows 文本模式写入」是可检测的通道指纹**；内容无损，按现行规则「第 1 次＝训诫留痕」处置，并**自认总控任务书口径（「1 次即拒收」）与本仓现行规则（「同一刀第二次」）不一致**）→ **独立评审 ACCEPT-WITH-FINDINGS，无 HIGH**（**F1 MEDIUM = near-tie 触发规则未被任何测试真钉**（变异 m5 曾整仓 0 RED）、**F2 MEDIUM = 冻结 43 例本刀未跑**（评审自建适配器实测 40/43；三条分歧：S04/S42 合并 vs `error:true`、S20 契约校验先于上下文裁决）、F3/F4/F5/F7 LOW、F6 INFO；13 组变异 12 RED）→ 处置修 `d876ed5`（F1 重建**非支配 + 超窗**对 + **变异自证 2 RED** / F4 judgement 1·3·4·6 补 `Revisit` + 按条目钉 + 两条恒真断言换强形态 / F2 三分歧行为钉 + judgement 8–10 / F3 judgement 11 / F5 judgement 12 / F7 judgement 13 / F6 judgement 14(a)–(e)）→ 总控亲跑 **2162/0/0**（非 phase6/7 不含 deletion = **1158 逐值不变**；phase7 = **218**；mypy **130**/0；零迁移；冻结面零改）→ 在 `d876ed5` 盖 **`VR-OPI-fc9d8ca7-…8` PASS** → sync SYNCED（**124 对象**）。**留待下一刀**：`test_opportunity_expiry_outranks_cost_criteria` 的候选对**同为 Pareto 可比**（其 selected 亦「因错误理由」成立、对 F1 变异不敏感）——处置时已登记、未改。**Phase 7 母计划/相位验收**：`PLAN-OPI-2394139d-…8` / `VAL-OPI-2394139d-…10`（其 ⑤ 记 `OQ-023A` 无载体 ⇒ 以 43 例为可执行替代）。**P7-0 = COMPLETE（消费边界冻结：交付 + 评审 + 处置 + 盖章）**：首交 `f4350ce`+`7e3b7e4` 因**同一刀两次通道违规**判**整刀拒收**并回退 → 重交 `e388c4c`（执行者在写回执前 API 余额中断；按「执行者遗留先例」**as-found 吸收**）→ **独立评审（新上下文 executor-pro 只读）ACCEPT-WITH-FINDINGS，无 HIGH**（F1 MEDIUM = 键列脏行探针漏盖：by-key 面 `Ok(None)`/`Ok(False)` vs list 面 `Err`；F2/F3 LOW = OFF 映射标签与若干 `Revisit` 缺席；F4/F5 INFO；**10 组变异全断言式 RED**）→ 处置修 **`38ca076`**（F1 `CAST(...AS TEXT)` 探针 + `DIRTY_VALUES` 四列→六列 / F2 标签与钉扩成四行 / F3 六处 `Revisit` / F4 `fa1` 登记为 module-level only / F5 顺序声明）→ 总控亲跑 **2047/0/0**（非 phase6/7 不含 deletion = **1158 逐值不变**；phase7 = **103**；mypy **129**/0；**零迁移**；冻结面零改）→ 在 `38ca076` 盖 **`VR-OPI-2394139d-…5` PASS** → `design_sync` SYNCED（**120 对象**）。**两项必须留痕**：(1) **通道合规无回执**——重交的全部目的即通道合规，而 attestation 缺失；评审查了行尾/编码/BOM/mtime 序列/与首交 24/25 逐字节相同等旁证后判 **UNCERTAIN**（证据倾向合规，但不可认证）；(2) 评审确认 p6-2 F-2 的描述更正成立——**未复现** `AttributeError`，实为「静默 `Ok(None)`/`Ok(False)` vs `ValueError`」。**机制事实（新）**：`--collect-only -q` 因 `pyproject.toml` 的 `addopts="-q"` 变 `-qq`，**总数会被抑制**（须写 `-o addopts=""` 才打印 `N collected`；`python -m pytest -q` 同理）。**外评采纳落位（本会话前半）**：`PLAN-OPI-b99560d4-…32`（3 VERTICAL_SLICE + 2 RISK_SPIKE）+ `VAL-OPI-b99560d4-…34`（验收六组）+ `TASK-OPI-b99560d4-…36`（**外评八条取证 + 判定 + 裁置全文在任务书里**）。（交付/评审/处置/盖章的完整经过见本行开头的 P7-0 块；`decision_add` 契约本会话**连续 9 次**拒收 ⇒ **门二 DEC 与外评采纳/P7-0 DEC 均待补铸**）。**门二 = COMPLETE**（BF-05 source-aware 删除与墓碑：交付 + 评审 + 处置 + 盖章）。**门二（本会话）**：开工 `PLAN-OPI-b99560d4-…14` + `TASK-OPI-b99560d4-…19`（**四查 + 裁决 R1–R15 全文在任务书里**）→ 执行者交付 `c0b1733`+`bdca3cb`（migration **0014_deletion_tombstone**/v14 + `src/elc/deletion/` 七文件 + `tests/deletion/` + **27 处既有迁移钉搬迁**）→ 总控亲跑 **1944/0/0**（非 phase6 **不含 deletion = 1158 逐值不变**；phase6 = 666；deletion = **120**）、ruff clean、mypy **126** 文件 0 error、compileall clean、head **0014/v14**、冻结面零改 → **独立评审（新上下文 executor-pro 只读）：ACCEPT-WITH-FINDINGS，无 HIGH**（A–H 全 PASS；6/7 变异组断言式 RED，m7 暴露 F3；2 MEDIUM = F1 恒真断言 / F2 §19 profile 腿不可达）→ **处置修 `4ce43ed`**（F1 改正 / F2 登记 + 钉住现状 / F3 越界删除 probe（仓外变异实测 RED）/ F4 §27 receipt 取舍登记 / F5 六处 writer 补偿断言 AST 化 / INFO-1 PlanningLedger 无表登记）→ 在 `4ce43ed` 盖 **`VR-OPI-b99560d4-…28` PASS**（验收件 = **取代件** `VAL-OPI-b99560d4-…26`）→ `design_sync` REBUILD+CHECK **SYNCED（117 对象）**。**执行者自报 1 次通道违规**（Bash heredoc 直写 `tests/deletion/test_deletion_probes.py`，已用 Edit 修复、未入提交；评审核无最终正确性影响 ⇒ 按「同一刀第 1 次＝训诫留痕」）。**机制事实（新，勿再踩）**：`decision_add` 契约本会话**连续 6 次**以「`expected` missing + unexpected」双错拒收（同一形状在 `plan_build`/`task_build`/`validation_define` 全部通过）⇒ **门二 DEC 未铸**，其内容已回填本文件 + 门二任务书；下次会话**先试补铸门二 DEC**，若仍拒收则继续以 `TASK-…19` + 本文件为权威。**PHASE 6 COMPLETE（工程四刀 + 相位收口均已毕）**——**p6-0/p6-1/p6-2/p6-3 均 COMPLETE**（见下三块与 p6-3 块）；**相位收口已毕（本会话）**：处置 `README` src 行 metadata（`97b0dc7`，p6-0 F-8 挂账项）→ 铸同题扩版相位 VAL `VAL-OPI-b99560d4-…6` **取代** `VAL-OPI-a68fd9eb-…44`（其 ⑤「migrations 增至 0011」句已假；扩版改 **0013/v13** 并新增 ⑥ `PlannerConstraint` §9 组、补 1157→1158 自证钉对账、路径集扩入 README.md/AGENTS.md）→ 在**最终验证过的提交** `97b0dc7` 盖章 **`VR-OPI-b99560d4-…8` PASS**（首提即过，无 INCONCLUSIVE）→ 收口 `DEC-OPI-b99560d4-…10`（四刀链 + 取代关系 + 遗留账七项接管 + **门二四查预告**）→ `design_sync` REBUILD+CHECK **SYNCED（113 对象）**。收口读数（总控亲跑，`97b0dc7`）：**1824/0/0**（非 phase6 = **1158** = 1157 + 1 条自证钉；phase6 = 666 = 70+155+252+189）、ruff clean、mypy **119** 文件 0 error、compileall exit 0、迁移 head **0013_planner_constraint（v13）**、**历史迁移 0011/0012/0013 纯新增 0 删除**、冻结面零改动；**下一动 = ①门二（BF-05 source-aware 删除与墓碑，IP §16 DoD #25）独立一刀**（位次 = Phase 6 收口后、Phase 7 开工前；`DEC-…6259f6fd.…8` R14 在册）；**②再后 = Phase 7（Planner shadow mode + ActiveLearningFrontier）开工第 0 步**。**p6-3 已 COMPLETE**（`PLAN-OPI-5a0be06d-…13` 的 `p6-3-planner-constraint`）：交付 `ec825b0` + 处置修 `4a941a1`；切片验收 `VAL-OPI-5a0be06d-…18` 盖章 **`VR-OPI-5a0be06d-…22` PASS**；**独立评审（新上下文 executor-pro 只读）：ACCEPT-WITH-FINDINGS，A–H 全 PASS、无 HIGH**——自写解析器 6 项逐字复核、12 条迁移摘要双算全等、自建探针在真 app.db `FAILS: 0`、**7 组变异全断言式 RED**（并**再现**了执行者自报「首轮漏网」的半腿组：单形状逃逸、双形状捕获 ⇒ 扩例是承重的）；**L-1（scope 封闭性表述过强）/L-2（写读面不对称未说明）/INFO-1（引文范围）/INFO-2（后继钉写法）四条已修**（`4a941a1`：0013 头注释把 Types「钉住的列表」与 Scope「**例如**⇒封闭是本刀 schema 选择」拆开陈述 + 2 条反向钉；写面 docstring 写明「读出时校验 + 恢复路径 = flag 面」；引文 586–607→586–615；后继钉改字面量）、**L-3（预算带）留痕并裁定**（实测 +896/+753/+285 三口径 ⇒ 预算数字是估算、不返工，但今后任务书写预算带**须写明计数口径**）、INFO-3–INFO-7 留痕（含 **DEFER**：预探针在 fence 前，无写入故无正确性影响，归下次触碰 `user_config/store.py` 的刀）。**交付面**：`migrations/0013_planner_constraint.sql`（§9 九列逐字 + 四类型/三 scope 落 CHECK + FK + stamp 13，只 DDL）+ `elc/user_config`（两 StrEnum + `PlannerConstraint` + 5 面：`record_planner_constraint`/`set_planner_constraint_active`/`get_planner_constraint`/`active_constraints`/`active_constraints_for_target`）+ registry 条目 + **迁移代号钉 22 处 1:1 化入 `tests/conftest.py` 共享常量**（+1 自证钉）。读数（修后，总控亲跑）**1824/0/0**（非 phase6 = **1158** = 1157 + 1 条自证钉；phase6 = 666）、ruff clean、mypy **119**、compileall clean、迁移 head **0013_planner_constraint（v13）**、冻结面零改动；design_sync 已 SYNCED（111 对象）。**本刀教训（总控起草缺陷，已在 `DEC-…5a0be06d.…23` 承认）**：任务书 §6 把总数 1638 误写成「非 phase6 收集数」（正确基线 1157）⇒ **任务书里的数字面也要自洽**（与 p6-0 F-1 同族）。**p6-0 已 COMPLETE**（三对象按 canonical §5.1 落地 + migration 0011/v11；盖 `VR-OPI-a68fd9eb.…52` PASS；处置 `DEC-OPI-a68fd9eb.…57`）；**p6-1 已 COMPLETE**（`PLAN-…42` 的 `p6-1-scheduler-durable-core`）：交付 `0c19e36`+`ff58611`（§5.2 `ScheduleItem`/`ReviewEvent` 逐字 + migration **0012**/v12 + store/controller/接线 + F-4/F-7 兑现 + tests/phase6 净 142 例）+ 评审处置修 `491f691`（+13 例）；切片验收 `VAL-OPI-6259f6fd-…10` 盖章 **`VR-OPI-6259f6fd-…14` PASS**；**独立评审（新上下文 executor-pro 只读）：ACCEPT-WITH-FINDINGS，A–H 全 PASS、无 HIGH**——12 组变异全被测试捕获（均断言式 RED）、自建探针在真 app.db 33 条（32 PASS + 1 FAIL=F-1）、三层逐字比对（doc 块 ↔ `dataclasses.fields` 顺序 ↔ `PRAGMA table_info`）、collation 独立实验（Python max == SQLite `BINARY`，`NOCASE` 会给出相反结果）；**1 MEDIUM 已修**（F-1 `review_event` 静默 `created_at` 使同 id 同内容重试被判 `CONFLICT`——照 `ScheduleItem.updated_at` 对称做法改为「未声明时间戳在重放时是通配、已声明须逐字相符」，总控亲验修前反例复现/修后转绿且反向仍拒）+ **2 LOW 已修**（F-2 四外键统一前置探针→`NOT_FOUND` 且 DomainError 消息零裸 sqlite 文本；F-3 F-7 字节序边界入 docstring + 显式钉）+ **7 INFO 留痕**（F-4..F-10，含 ScheduleView 两桶改名 `due_*targets`→`due_*items`、第三处测试改名、R3 残留扫描范围声明、BF-05 登记载体、F-6 大小写扫描 DEFER、F-9 推送后 CI、F-10 typing-only 挡枚举）。**处置与全量留痕见 `DEC-OPI-6259f6fd-…16`**。读数（**p6-1 收口时**，总控亲跑）**1382/0/0**（非 phase6 = **1157 逐值不变**；phase6 = 225 = p6-0 的 70 + p6-1 的 155）、ruff clean、mypy **118** 文件 0 error、compileall clean、迁移 head **0012_schedule_review（v12）**、冻结面零改动；design_sync REBUILD+CHECK 已 SYNCED（102 对象）。**p6-2 已 COMPLETE**（`PLAN-…42` 的 `p6-2-due-and-schedule-view`）：交付 `c4a6415` + 处置修 `63c8064`；切片验收 `VAL-OPI-5a0be06d-…4` 盖章 **`VR-OPI-5a0be06d-…8` PASS**；**独立评审（新上下文 executor-pro 只读）：ACCEPT-WITH-FINDINGS，A–H 全 PASS、无 HIGH**——**5 组变异全被断言式 RED 捕获**（四步派生 / 内容寻址版本 / 视图桶 / 零时钟 / 共用尺子）、**8 个自建探针在真 app.db 全绿**、**真教学尝试链上产出真 freshness anchor**（证伪了执行者回执的过宽表述）、断钉面逐行审「无删函数、无断言放松」（仅 2 处真值翻转且带补偿断言）；**F-1/F-3/F-4 三条 LOW 已修**（桶序公开面写「stored spelling」+ 混合 offset 钉 / phase2 测试 1:1 改名 / `spacing.py` 节号 `§11`→`§13` + 短语钉）、**F-2 LOW DEFER**（严格解码抛裸异常的触发条件 = 下次触碰 `scheduler/store.py` 的刀或 Phase 7 DEGRADED 装配；总控亲验：同一行脏数据在两个读面抛**两种不同**裸异常 `AttributeError` vs `ValueError`）、F-5–F-10 INFO 逐条留痕（含 `0012` 头部 `event_type` 词表的**过期预言**、`is_review_due` 含 OVERDUE **已追认**、anchor 为 target 级 / 行为模态键级）。**交付面**：`src/elc/scheduler/spacing.py`（**纯策略**：四步派生 + 内容寻址 version + BF-02 四锚 urgency，零 SQL 零时钟）+ controller 三法实装（`recompute_schedule_item` / `get_schedule_view(as_of)` / `is_review_due`）与 **P6-2 指针删除** + `ScheduleView` 加 `as_of` + Learning query face 1:1 增量 `get_learning_watermark()`（`FreshnessView` 字段集不变）+ `store.list_schedule_items()`（零解释读）；**零迁移**。读数（修后，总控亲跑）**1638/0/0**（非 phase6 = **1157 逐值不变**；phase6 = 481 = 70 + 155 + 252 + 断钉净 +4）、ruff clean、mypy **119** 文件 0 error、compileall clean、迁移 head **0012_schedule_review（v12）**、冻结面零改动；design_sync REBUILD+CHECK 已 SYNCED（106 对象）。**下一动 = ①p6-3（`PlannerConstraint` / suppression scope —— IP §7 明列的 Phase 6 末项，按 `DEC-…5a0be06d.…2` R11 归 user_config；**按红线 31③ 先 `plan_build` 铸其计划工作项再铸任务书**）→ ②Phase 6 相位收口**（须以**同题扩版 VAL 取代 `VAL-…a68fd9eb.…44` ⑤「migrations 增至 0011」句**并重签；R18/`DEC-…9` 已预告）。**流程偏离留痕**：p6-2 实际按「复核 → 评审 → 处置修 → 盖章」走（书序是「复核 → 盖章 → 评审 → 处置」），已在 `DEC-…9` 自报，下刀回到书序。Phase 5 已工程 + 语义双 COMPLETE（以下为其记录）。**PHASE 5 COMPLETE（工程 + 语义均已重签）**——P5-R（Semantic Repair）已毕：交付 `f635ea2` + 处置修 `8fa1eba`（F-1 声明精度）/`7039889`（F-2 README 收窄）；切片验收 `VAL-OPI-a68fd9eb-…24` 盖章 **`VR-OPI-a68fd9eb-…29` PASS**；**相位语义验收重签** `VAL-OPI-a68fd9eb-…34` 盖章 **`VR-OPI-a68fd9eb-…36` PASS**（取代 `VAL-…4d516e4f.34` 中被证伪的②③表述）；处置与取代声明见 `DEC-OPI-a68fd9eb.…32`。读数 **1157/0/0**（761 既有零放松 + 396 phase5）、ruff clean、mypy 117、compileall clean、migrations 0010/v10 零新增、冻结面零改动。**P5-R 语义总结（引用读数前必读）**：①9 条 seed REALIZES 全 `CURRICULUM_MAPPED`（未经课程审核）且 **capability credit 仅在 `editorial_status=CANONICAL_APPROVED` 时成立** ⇒ 语料下教学侧 ALTERNATIVE_SUCCESS **只产 RESOURCE NEUTRAL 单条诚实 claim**；②静默证据两层化——**仅 RESOURCE + 整句 form 类命中**才铸证据，CAPABILITY/slot/嵌入/引用/转述/元语言/否定一律 **observation-only（零 claim 零 state）**；`evaluator_confidence=0.60`（BF-01 §10 门 0.50 之上、strong retrieval 门 0.70 之下）；③readiness 按 canonical 原文 **fail-closed**：R0 三项（含 assessment_membership）、R1 四 facet（pos/sense/basic_definition/forms），**语料 14/14 报 `level=None`**。**独立评审（executor-pro，只读）结论**：ACCEPT，A–H 全 PASS 无 HIGH；4 组变异全被测试捕获（14/13/15/4 FAILED）；自建 12 句 shipped 路径探针逐句符合准入门；基线 1088 已由评审用 `git archive` 纯净副本独立复现。**下一步 = Phase 6（Scheduler + Goal Portfolio）——开工第 0 步已完成**：**开工 DEC `DEC-OPI-a68fd9eb-…40`**（四条前沿裁决：ReviewState 词表取 canonical 四词 / §5.1 三件完整替换 / ScheduleItem·ReviewEvent 按 §5.2 新建 / 无 review 状态机⇒转移语义作**派生判据**入册；范围 p6-0 Goal·Policy·Focus、p6-1 Scheduler durable core、p6-2 due·ScheduleView；不变量含 freshness≠due、模态键、append-first、local-only、TEXT_PRODUCTION 不产 voice/audio debt）+ **母计划 `PLAN-OPI-a68fd9eb.…42`**（p6-0 MAY_PARALLEL p6-1，p6-1 MUST_PRECEDE p6-2）+ **相位验收 `VAL-OPI-a68fd9eb.…44`**（六组，路径集 `{src/elc, migrations, tests}`）已铸；**p6-0 已 COMPLETE**（按上方 `PLAN-…42` 的 p6-0 工作项）：交付 `e55a4a6`（三对象按 §5.1 完整替换 + migration 0011/v11 + 接线 + tests/phase6 70 例）+ 处置修 `da04ec2`（评审 F-2/F-3，仅 tests/phase6）；切片盖章 **`VR-OPI-a68fd9eb.…52` PASS**（首提即过）；处置 `DEC-OPI-a68fd9eb.…57`——**独立评审 A–H 全 PASS 无 HIGH**，4 组变异全被测试捕获，自建探针在真 app.db 复现版本纪律/拒绝路径/幂等/append-first/epoch fence；**F-1 是总控任务书缺陷并已追认**（14 处既有测试修改 = 12 迁移 stamp 钉 + 2 毕业钉，1:1 机械搬迁；教训：今后每刀涉及新迁移时，stamp 钉搬迁必须在该刀任务书里**预先写明**）。**下一刀四查必答（p6-1 开工前立）**：F-4（`effective_from` 的 `""` 哨兵 vs `str | None`，接线读书面前须裁定）+ F-7（SessionFocus 无 per-conversation 读面，接 Planner/Persona 视图的刀必须补增量读方法）+ SessionFocus **append-first 属派生判据**（§5.1 无版本列）——**三项均已由 p6-1 兑现**（F-4 登记 + F-7 三处读面 + 字节序判据）。开工取证：`learning/queries.py:36 get_freshness`（`last_strong_retrieval_at`/`stability_band`）是 Scheduler 的真读输入（非夹具）——**p6-2 的 due 决策读面即此**（p6-1 未消费它，属 p6-2）。**历史记录（其语义断言已被 `DEC-…a68fd9eb.32` 取代）**：**Phase 5 工程收口**（原）——**PHASE 5 = 工程 COMPLETE / 语义验收曾撤回、已由 P5-R 重签**（外评采纳 `DEC-OPI-a68fd9eb.…15`，三个 semantic blocker 经总控逐条取证成立：①**curriculum linkage 批准无据**——links.json 9/9 条 CANONICAL_APPROVED+primary_flag 而 rationale 自述依据是 P3 测试 fixture，且 credit 路径 `content/store.py:343-355` 不筛 editorial_status、capability 实体本身无功能定义（只有单一 exemplar+slots）⇒ REALIZES 在源头不可证伪；②**silent evidence 把「命中确定性」当「表现确定性」**——`evaluator_confidence=1.0` + resolver 接受嵌入跨度（引用/转述/元语言/否定可铸 SPONTANEOUS SUCCESS 四维强证据），canonical 依据 PRODUCT_CONTRACT §2.5「用户复述刚显示的完整答案」≠会了、BF-01 §10 evaluator_confidence 是判据置信；③**R0–R4 误报**——R1 要求 POS/sense/basic definition/forms（语料全无）、R0 第三项 assessment_membership 被实现列为 DECLARED_ABSENT，严格三件读法下连 R0 亦不满足）。**（历史记录）P5-R 四工作项（已全兑现）**：四工作项 = curriculum truth（降级未审 link + credit 加 CANONICAL_APPROVED 门 + 反例回归）/ silent evidence 两层化（匹配观察 vs 证据 claim；整句/准整句、非嵌入、非引用元语言才铸强证据；capability 不得凭 lexical token 铸）/ readiness 严格化（∧/∨ 读法显式裁决；补数据或走 `DECISION_REGISTER.md` 版本条目修订 canonical——**后者属用户裁决**）/ 回归（761 既有与 behavioral_baselines 零放松；tests/phase5 相位自建断言可按语义修正但须逐条留痕）。**（历史）Phase 6 grilling 曾可并行、implementation 曾 HOLD 至 P5-R PASS——该 HOLD 现已解除**。以下 Phase 5 段为工程记录（**语义以本段开头的新总结为准**）——**三刀链**：P5-0 `f912948`（`TASK-…4d516e4f.38` / 切片验收 `VAL-…40` 盖 `VR-…42` PASS / 处置 `DEC-…43`）+ P5-1 `0b0d072`（`TASK-…46` / `VAL-…48` 盖 `VR-…50` PASS / 处置 `DEC-…51`）+ p5-2 `d1ccbd2`（`TASK-…55` / `VAL-…57` 盖 `VR-…59` PASS / 处置 `DEC-…60`）；**相位验收** `VAL-…4d516e4f.34` 盖章 **`VR-OPI-a68fd9eb-…9` PASS 零告警**（六组：只读 content.db+registry loader / R0–R4+两视图+production provider 接真链路 / origin separation+candidate exclusion / 自然对话→target resolution→target-specific Performance Evidence→Estimator→LearnerState E2E / 既有 761 零放松+四门绿+0 skipped / §6 未验数字零转述）；收口 `DEC-OPI-a68fd9eb.11`。读数 **1088/0/0**（761 既有零放松 + 327 新：P5-0 138 / P5-1 83 / p5-2 106）、ruff clean、mypy 116、compileall clean、migrations 0010/v10 零新增、冻结面零改动。**Phase 5 交付面**：`content_src/`（14 实体=9 RESOURCE+5 CAPABILITY，§24.1 七列逐字，迁移自 P3 fixture 且原件零改动）+ `curriculum/`（5 capability / 9 CurriculumLink / prerequisites 空集）+ `elc/content/{build,store,queries,types}.py`（确定性构建：本会话两路独立构建 sha256 同值 `2c617a95…bde96`，与 P5-0 记录逐位相等；只读连接写全拒；unknown ID 全 NOT_FOUND）+ `elc/curriculum/{readiness,provider,store}.py`（R0–R4 三键集 + production `ContentBackedTeachingTargetProvider` + §24.11 origin/candidate exclusion）+ `elc/learning/{target_resolution,silent_claim,silent_evidence}.py`（抽取式 resolver + 静默证据链）；**Phase 3 遗留的 fixture 目标供给自此被真链路替换**（E2E 零 `_seed()`/零 fixture）。**机制事实留痕（本轮新增，勿再踩）**：①`validation_submit` 的 verdict 由 METRIC_COMPARATOR 按 **metric 名在 measurements 中命中**决定——未命中即 `INCONCLUSIVE`（不是 FAIL）；`==` 为严格相等（阈值 1 ⇒ 须提交数值 `1`，布尔 `true` 判不等）；`expected` 缺 `repository_basis_refs` 键报 `EVIDENCE_BASIS_REFS_ABSENT`（显式空数组亦静默）；②Write/Edit 按目标文件既有行尾复写（改行尾须「无信号 Write 两步法」）；③大文件禁单次全量 Write 复写（`runtime/controller.py` 含历史 mojibake 字节）。**Phase 4 段（历史记录，收口见 `DEC-…4d516e4f.29`）**：**PHASE 4 COMPLETE**（rev 76；sync 见收口记录）——五刀链 P4-0 `d8866b8`（`VR-…5ba74efc.91`）+P4-1 `2acaaca`（`VR-…109`）+P4-2 `ffcafcf`（`VR-…4d516e4f.12`）+P4-3 `6ef5ca1`（`VR-…21`）+P4-4 本刀（`VR-…4d516e4f.28` PASS，处置与收口 `DEC-OPI-4d516e4f-…29`）；五验收全 PASS（VAL-…70/72/80 直盖；VAL-…74/76 路径集过窄 → 同题扩集 VAL-…4d516e4f.7/.17 代盖并留痕取代）。**Phase 4 交付面**：Relationship durable core（Recorder P4-G1 门/validate 八规则/dedupe/append-first supersede/sensitive gate 四判定序/Persona×User 全隔离）+ **CP4 projection runtime**（确定性 pj- id/幂等 enqueue/五态 CAS/slice+based 双重重校验无盲重放/启动三连 reopen→ensure→drain/F-5 claim 护栏/构造期执行器完备性 fail-fast）+ **两投影类型**（RELATIONSHIP + EPISODE；episode §5.3 十列逐字、幂等版本感知重建）+ **DisclosedUserProfile**（§5.1 列集逐字+显式同意门+高敏仅 persona-specific ∧ RICH）+ **PromptCompiler 八节**（persona→profile→contract→history→relationship→episode→teaching→channel；byte-determinism；跨 persona 零泄漏双向）+ 启动恢复五步 + 全链三模式隔离。**P4-4 收官刀亮点**：压力套件抓到并修复真实缺陷 F-1（分类器读失败原被洗成「空成功」COMMITTED、烧掉确定 id 致该轮投影永久丢失 → 检出该 refusal 即 FAILED_RETRYABLE 可重试；评审两次独立反证）；E2E 四组（含 guard 释放/不重发 assistant 直证/durable 行逐字对账/两次重建编译逐字节相等）。读数 **761/0/0**、mypy 108、migrations 0010/v10、冻结面零改动。**遗留清单七项入册 `DEC-…29`**（Phase 5 接续用）。**外部评审（Phase 4 复核 + Phase 5 GO）已采纳入册 `DEC-…32`**：表述收窄=**Relationship/Episode infrastructure COMPLETE；automatic natural-language relationship extraction NOT production-wired**；三重生产硬门（均不阻塞 Phase 5、均不得因「PHASE 4 COMPLETE」被遗忘）：**门一** model proposal 的语义支撑与权威分离（受信 sensitivity authority + 真实 consent 记录 + 抽取式 evidence span/独立语义校验——现 gate 判定序已墙断「模型推断高敏」，但两字段仍是候选层声明）；**门二** BF-05 source-aware deletion + tombstone（**IP §16 DoD #25，V1 内必须排位次**；现状：src 零 delete/tombstone 实现）；**门三** untrusted durable memory → prompt 安全 framing（JSON framing/长度前缀/显式不可信分隔符 + section-spoofing 测试；触发条件=**真实 external provider 或 production candidate provider 任一启用前**，已由此句取代 DEC-…29 遗留③的「Phase 9」表述）；另 CP4 claim→domain write 的 base CAS 加固随下次触碰 CP4 带入。**Phase 5 三刀明细（历史记录）**：母计划 `PLAN-OPI-4d516e4f-…36`（p5-0 基底 / p5-1 readiness+production provider / p5-2 natural-chat 闭环，MUST_PRECEDE 链）三工作项全毕；相位验收 `VAL-OPI-4d516e4f.34` 已于本轮盖章 PASS。**P5-0 已 COMPLETE**（`TASK-…4d516e4f.38`，切片验收 `VAL-…4d516e4f.40` 盖 `VR-…4d516e4f.42` PASS，处置 `DEC-OPI-4d516e4f-…43`）：`content_src/`（14 实体迁移自 target_fixtures.py，原件与 P3 消费者零改动）+`curriculum/`（5 capability / 9 CurriculumLink）+`content/{build,store}.py`（确定性构建：四路 sha256 同值；只读连接写全拒；unknown ID 全 NOT_FOUND；§24.1 七列逐列）+`tests/phase5/` **138 用例**。评审 **A–H 全 PASS + 两轮处置**：F-1 写通道单一化并**发现机制事实**——Write/Edit 按目标文件既有行尾复写，改行尾须「无信号 Write 两步法」（**修正了此前锁定的「Edit 往返」表述**）；F-2 校验面四缺口补齐（format/format_version/实体 language 交叉校验）；F-3 §11 词表 canonical 抽取钉；F-5 措辞收窄；F-4/F-6 留痕（F-6 归 p5-1）；第 7 次 Bash 直写按先例处置。读数 **899/0/0**、mypy 110、零 migration、冻结面零改动。**p5-1 已 COMPLETE**（`TASK-…4d516e4f.46`，切片验收 `VAL-…4d516e4f.48` 盖 `VR-…4d516e4f.50` PASS，处置 `DEC-OPI-4d516e4f-…51`）：`curriculum/readiness.py`（§8.1/§24.10 五级判定，三键集；R4 要求可测 detection；**语料 14 target 全为 R1、无虚报 R4**）+`curriculum/provider.py`（production `ContentBackedTeachingTargetProvider` 接 content.db 只读面，语义照 P3 port）+`curriculum/store.py`（薄读适配+12 方法签名统一 = DEC-…43 F-6 收口）+origin/candidate exclusion（§24.11 判据）；**新 E2E 全走真供给链，零 `_seed()` 零 fixture**。读数 **982/0/0**、mypy 113。**两处留痕必读**：①**R1 读法是实现侧解释而非 canonical 裁决——严格读法（POS/sense/basic definition/forms 逐字）下本语料 14 个应是 R0**（前提「全实体皆 EXPRESSION」已由 pin 钉住）；②**全语料止步 R1 ⇒ 按 §8.1 默认表无一 target 满足 Planner 默认门槛**（内容侧补课：PedagogicalProfile/ResourceLabel/PackOverlay/explanation/example policy/contrast/TypicalError/detection fixtures）。**p5-2 已 COMPLETE**（`TASK-…4d516e4f.55`，切片验收 `VAL-…4d516e4f.57` 盖 `VR-…4d516e4f.59` PASS，处置 `DEC-OPI-4d516e4f-…60`）：确定性抽取式 resolver + 静默证据链（无 TeachingMoment/Gate/Planner）+ 供给降级不阻聊；**两条 MEDIUM 已修**——F-1 slots 假阳性面（3 个单 token 目标会让 \"I want to see that movie.\" 铸出 1.0 耐久 claim）：改「≥2 组或多 token 才走 slots」+ 把假阳性面写成 **§8.1 R4 detection policy 的第一批声明输入**（实测同构样本 5/23→1/23 且留存例为真阳性）；F-2 Moment 开启窗口错标：resolve 前查 durable 同 target moment，命中即**跳过**（fail-closed）+ 反序反例测试。读数 **1088/0/0**、mypy 116。**Phase 5 首轮收口（原；其语义断言已由 P5-R 修正并重签，见本段开头）**：`VAL-…4d516e4f.34` 曾盖章 `VR-OPI-a68fd9eb.…9`（六组「测试绿」事实仍成立，但 R0–R4 层与 linkage 批准两项语义已被证伪并修正）；内容侧补课清单九项、语义口径四项、metadata 三件、路径集判据与交接全部入册 `DEC-OPI-a68fd9eb.11`——其中「R1 是解释性读法」一条**已被外评否决、亦已被 `DEC-…a68fd9eb.32` 取代，不得再作为合规依据引用**。**🔴 新会话交接要点（Phase 6 开工前必读）**：①**续接首读对象**（全 id 可直接 design_get）：`DEC-OPI-a68fd9eb-…32`（**P5-R 处置 + PHASE 5 语义验收重签 + 取代声明**——**下一相位先读**）＋`DEC-OPI-a68fd9eb-…15`（外评采纳：三 blocker 取证 + P5-R 授权）＋`DEC-OPI-a68fd9eb-…11`（PHASE 5 工程收口：交付面/内容补课清单/metadata 三件；**其 R1 解释性读法与「语料全为 R1」两句已被 `DEC-…32` 取代**）＋`DEC-OPI-4d516e4f-…60`（p5-2 处置）＋`PLAN-OPI-4d516e4f-…36`（Phase 5 母计划，三工作项全毕）；背靠 `DEC-…4d516e4f.32`（三重硬门+禁 _seed()）与 `DEC-…4d516e4f.29`（Phase 4 收口+遗留七项）。②**下一动 = ①p6-3（`PlannerConstraint` / suppression scope，IP §7 明列的 Phase 6 末项；按 `DEC-…5a0be06d.…2` R11 归 user_config、消费方是 Planner/Gate）→ ②Phase 6 相位收口（须以同题扩版 VAL 取代 `VAL-…44` ⑤ 的「增至 0011」句并重签）**：Phase 6 开工第 0 步与 p6-0/p6-1/p6-2 **均已完成**（`DEC-…40` 四条前沿裁决 / `PLAN-…42` 三工作项**全毕** / `VAL-…44` 相位验收；p6-0 交付 `e55a4a6` + 处置修 `da04ec2`，盖章 `VR-…a68fd9eb.…52` PASS，处置 `DEC-…a68fd9eb.…57`；p6-1 交付 `0c19e36`+`ff58611` + 处置修 `491f691`，盖章 `VR-…6259f6fd.…14` PASS，前沿裁决 `DEC-…6259f6fd.…8`、处置 `DEC-…6259f6fd.…16`；p6-2 交付 `c4a6415` + 处置修 `63c8064`，盖章 `VR-…5a0be06d.…8` PASS，前沿裁决 `DEC-…5a0be06d.…2`、处置 `DEC-…5a0be06d.…9`）；四查必答含 (a) canonical 逐字达标面（不得以自建语义+绿测试背书）、(b) **`SessionBudgetView` 归属**（p6-1 R10 登记：若不在 p6-2 范围则显式留痕至 Phase 7/8）、(c) **`source_learning_watermark` 语义** + `learning/queries.py:36 get_freshness` 真端口为 due 决策输入、(d) **`event_type` 词表**（首个按值分支消费者）+ **spacing 阶梯**（`SpacingStage` revisit 条件已就位）+(e) **`ScheduleView` 产出形状**（现名 `due_items`/`overdue_items`）与 `PlannerConstraint`（DATA_MODEL §9）、(f) **BF-02 §5「missing Scheduler → schedule_urgency = 0」禁用项** + R4 的 urgency 数值图属 Phase 7 Planner 特征装配、(g) 读数口径沿用 `DEC-…a68fd9eb.…32`、内容补课未落地前不得假设 readiness ≥ R2、(h) **相位收口时须以同题扩版 VAL 取代 `VAL-…44` ⑤ 的「migrations 增至 0011」句**（p6-1 R18 预告）。③**Phase 5 交付面**：`content_src/`+`curriculum/`（14 validated target 迁移自 P3 fixture，原件零改动；content.db 由 `elc.content.build` 确定性生成、只读读面）+`elc/curriculum/{readiness,provider,store}.py`（R0–R4 判定+production TeachingTargetProvider+薄读适配）+`elc/learning/{target_resolution,silent_claim,silent_evidence}.py`（抽取式 resolver+静默证据链）+`elc/content/{store,queries,types,build}.py`；**Phase 3 遗留的 fixture 目标供给自此被真链路替换**。④**仍未闭的生产硬门**（DEC-…32，各有触发条件，不得因相位推进而遗忘）：候选 provider 的语义支撑与权威分离 / BF-05 删除与墓碑（**IP §16 DoD #25，V1 内须排位次**）/ 不可信记忆进 prompt 的 framing（触发=真实 external provider 或 production candidate provider 任一启用前）。⑤**通道纪律现状**：「Bash 使用清单」机制已捕获多次违规（最近一次 = p6-2 执行者以 Bash 在**仓内** `cp` 一个临时 `.py` 再于同命令删除——已自报、未入提交、无损留，按「同一刀第 1 次＝训诫留痕」处置并在 `DEC-…5a0be06d.…9` 立规：**仓内零写入（含临时文件）；「同一命令内创建并删除」不豁免**，诊断一律用仓外临时目录）；字节零差复写=Edit 往返仅保内容不变、**改行尾须「无信号 Write 两步法」**；大文件禁单次全量 Write 复写（`runtime/controller.py` 含历史 mojibake 字节）；⑥**Phase 6 grilling 必裁前沿（开工前实证，已逐字比对 canonical；①③④ 已由 p6-1 前沿裁决 `DEC-…6259f6fd.…8` 裁定、②已由 p6-0 `DEC-…a68fd9eb.…40` 裁定——下列保留为取证记录）**：`src/elc/scheduler/types.py:19-26` 的 ReviewState 词表（NEW/LEARNING/REVIEW/LAPSED/SUSPENDED）与 canonical `DATA_MODEL.md` §5.2 的 `review_state`（NOT_SCHEDULED/UPCOMING/DUE/OVERDUE）**disjoint**、字段名亦不一（`state` vs `review_state`）；`src/elc/user_config/types.py:193-220` 三件与 `DATA_MODEL.md` §5.1 差距（LearningGoalPortfolio 缺 5 字段 / TeachingPolicyProfile 缺 10 字段且多 1 字段 / SessionFocus 零字段吻合）；`ScheduleItem`/`ReviewEvent` 在 `src/` **零实现**、`docs/STATE_MACHINES.md` 无 review 状态机（grep "review" 0 命中）⇒ 形状与词表须按 canonical **完整替换**（不得在 Phase 0 skeleton 上小修）；派生钉：freshness ≠ review_due（IP §7 原文）、EvidenceModality 分离（canonical）、TEXT_PRODUCTION 不产 voice/audio debt（IP §16 #22 / DECISION_REGISTER:20 派生，非原文）
- **遗留账（换会话时先读本行）**：① **相位收口与门二 = 均已毕**（相位收口：扩版 VAL 取代 → `VR-…b99560d4.…8` PASS 于 `97b0dc7` → 收口 `DEC-…b99560d4.…10`；门二：`PLAN-…14`/`TASK-…19` → 交付 `c0b1733`+`bdca3cb` → 独立评审 ACCEPT-WITH-FINDINGS 无 HIGH → 处置修 `4ce43ed` → **`VR-…b99560d4.…28` PASS** 于 `4ce43ed` → sync SYNCED 117）。**下一步（按序）**：① **补铸 DEC = 已于第十五会话全部铸出**（门二 `DEC-OPI-44647873-…4` / 外评采纳 `…7` / P7-0 `…10` / p7-1 处置 `…12` / p7-2 处置 `…14`；`decision_add` 契约已恢复——**至此 DEC 面与仓库面已对齐**）；② **Phase 7（Planner shadow mode + ActiveLearningFrontier）本体开工第 0 步 = 已完成**：母计划 **`PLAN-OPI-2394139d-206d-4b01-bd61-c9789866c287.8`**（四工作项 `p7-1-planner-kernel-and-trace`（Hard Rules + Policy Utility + activation threshold + deterministic tie-break + NO_TARGET + PlannerEvaluation/Decision/ExecutionStatus/PlannerTrace；**消费 P7-0 的 FeatureAuthority**）/ `p7-2-candidates-scope-priority`（Track A/B + canonicalization + prerequisite resolver + content readiness gate + UserIntentScope + ConversationPriorityView）/ `p7-3-frontier-ledger-coverage`（ActiveLearningFrontier + PlanningLedger + CoverageDebt + overexposure）/ `p7-4-shadow-mode-and-oq023a`（shadow mode：只出 trace 不自动教学 + **复现 OQ-023A 12+ conflict cases**，IP §8 Acceptance）；relations：p7-1/p7-2/p7-3 互 MAY_PARALLEL，三者皆 MUST_PRECEDE p7-4；首位值锚 = p7-1）；**相位验收 `VAL-OPI-2394139d-…10`** 已铸（八组；其 ⑤ 明记 **`OQ-023A` 在本仓与旧仓均无载体**，验收判决面以 `behavioral_baselines/planner/planner_stress_cases_v1_1.json` 的 **43 例**为**可执行替代**）——**相位验收已完成**：八组逐组取证 + 在最终提交 `5395694` 上盖章 **`VR-OPI-44647873-…38` PASS** + 收口 **`DEC-OPI-44647873-…39`** ⇒ **PHASE 7 = COMPLETE；下一动 = Phase 8 开工第 0 步**；**p7-4 已毕**（`f66cc2d` + 处置 `5e33d0d`，盖 `VR-OPI-44647873-…35` PASS；43/43 且与冻结参考实现逐例一致；S04/S42/S20 三分歧已显式建模）（含 43 例复现与 shadow mode 装配；S04/S42/S20 三分歧须显式建模）。**p7-3 已 COMPLETE**（交付 `821b247` + 处置修 `51a9868` + 收口 `b7b29f3`，盖 `VR-OPI-44647873-…23` PASS 于 `b7b29f3`；前沿裁决 `DEC-OPI-44647873-…16`、处置 `DEC-OPI-44647873-…25`；**台账载体 = 纯核 + 显式登记「无表」**，落表刀须连删除面语句一并落）；**p7-2 已 COMPLETE**（交付 `4c6afb7`+`40ffa5c` + 处置修 `53b330e`，盖 `VR-OPI-fc9d8ca7-…20` PASS 于 `53b330e`）；**p7-1 已 COMPLETE**（交付 `f79a36d` + 处置修 `d876ed5`，盖 `VR-OPI-fc9d8ca7-…8` PASS 于 `d876ed5`，sync SYNCED 124）；**补铸 DEC 已于第十五会话全部铸出**（门二 `…4` / 外评采纳 `…7` / P7-0 `…10` / p7-1 处置 `…12` / p7-2 处置 `…14`——`decision_add` 契约已恢复）；四查必答含 (a) canonical 逐字（`DOMAIN_MODEL.md` §10/§10.1/§11/§12/§13、`DATA_MODEL.md` §14/§24.14、`IMPLEMENTATION_PLAN.md` §8、`behavioral_baselines/planner/*` 的 BF-02 §5 `feature_assembly_status`/`snapshot_status`/missing_authorities 与 DEFERRED 路径）、(b) 若新增迁移则同族钉搬迁**须预先写明**（现 head 0014/v14）、(c) 路径集 + 预算带（**须写明计数口径**；collect 命令须加 `-o addopts=""`）、(d) 门二/P7-0 遗留 DEFER（见 ③）与 **P7-0 已冻结的消费面**（Phase 7 必须消费 `FeatureAuthority`/`feature_assembly_status`/`snapshot_status`/`missing_authorities`/`ScheduleCurrency`/两视图/`ReviewEventRole`，不得绕开）；四查必答含 (a) canonical 逐字（`DOMAIN_MODEL.md` §10/§10.1/§11/§12/§13、`DATA_MODEL.md` §14/§24.14、`IMPLEMENTATION_PLAN.md` §8、`behavioral_baselines/planner/*` 的 BF-02 §5 `feature_assembly_status`/`snapshot_status`/missing_authorities 与 DEFERRED 路径）、(b) 若新增迁移则同族钉搬迁**须预先写明**（现 head **0014/v14**；head/stamp 常量集中在 `tests/conftest.py`）、(c) 路径集 + 预算带（**须写明计数口径**）、(d) 门二遗留 DEFER（见 ③）与「Phase 7 Planner 消费面」清单（见 ⑤）。② **门二交付面（引用时必读）**：`migrations/0014_deletion_tombstone.sql` + `src/elc/deletion/{types,plan,store,controller,commands,queries,__init__}.py` + `tests/deletion/`（**120 例**）+ registry `OWNER_DELETION` 条目；**已知有据收窄（三处登记）**：CONVERSATION 不触碰 `user_profile` ⇒ **S41 的 `must_delete:["profile_city"]` 在 CONVERSATION scope 下不可满足**（无 provenance 腿；§17 vs §5.1 列集冻结的结构冲突），`PROFILE_FIELD` 以整集清空**超删** ⇒ **S44 的 `must_keep:["profile_lang"]` 不保留**；`ledger1`/PlanningLedger 在 V1 无表；**V1 无 import/backup 面**（§24「tombstone wins」以谓词 + 守卫兑现）；无 embedding/index 存储、无远端调用、无 UI 删除入口（均明文登记）。③ **DEFER 清单（各有触发条件，勿静默丢）**：p6-0 F-5（版本单调性，触发=出现第二写入方）/ p6-0 F-6（`_teaching_policy_row` 的 `ValueError`→`Err`，下次触碰该文件）/ **p6-2 F-2**（`scheduler/store.py` 的 `_item_from_row` 严格解码抛**裸异常**——总控亲验：同一脏行在 `get_schedule_item` 抛 `AttributeError`、在 `list_schedule_items` 抛 `ValueError`；触发=下次触碰该文件或 Phase 7 DEGRADED 装配）/ **p6-2 F-5**（`migrations/0012` 头部关于 `event_type` 词表的**过期预言**；触发=下次触碰 0012）/ p6-3 INFO-3（`user_config/store.py` 的预探针在 epoch fence 之前；触发=下次触碰该文件）/ **p6-3 INFO-5**（`tests/phase6/test_p6_3_invariants.py` 的 `test_the_gate_is_still_not_wired_to_a_constraint` 以「gate.py 文本不含表名」为钉 = 脆性，接 Gate 的刀须连带更新）；**门二 DEFER（新增三项）**：F3 声明面 vs 执行面只有 CONVERSATION / LEARNING_TARGET / RELATIONSHIP_PAIR 三 scope 有行为腿（余四 scope 仅结构钉）/ `session_focus`「随会话被删」只有声明面覆盖 / 「删除面完整语句映射」包级不变式未写（F5 后 `write_statements` 已能给出全图，随时可写）；**Phase 7 DEFER（新增，各有触发条件，勿静默丢）**：① p7-1 的 `test_opportunity_expiry_outranks_cost_criteria` 候选对**同为 Pareto 可比**（selected「因错误理由」成立、对 F1 变异不敏感）——处置时已登记未改；② **`OQ-023A` 无载体** ⇒ p7-4 以 43 例为可执行替代，其 **S04/S42/S20 三分歧必须显式建模**（S04/S42：重复 `canonical_key`/同 id 被**合并** vs 预期 `error:true`；S20：缺向量 + INCOMPLETE ⇒ 本仓抛 `PlannerInputError` vs 参考给 DEGRADED）；③ **`NOT_SCHEDULED` 的计分取决于取行路径（第十六会话修正——由外评触发，总控探针实测）**：`schedule_urgency_of(NOT_SCHEDULED 行, CURRENT) = 0.0`、`(None, CURRENT) = None`、`(行, STALE) = None`；而**出厂生成器直取行**（`candidates.py:1695` 的 `inputs.schedule.get_schedule_item(...)`，**非**三桶视图），P6-2 又为未排程目标写 `NOT_SCHEDULED` 行 ⇒ **真链路已能计 0.0**；p7-1 judgement 13 的 gap 只对「经 §10 三桶视图取行」的调用方成立（两者今日不同路）。Phase 8 E2E **两条路径都要钉**（直取 ⇒ 0.0、仅视图 ⇒ gap）并回答 judgement 13 的 revisit；④ `PlannerEvaluation` 记录形状 vs `DATA_MODEL §14` 漂移：**p7-3 已落其中一列**（`frontier_candidate_ids`，追加式：实现末位 vs §14 第 3 位，已双向钉住）⇒ **剩余** `factor_trace`/`created_at` 与 `RuntimeDecisionOutcome` 无 record 实现——触发 = 持久化刀（shadow-mode run 或台账落表刀）；⑤ **canonical 自相矛盾（需用户裁决）**：`DOMAIN_MODEL.md` §10.1 行 621 列 **11** 个 benefit（含 `communicative_impact`）而 §11 行 663–674 只有 **10**；实现按 11 读（唯一能消费 BF-02 §12 权重表的读法）并把 §11 短表钉为「文档短表」——**修 canonical 须经 `DECISION_REGISTER.md` 版本条目**；⑥ **评审侧 dmcp 读面故障**（p7-2 评审：`design_get` 对任何对象 `OBJECT_NOT_FOUND`；p7-1 评审：正文被截断到 4000 字符）⇒ 派单 prompt 必须内置任务书关键条款；⑦ **`binding_class` = 源词**使 §4 跨源 merge 永不触发（p7-2 已登记；p7-4 复现 GS22 前须先统一两源读法）；⑧ **p7-3 DEFER（第十五会话新增）**：台账「无表」登记（`PLANNING_LEDGER_STORAGE=NO_TABLE_V1`）⇒ 落表刀**须连删除面语句一并落**（依据 `SECURITY_PRIVACY_DELETION_CONTRACT:87/:747` 把 PlanningLedger user history 列为可删内容）；前沿的**位置是集合身份论证**（物化点在 `kernel._evaluation` 末端，今日无观测差异）⇒ 若出现「在 Policy Utility 之前读前沿」的消费者须重新和解（已登记于 `frontier.py` + Revisit）；`UNLANDED_AUTHORITIES[PLANNING_LEDGER]` 文案按任务书**逐字保留**（字面已过期，含义由 `ledger.py` 顶部登记接管）；两条链例（`test_an_exposure_outside_the_window_is_not_counted` / `test_the_ledger_turns_the_coverage_debt_source_on`）在特定变异下**巧合通过**（非掩蔽——两条腿各由其它测试承重，证据强度已在册）；两个新模块**无独立冷启动钉**（经 `__init__` 间接覆盖，评审实测逐模块 import OK，下次触碰可补两行）；⑨ **p7-4 DEFER（第十五会话新增）**：`_UNDECLARED_ROW_URGENCY` 的 fallback 语义是本刀读法（已被钉为**不可达**；canonical 若裁定「缺声明 ⇒ 无行」需最小改回）/ F2 新臂的 `KeyError` 路径**无专属用例**（仅代码路径覆盖；下次触碰 `stress_suite.py` 的刀可补一例）/ F5 的 `custom=="DEGRADED"` 分支里 `and run.decision is None` 恒真但**保留**（对冻结 runner 原句的忠实复刻）/ shadow 的两个未接线面（`plan` 的派发半、`get_planner_evaluation` 的 store）接上时须**连带更新** `test_the_service_forwards_the_shadow_run_and_refuses_the_unwired_faces` 与 Gate 1 注释**p9-1 DEFER（第十九会话新增，五条，各带触发条件）**：F4 断言冗余（非错）；F5 `delivery_store.py:52` 措辞不精确（同族四处已在，改会牵连 db 层口径 ⇒ 触碰该层时统一）；F6 **registry 同名不同形**（`registry.py:160` 的 `validator_result` 绑 `runtime.types.ValidatorResultRecord`，而 durable 行是 `persona.types.ValidatorResult`；触发 = 触碰该 registry 条目或该形状的刀）；F7 **`validator_result.created_at` NOT NULL 而 `persona/validator.py:56` 默认 None ⇒ None 判 `VALIDATION_FAILED`**（触发 = p9-2/p9-4 的写者必须自带 instant）；F8 `final_rendered` 的 (0,1) CHECK 属已登记读法。**p9-2 DEFER（第十九会话新增，五条，各带触发条件）**：F4 `SMALL_BUFFER_CHUNKS` 为纯声明（结构性质、非可调参数）；F5 SQL-free 钉只覆盖导入集（触发 = 下次触碰 `guarded_stream.py`）；F6 `finalize_streamed_delivery` 未写「调用方持 guard」（触发 = 下次触碰该面）；F8 M12 属等价变异非缺口。**p9-3 DEFER（第十九会话新增）**：INFO-1..6 与两小风险（见「当前状态」p9-3 块）；**教学面接线后仍开着**的登记：`GUARD` 尚不覆盖 §22 恢复面（Phase 10 属）；载体表两条收窄各带 Revisit（canonical 钉死比对方时重开）。**Phase 9 状态（第十九会话）**：p9-0 / p9-1 / p9-2 / p9-3 / **p9-4 均已 COMPLETE**（p9-4 = 交付 `8df2dbd` + 处置 `dabf8b2`，盖切片 `VR-OPI-e26b27a7-…31` PASS 于 `dabf8b2`，**head 0018_delivery_records（v18）**，读数 3433/0/0）；**下一动 = p9-5（末刀，`p9-5-stress-and-e2e`；路径集仅 `tests/`）**：外评**九条承诺清单**逐条可证或被诚实登记 + E2E（普通 chat GUARDED_STREAM 与 teaching BUFFERED_VALIDATED 全链各至少一条真库链路）+ 不回归（全量 + 四口径 + 60 例 gate 重放 + 43 例 planner stress）；**其后 = Phase 9 相位验收**（`VAL-OPI-92170eff-…27` 八组，需在实际最终提交上盖章）**+ 相位收口 + Phase 9 外评**（若有）；背景：Phase 9 六刀与 R1–R8 见 `DEC-OPI-92170eff-…23`，母计划 `PLAN-OPI-92170eff-…25` 第六值切片；**Phase 9 现役读数口径**：3433/0/0（phase3 280 / phase6 666 / phase7 420 / phase8 667 / phase9 401 / deletion 120；非 phase6·7 不含 deletion 2227）；**通道立规首次真执行（留痕）**：p9-2 首交因 2 次同类通道违规**整刀拒收**（仓外快照 `D:\_p92_rejected\ce771e0.patch` + `git reset --hard b0eb0c7`；被拒提交可按 hash 取回）→ 重交以 Write/Edit 纯吸收（提交面 sha256 逐文件相同）⇒ 该立规**可行且成本 = 一次重交刀**；⑩ **Phase 8 开工议程（外评采纳 `DEC-OPI-0f76024b-…1`，第十六会话）**——**p8-0..p8-3 已 COMPLETE（见本行 ⑭/⑯/⑰/⑱）⇒ 议程第 ①（CP2 落库）②（`OQ-024A` 无载体 ⇒ 60 例 gate benchmark 已作可执行替代）③（`SessionBudgetView` 与 `natural_break_available` 权威化）④（台账落表须连删除面语句一并落）四项已兑现；仍挂账的是 ⑤ 三条 non-blocker（跨源 merge 不造景 / `DM §10.1`(11) vs `§11`(10) 的 canonical 矛盾仍需用户裁决 / `OQ-024A`+`OQ-023A` 均无载体这一事实）+ **p8-4/p8-5 两个待开工作项**；下一动 = p8-4**：外评确认 **`PHASE 7 COMPLETE = YES`（不需 reopen）+ `PHASE 8 = GO`**；其五段切片采纳为**提议**（位次由开工第 0 步 `plan_build` 定）：`P8-0 CP2 durable` → `P8-1 SELECT → Gate OPEN` → `P8-2 SessionBudget + ACTIVE_MOMENT continuation` → `P8-3 台账落表 + ReviewEvent 语义` → `P8-4 integration stress` → `P8-5 rollout gate`（P8-0..P8-4 可立即开工）。**真实 automatic-teaching rollout = HOLD**，原因 = 语料 **14/14 readiness=None** ⇒ R2/R3/R4 可用目标 **0**（BF-02 §10 原文：`PROBE R2+ / user-initiated R3+ / automatic general·review R3+ / automatic CURRENT_USER_ERROR R4`；`runtime_generated_ready` 不能使 automatic 路径绕过 R3/R4），**不是 Planner**。开工必带五项：① **CP2「Decision durable」**（`RUNTIME_ARCHITECTURE.md:183`：`DecisionCycle` / `PlannerExecutionStatus` / `PlannerEvaluation` / `PlannerDecision` / `GateDecision?`（仅当 SELECT 到 Gate）**同短事务原子提交**；失败路径 `DEGRADED/FAILED/UNAVAILABLE + DEGRADED_NO_AUTOMATIC_TEACHING`、**不伪造 PlannerDecision**）+ **崩溃窗口**（RA 的 `Crash after CP1/CP2/CP3`）——即「先 decision durable、再 Gate」，不得 `run_shadow → selected → Gate OPEN`；② **`OQ-024A` 无载体**（全仓仅 `IMPLEMENTATION_PLAN.md:439` 与 BF-03 前置行）⇒ 以 `behavioral_baselines/gate/` 的 **60 例 benchmark + 参考实现（199 行）+ 决策表（20 规则 / 17 deny 优先序 / 3 contexts / 6 actions / 2 statuses / 2 decisions / 7 non-authorities）**为可执行替代（与 `OQ-023A`→43 例同形）；③ **`SessionBudgetView`**（P6-1 R10 遗留；BF-03 §17 runtime session state）与 **`natural_break_available` 权威化**（现为 shadow API 手工入参 `run_shadow(..., natural_break_available=)`，canonical 的家在 `DOMAIN_MODEL §13` 的 `ConversationPriorityView.natural_break_available`）；④ **台账落表须连删除面语句一并落**（`NO_TABLE_V1` 的 defer 在首次真产生 `teaching_presented`/`hint_presented`/`reveal_presented`/`user_skip` 时到期；依据 `SECURITY_PRIVACY_DELETION_CONTRACT:87/:747`）；⑤ 三条 non-blocker 继续挂账（跨源 merge **不造景** / `DM §10.1`(11 benefit) vs `§11`(10) 的 canonical 矛盾**仍需用户裁决**，外评建议以「§10.1 supersedes §11 short list」的最小澄清走 `DECISION_REGISTER` 版本条目——**未落地前不得作为合规依据**）；⑪ **dmcp 契约事实（第十六会话实测，换会话先读）**：① `validation_submit` 的 `run_observation` **必填 `repository_basis_ref`**（缺则 INPUT 层拒收；`context_package_ref` 若给必须同时带 `package_id`，不给则整个字段省略）；② `decision_add` / `plan_build` / `validation_define` / `task_build` 本会话**全部正常**（上会话 9 次拒收已成历史）；③ **每次 mutation 成功后 CST 会变**——下一次 mutation 前必须**纯读刷基**（最省做法 = 对任一**小**对象做单对象 `design_get`，其 `basis.observed_canonical_state_id` 即当前 CST；`decision_add` 的 `expected` 用「CST + design_revision + schema_version」三项）；④ `design_get` 的正文**截断到约 4000 字符**（长对象只回前段）⇒ 任务书/裁决的**关键条款必须内联进派单 prompt**（不可假定执行者/评审能读到 dmcp 正文）；⑤ 收口 sync（REBUILD_DERIVED + CHECK）读数：**144 对象 / rev 147 / SYNCED**；⑥ `validation_submit` 的 verdict 由 METRIC_COMPARATOR 按 **metric 名在 `measurements` 中命中**决定，`==` 为严格相等（阈值 1 ⇒ 须提交数值 `1`，布尔 `true` 判不等）；⑦ **`validation_submit` 的 `run_observation` 是 `additionalProperties: false`**：只收 `execution_state`/`repository_basis_ref`（必填）/`environment_basis_ref?`/`dataset_basis_ref?`/`external_basis_ref?`/`measurements`/`evidence_refs`——**不收 `limitations` / `unresolved_questions`**（那是 `review_submit` 的字段；本会话首次提交即因此被 INPUT 层拒收）⇒ 局限声明写进 `DEC` 与回执，不进 attestation；⑧ **执行者侧余额故障**：`executor-pro` 可能报 `Insufficient Balance`（本会话第 5 次执行者侧中断）——任务书已把裁决前置时**同刀改派 `executor-flash` 可完成**，且派单前必查 `git status` 确认无半成品残留。**第十七会话补**：**两个执行者模型都可能各自余额耗尽**（p8-0 首派 pro 失败→flash 成功；p8-3 首派 flash 失败→pro 成功）⇒ 派单失败时**换另一个执行者**通常即可，两边都可承担「任务书已把裁决前置」的实施刀；评审刀同理（本会话三次评审均 pro 成功）。**第十八会话补（两条运维事实）**：① **subagent 长时间无输出会被判 inactive 并中止**（约 600s 无活动；本会话 p9-0 评审首派遭此）⇒ **重派时在 prompt 里写「时间纪律」**（单条 Bash ≤90s、变异实验一次只做 1–2 组、跑子集、优先产出结论）通常即成功（本会话重派成功且自报零越界）；② **两个执行者模型本会话都各自耗尽过**（pro 2 次 / flash 1 次；另有 1 次 pro inactive）⇒ 「换另一个」仍是最有效的对策，但**同一刀的 TASK 书已把裁决前置**是前提（本会话每刀均满足）。⑨ **`task_build`/`validation_define`/`decision_add` 的嵌套对象是 `additionalProperties:false`**：`task_proposal` 里多带一个键（本会话实测：误加 `forbidden_changes_note`）会以 `An unexpected parameter … was provided` 被 INPUT 层拒收；`validation_submit` 的 `run_observation` 同理（不收 `limitations`/`unresolved_questions`，见 ⑦）——**提交前对着 schema 的字段名逐个核**，不要自创字段。⑩ **本会话（第十七）收口读数**：`design_sync` REBUILD+CHECK 四轮均 SYNCED（150 → 153 → 156 对象，rev 153 → 156 → 159）；每次 mutation（`task_build`/`validation_define`/`decision_add`/`validation_submit`）后 **CST 必变**、rev +1（TASK/VAL/DEC）或不变（`validation_submit` 只加 attestation）⇒ 下一 mutation 前必须纯读刷基（本会话曾因此吃到一次 `REVISION_CONFLICT`，错误面直接给出 current CST，照它重构 `expected` 即可）。。**第十九会话补（三条机制事实）**：① **dmcp 正文截断有本地绕法（重要）**——工作区对象全文在 `D:\DMCP-workspaces\<ws-id>\.design\findings\<OBJ-ID>.yaml`（内容其实是 JSON），**总控可用 Read/python 直读自己铸的对象全文**（本刀以此恢复了 Phase 9 母计划 p9-1 工作项与开工 DEC 的 R1–R8 全文；此前多会话受 4000 字符截断之苦 ⇒ **今后派单前先直读原件**）；② `validation_submit` 的 `run_observation` **必带 `evidence_refs`**（缺则 INPUT 层拒收；给一个 ObjectRef 即可；`limitations`/`unresolved_questions` 仍不收）；③ **仓外副本的行尾陷阱**——`git archive` 在 `core.autocrlf=true` 下导出会把行尾翻成 CRLF ⇒ 仓外变异/探针副本必须用 `git -c core.autocrlf=false archive`，否则行尾钉假红（本刀评审实测）。**同会话续补（三条，p9-2 刀内实测）**：④ **拒收程序已验证可行**——首交因 2 次同类仓内通道违规整刀拒收时的完整动作 = 「仓外快照（`git format-patch -1 <hash> --stdout > 仓外`）+ `git reset --hard <父>` + 被拒提交留 hash（仍可 `git cat-file`/`git show` 取回）+ 重交刀以 Write/Edit 只读吸收 + **提交面 sha256/blob 比对自证纯吸收**」；⑤ **越界分级（沿用本仓既有实践）**——**仓内**用 Bash 写文件 = 重级（触发 p8-4 立规的 ≥2 次即拒收）；**仓外**脚本用 Bash 写（heredoc / `python - <<EOF` 打补丁）= 轻级 ⇒ 训诫留痕；两刀内混算时按同类计数；⑥ **「逐字节相同」必须在提交面比对**——`core.autocrlf=true` 下既有 CRLF 工作树文件（如 `runtime/controller.py`）经 Edit 后工作树仍 CRLF、而提交 blob 是 LF ⇒ 工作树 sha256 会**假不等**；正确做法 = `git hash-object`（clean 后）或 `git show <hash>:<path>` 的 blob sha256，或用 `git diff <a> <b>` 为空作判据。**⑦（p9-3 刀内教训，派单口径）**：**「事实/载体映射表」类裁决必须在每一个消费者面上各验一次可达性**——p9-3 的七条 hard invalidation 载体表在普通 reply 上正确，一接到教学面就撞出两条结构性误报（lineage 腿：continuation 的 action cycle ≠ reply turn 的 active cycle ⇒ 恒 mismatch；lock 腿：`PERSONA_RESUME` 交付发生在 `terminalize_moment` 之后 ⇒ 恒 invalid），被既有 phase3/phase8 测试当场抓住并被迫收窄 ⇒ **今后任务书写这类表时，须要求执行者在每个面（本仓 = ordinary / teaching / resume）各跑一次可达性验证，并以「旧读法会红」作为收窄证据。**⑧（p9-4 刀内教训，评审口径）**：**当一刀改写了既有测试时，验证器须在父/子两个提交上跑同一变异**——p9-4 把 `test_p9_1_deletion_legs.py` 的 `TABLES[1:]` 改成 `TABLES[3:]` 时漏掉 `client_render_ack` 的钉（子提交同变异 0-RED、父提交 RED），**这种「强度降级」只有父子对照才能确证**（也是 `DEC-…e26b27a7.…32` 里 F2 的判据）；**⑨（p9-4 口径）预算带必须写明「按 src 行数还是含测试」**（本刀 src 净 +1597/−40 在带内、含测试 +3798/−91 超带）。
⑫ **换会话自检清单（30 秒）**：`git log --oneline -3` + `git status --porcelain`（须干净、tip 与 `origin/master` 同步；本会话收口时 代码交付 tip = **`dabf8b2`**（p9-4 交付+处置；其后有若干 `docs(agents)` 交接提交，最新一条见 `git log`）、`git rev-list --left-right --count HEAD...origin/master` = `0 0`）→ `design_status`（看 rev 与对象数是否 ≥ 上文记录；**本会话收口基线 = 182 对象 / rev 185 / CST `CST-3e2e6cfe-1f37-48d2-822b-76bdfed6725d`**；其 `result_refs` 应能看到最近的 TASK/VAL/DEC —— **`VALIDATION_RUN-VR-*` 在 `effects.attestation_refs` 里、不在 `result_refs`**，照 AGENTS 记的 VR id 直接 `design_get` 即可）→ 读本文件「当前状态」+「遗留账」→ 核对指令 `curl --noproxy '*' -sI https://github.com`（直连通即好用；TLS 失败先读 ⑬）。**换会话交接已加固（第十七会话末）**：⑩/⑪/⑫ 三处的现役入口、基线读数与契约事实均已按 p8-3 收口态刷新。
⑬ **推送路径（本会话再修正——**两条路都要先探**）**：git 与 shell 配有本地代理 `http://127.0.0.1:10801`（`http.proxy`/`https.proxy`/`HTTP_PROXY`/`HTTPS_PROXY`）。**该代理的状态会变**：上一会话它 LISTENING 但失效（TLS 立即断），本会话末它**恢复且很快**（`curl -x http://127.0.0.1:10801 -s -o /dev/null -w '%{http_code} %{time_total}' https://github.com` → **200 / 1.19s**），而**直连已劣化**（`curl --noproxy '*'` → 200 但耗满 20s 超时；旁路推法 `-c http.proxy= -c https.proxy=` 报 `Recv failure: Connection was reset` / `Failed to connect to port 443 after 21177 ms`）。⇒ **判定顺序（取代旧条）**：① 先两条各探一次（上面两条 curl）；② **谁通用谁**（本次 = 直接 `git push`（经代理）成功；旁路推法失败）；③ 两者皆不通才是网络问题；④ 本会话推送读数：`0bf0bdd..24f6f71` 与 `24f6f71..2f5f50e` 均**经代理**发出，**CI 四门 SUCCESS（run `35879933209`）**。**旧条（历史）**：git 与 shell 都配了本地代理…该端口仍在 LISTENING 但已失效…解法 = 一次性旁路 `git -c http.proxy= -c https.proxy= push origin master`；本会话 8 个提交已推（`868b1f3..9df7eb1`）、CI 四门 SUCCESS（run 35859493443）。④ **过期表述**（理由已过期、结论仍诚实，最小 diff 未动）：`src/elc/teaching/gate.py:65/:213`「Phase 6/7 tables that do not exist yet」（表已存在）→ 接 Gate 的刀一并订正（`README` 的 src 行已由本会话 `97b0dc7` 订正）；⑤ **留待更后相位**：`SessionBudgetView` → Phase 8（输入是 BF-03 §17 的 runtime session state）；`event_type` 词表 → Phase 8（首个按值分支消费者是复习记录流）；`PlannerConstraint` 的 `THIS_SESSION` 会话绑定 + **半腿行**（target 腿一有一无）语义 + `ScheduleView` 桶序「串序 vs 时刻序」+ `review_urgency` 的消费方式 → Phase 7 Planner；`INTERVAL_DAYS`/`GRACE_DAYS` 校准 → Phase 11（若改须同步换 `SCHEDULER_MODEL_VERSION`）；⑥ **内容侧补课**（P5 遗留九项）未落地前**不得假设任何 target readiness ≥ R2**（语料 14/14 报 `level=None`）；⑦ **三扇生产硬门**（`DEC-…4d516e4f.…32`）触发条件不变：门一候选 provider 语义支撑与权威分离 / 门二见②/ 门三不可信记忆进 prompt 的 framing（触发=真实 external provider 或 production candidate provider 任一启用前）。
⑭ **p8-0（CP2 durability）= COMPLETE（第十六会话）**：交付 `42d1b16` + 处置 `0479cbf`；盖 `VR-OPI-64c88704-…13` PASS 于 `0479cbf`；处置 `DEC-OPI-64c88704-…11`（评审 findings 逐条裁定 + 两处总控起草缺陷自认 + 执行者侧余额故障留痕）；**下一动 = p8-1（Gate 自动面）**。**p8-0 DEFER（各有触发条件，勿静默丢）**：① **F9**——给 `planner_store.record_planner_cycle` 传裸 `str` 状态会抛裸 `AttributeError`（事务整体回滚 + fail-closed），触发 = 下次触碰 `src/elc/platform/db/planner_store.py` 的刀（同族 p6-2 F-2）；② **`src/elc/teaching/gate.py:60/:66`** 的「Phase 6/7 tables that do not exist yet」（0013/0015 均已落）⇒ 归 **p8-1**（接 Gate 的刀）一并订正；③ **评审未覆盖面**：CI（Ubuntu 四门）未在会话内亲跑、**并发双写者**未测（两连接同写同 cycle 理论由 `BEGIN IMMEDIATE` 串行化）⇒ 归 **p8-4 集成压力 / p8-5 rollout 门槛**补证；④ **`PlannerService.get_planner_evaluation` 仍不接线**（理由已改真：durable 行是 **id 形状**、该面声明的是**对象形状**）⇒ 触发 = 决定该面 envelope 的刀；⑤ p8-0 两条新 registry 条目绑 `planner_version`（§14 另拼的 `policy_profile_version` **不在** VERSION_FIELDS 故不绑）——canonical 版本条目若改动，条目须随动。
⑮ **Phase 8 余项位次（母计划 `PLAN-OPI-0f76024b-…5`）**：**p8-1/p8-2/p8-3 均已 COMPLETE（见 ⑯/⑰/⑱）⇒ 下一动 = p8-4** `automatic-integration-stress`（**同 turn 集成**：普通 turn 的 planner→gate→moment 与 `EphemeralTeachingDirective`/prompt/delivery 的连接 + **exposure 生产者**（`teaching_presented` 的写手）+ 崩溃窗口的 Moment 半 + `get_action_for_turn` 读回 action_id + 装配层手工 bool 的收口（F11）+ `ConversationPriorityView` 的生产者 + 台账事件 refs/会话 provenance 腿）→ p8-5 `rollout-gate`（RISK_SPIKE；**真实 rollout 仍 HOLD**：语料 14/14 `readiness=None` ⇒ R2/R3/R4 可用目标 0，BF-02 §10 门槛）。
⑱ **p8-3（台账落表 + 删除面 + ReviewEvent 语义）= COMPLETE（第十七会话）**：交付 `ff769e9` + 补 `280adc1` + 处置 `ddbfd90`；盖 `VR-OPI-6d40862d-…15` PASS 于 `ddbfd90`；处置 `DEC-OPI-6d40862d-…13`（评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM；**14/14 变异全 RED**；F1/F2 修 + INFO-1/3 修 + INFO-2 确认 + INFO-4/5/6 登记；四项总控起草缺陷自认；四项执行者异议全判成立）；**本刀新增迁移 0016（head v16）**。**p8-3 DEFER（各有触发条件，勿静默丢）**：① **INFO-4**——`ledger_store.py` 无独立冷启动钉（触发=触碰该文件）；② **INFO-6**——0016 头部的 rollup 不物化理由需收窄为**范围论证**（触发=触碰 0016，动它须同步其摘要钉）；③ **exposure/accrual 生产者 + 事件 refs**（呈现的 Moment/会话 provenance）触发=**p8-4**（届时 ref 列与 CONVERSATION 腿须**同刀**落，否则 FK 会让会话删除死在外部键上）；④ **rollup 物化**触发=首个从 durable 台账读 rollup/最近家族的消费者（须连删除面语句一并落；今日 `read_ledger()` 恒答默认值，且带非默认 rollup 的 `PlanningLedger` 无法持久化）；⑤ **`event_type` 四词待校准**触发=canonical 补词表（整表搬家）或首个按值分支的消费者（须先引 `spacing.py` 的冻结读法）；⑥ **registry 的 `planning_ledger` schema 取视图类**触发=下次触碰 registry（若认为应改为行级记录 `TargetLedgerRow`）；⑦ **`NO_TABLE_V1` 的历史时态引用**（`planner/__init__.py:38`/`candidates.py:140`/phase7 两测试）**经总控裁定不需清扫**（读作历史且真实）——若某刀认为需改，改为历史叙述而非现役断言；⑧ **`INFO-5`**——`ff769e9`/`280adc1` 的 commit message 措辞不准（「导出集不扩」实增两名），红线禁 amend ⇒ 只此留痕。
⑲ **p8-4（同 turn 自动教学集成 + exposure 生产者 + 0017 provenance 腿）= COMPLETE（第十八会话）**：交付 `08dda87`（含处置刀产物）；盖 `VR-OPI-92170eff-…7` PASS 于 `08dda87`；处置 `DEC-OPI-92170eff-…8`；**本刀新增迁移 0017（head v17）**；**下一动 = p8-5（`rollout-gate`，RISK_SPIKE）**。**p8-4 DEFER（各有触发条件，勿静默丢）**：① **F3**——`review_event.teaching_moment_id`（0012，nullable FK→teaching_moment）**无 moment 腿** ⇒ CONVERSATION 删除 `Err(FOREIGN KEY)`（今日无生产写者，`record_review_event` 零调用方）⇒ 触发 = review-outcome 路径落笔时**先裁定语义**（随 moment 移除 vs 清引用）再按同形状加腿；② **CP3 崩溃的曝光欠记**——action 已 TERMINAL 时 `turn_record` 停在 `DELIVERING`，普通 loop 直接 CONFLICT ⇒ 交付腿进不去（「呈现了但无事件」）⇒ 触发 = 普通 loop 的 `DELIVERING` 调停刀；③ **`_deliver_teaching_action` 的 terminal 重放分支今日不可达**（同②），且 exposure 的重试写入（新 instant）会是 `CONFLICT` 而非 no-op（F2 措辞已收窄）⇒ 触发 = 需要「CP3 后自动和解」的刀；④ **CP2 已提交后读回失败的 torn state**（Moment 停 `OPENING`、锁在握、无交付；`Err` 臂下普通回复可能落在 `-automatic-open` action 上）⇒ 触发 = 下次触碰该路径的刀（是否给 OPENING 残留一个调停/清理面）；⑤ **F5**——`p8_4_honesty` 两钉是子串扫描（别名/`**kwargs` 可逃逸；评审以 alias-aware AST 扫描证实事实成立）⇒ 触发 = 触碰该缝的刀（升 AST 钉）；⑥ **F6**——三覆盖缺口：`request_teaching`+注入 wiring 无组合用例 / RETRY「不写」只有映射单测 / `record_event` 硬编码 `LedgerKeyType.TARGET`（CAPABILITY focus、FAMILY 面）⇒ 下一刀可各补一例；⑦ **F7**——`record_leg_failure` 的降级记录被 P8-0 重放规则**永久化**（第二次组装成功只拿到 CONFLICT 并被吞；反向不对称：CP2 之后失败不落记录，已登记）；⑧ **F4**——`_replay_terminal` 的 `TurnCompletion.ledger_event=None` = 「本次调用没写」**非**「没发生过曝光」；⑨ **并发双写者未测**（评审未覆盖面）⇒ 归 p8-5；⑩ **CI（Ubuntu 四门）本会话未亲跑** ⇒ 归推送后。
⑳ **本会话（第十八）机制事实**：① **CR 计数禁用 Bash `grep -c $'\r'`**（Git-Bash 下 `$'\r'` 被当空 pattern ⇒ 报**行数**）——一律用 python 字节计数或 `git ls-files --eol`；② `mypy` 的 CI 口径是 `mypy src/elc`（`mypy .` 无配置会报数百 error）；③ 评审侧/执行者侧 `touch tmp` 也会改写 mtime（本次未发生，仅备记）。
㉑ **p8-5（rollout-gate，RISK_SPIKE）= COMPLETE ⇒ PHASE 8 = COMPLETE（本会话）**：交付 `9506c56`；切片章 `VR-OPI-92170eff-…14` + **相位章 `VR-OPI-92170eff-…18`** PASS 于 `9506c56`；处置 `DEC-OPI-92170eff-…16` + 收口 `DEC-OPI-92170eff-…19`；**零迁移（head 0017/v17）**。**p8-5 DEFER（各有触发条件，勿静默丢）**：① **四缺读面无生产实现**（moments / gate decisions / planner decisions / claim count——端到端只由 `tests/phase8/p8_5_world.py` 的四条只读探针满足）⇒ 触发 = 首个生产可用报告落这四面；② **滚出（rollback）仅具名手段**（`BACKOFF_MEANS` 无执行面）⇒ 触发 = BF-02 §21 校准流程落地；③ **overexposure 中立带 = `("NONE",)`** 是本刀声明读法（LOW=1 次亦计入）⇒ 触发 = canonical 给危险线；④ **`test_no_shipped_module_declares_a_stage_of_its_own` 的形状**（禁 src 内 `RolloutStage.<成员>`）⇒ 触发 = 生产装配从配置读阶段的刀（它正是该刀要面对的问题）；⑤ **LOW-1 残余**（`**kwargs` splat / `getattr`/`globals()` / 函数体内别名仍不可见，已入 pin docstring）⇒ 触发 = 下次触碰该 pin；⑥ **skip/reject 单值表述**（台账 `recent_skips` 与 moment abort 由 canonical 和解后单值才可再表述）⇒ 触发 = 该 spec 的 revisit；⑦ **INFO-1 同族措辞**（`automatic_teaching.py:217-220` 把 `automatic_teaching_enabled_of` 与进程级声明并列）未扫 ⇒ 触发 = 下次触碰该文件；⑧ **阶段关闭 vs 策略 OFF 共用 deny 词**（rollout 观测不可区分）⇒ 触发 = 需诊断区分时（须新铸 reason 词，Gate 冻结面）；⑨ **跨进程并发未测**（并发证据是同进程两连接；WAL 多进程争用未验）⇒ 触发 = 多进程 profile 或 rollout 前的可靠性刀；⑩ **阶段无端到端配置证据**（今日只有注入参数）⇒ 触发 = 开闸前的生产配置面。**Phase 8 收口后（本行）**：六工作项 DEFER 共约 42 条在册（p8-0 5 / p8-1 6 / p8-2 8 / p8-3 8 / p8-4 10 / p8-5 10）；两条 canonical 级挂账不变（`DM §10.1`(11) vs `§11`(10) 需用户裁决；跨源 merge 不造景）；内容侧补课九项 = rollout 开闸前置。
㉓ **p9-0（carryover + trust boundary）= COMPLETE（本会话）**：交付 `77fa549`（含处置）；盖切片 `VR-OPI-92170eff-…33` PASS 于 `77fa549`；处置 `DEC-OPI-92170eff-…34`；**零迁移（head 0017/v17）**。**p9-0 DEFER（各有触发条件，勿静默丢）**：① **anchor 裁置请求**（`review_record.py` + `spacing.py` docstring 双落）——现读 **ANCHOR_ONLY** ⇒ **失败会推迟下次复习窗口**（反复失败不缩短回路）；替代读法 ADVANCE_ONLY ⇒ 不推迟；**触发 = 第一个真实 ReviewEvent writer 上线前；改它须走 `docs/DECISION_REGISTER.md` 版本条目**；② `decode_factor_trace` 的**旧格式臂仍有同族洗白**（`tuple(str(...))`，仅脏行/手改旧行可触发）⇒ 触发 = 下次触碰 `trace_document.py`；③ LOW-3 姊妹测试（`test_the_referral_does_not_change_anything_it_reports`）docstring 自称已钉而函数体未钉 ⇒ 触发 = 下次触碰该测试文件；④ **pre-cut 行不可重放**（有据收窄：judgement 9 Revisit + `planner_store._replay` docstring 双声明；前移路径 = re-encode 迁移）⇒ 迁移落地前对 pre-cut 行的 CP2 重入恒 CONFLICT；⑤ `0015` 头部现役句为历史（留冻结文件；其 Revisit 已由 p9-0 触发并在 judgement 9 点名）⇒ 若今后允许改 0015，须连 `PRE_0015_DIGESTS` 类摘要钉一并处理；⑥ 门三的**行定向契约**口径（值可含标记文本、不构成边界；按行扫描的读者才是本框面向的）⇒ INFO-1 已收窄散文，按子串任意位置扫描 marker 的读者不是本框面向的读者；⑦ `terminalizing_action` 无类型校验（与本仓带注解布尔事实的既有约定同形）⇒ 触发 = 全仓布尔校验收紧的刀。**执行者 8 项异议全成立 + 总控起草缺陷 2 项（路径集枚举漏 `tests/phase9/__init__.py`；条款称「1 处钉」实为 4 处四族钉）**见 `DEC-OPI-92170eff-…34`；**通道**：首刀（1 次仓外探针 Bash 改写自报）与处置刀（15 次写入全 Write/Edit；变异脚本全落仓外）均无仓内违规，评审首派 600s inactive 被中止后重派成功（自报零越界）。
㉒ **Phase 8 外评采纳后的 P9-0 carryover 与 Phase 9 议程（`DEC-OPI-92170eff-…21`，本会话）**：**P9-0 必须项（两条 MEDIUM，均逐条取证成立）**：① **durable `factor_trace` 完整化**——现状 `planner/records.py:32` 把 `factor_trace` → `PlannerEvaluation.reason_trace`（散文行），逐因子证据（`kernel.py:940` 的 `CandidateTrace.benefit/cost/gaps`）只存内存 ⇒ crash 后无法回答「learning_need 多少 / 来自哪个 authority / 哪个 factor 有 gap」；修法 = 把 structured trace 写入**现有 TEXT 列**（versioned deterministic JSON，**无需新迁移**），并**同刀改写 `records.py` 登记 9 与 §14 列的「内容语义」**（该登记目前只覆盖物理形状——这是外评命中的、仓内未登记的一面）。② **`ContinuationFacts.terminalizing_action`**——`gate.py:833`（USER_REQUESTED_CONTINUE 路）无此字段而 `gate.py:1364–1368`（AUTO_CONTINUE 路）有 ⇒ 两条 path 不对称、冻结参考 §8 的 terminalizing 豁免在该路不可达；修法 = 按 `AutoContinuationFacts` 同形补字段（默认 `False`）+ `terminalizing = action in {REVEAL, TERMINAL_FEEDBACK} or facts.terminalizing_action` + 补例（本项 = p8-1 DEFER F11；**外评与我方独立发现同一件**，双向印证）。**P9-0 其余（外评建议位）**：**门三**（untrusted durable memory → prompt 的 section-spoofing framing；触发条件已由 `DEC-OPI-4d516e4f-…32` 定义，外评判「现在正式进入 Phase 9」）、**ReviewEvent writer contract**（含 **anchor 语义重裁**：现行 `spacing.py` 的 `ReviewEventRole` 表是 P7-0 冻结的 **ANCHOR_ONLY**——`engaged=False` 仍推进 anchor ⇒ 失败会推迟下次复习窗口；必须**在第一个真实 ReviewEvent writer 上线前**重裁）、Planner pure evaluator API 收敛（可顺手）。**rollout 前置（与 p8-2 DEFER ⑤ 合并）**：SessionBudget 的 `remaining=None` 今日行为 = 「还有预算」（`automatic_controls.py:119` 的 `remaining is not None and remaining <= 0`）⇒ 必须区分 `UNBOUNDED / KNOWN_REMAINING / UNKNOWN`，且 **UNKNOWN 不得 fail-open**。**Phase 9 交付归属**：CP3 曝光欠记（p8-4 F8）与 CP2 OPENING 残留（p8-4 DEFER ④）⇒ P9-4/P9-5。**P9-5 的承诺清单**（外评原文）：partial ≠ full exposure / no ACK ⇒ `SERVER_SENT_UNCONFIRMED` / late ACK 只升 certainty 不提 mastery / barge-in 旧 provider 尾不入 transcript / cancelled·superseded late callback 零 canonical side effect / `action TERMINAL + turn DELIVERING` 可保守和解 / 实际送出 ⇒ exactly one ledger exposure / 未送出 ⇒ no exposure / duplicate reconciliation ⇒ 零重复 canonical side effect。**外评分维度评级（逐条确认）**：Durable Planner CP2 / Automatic Gate / SessionBudget / PlanningLedger durability / Same-turn automatic integration / Gate benchmark / Planner regression / Rollout honesty = **PASS**（SessionBudget 一条收窄为「PASS + rollout 前置」）；Factor-level durable audit 与 User continuation edge = **MEDIUM debt（已归 P9-0）**；Delivery·exposure crash window = **Phase 9**；Real rollout telemetry 与 Real R3/R4 content = **not ready**（内容侧补课 = 开闸前置）。
⑰ **p8-2（会话预算视图 + 权威化）= COMPLETE（第十七会话）**：交付 `14041d0` + 处置 `2f0b8e3`；盖 `VR-OPI-6d40862d-…7` PASS 于 `2f0b8e3`；处置 `DEC-OPI-6d40862d-…5`（评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM；F1 修 + F2–F6/F12 修 + F7–F11 登记；两裁定 = `sh2` 印章 bump / `gate.py` 散文授权；三项总控起草缺陷自认；评审侧 1 次通道违规训诫留痕）；**三刀零迁移**。**p8-2 DEFER（各有触发条件，勿静默丢）**：① **F7**——`planner/frontier.py` 的**模块散文**「runtime session state which no cut produces」已过期（条目本身有意冻结）⇒ 触发 = 触碰 `frontier.py` 的刀，届时**两处引文一并交叉钉住**（后者评审指出无交叉校验钉）；② **F9**——`teaching/store.py` 的会话读面 `WHERE conversation_id = ?` **不承重**（派生层再收窄；但 SQL 侧单独失效无声）⇒ 触发 = 触碰该文件的刀；③ **F10**——`shadow.py` 的窄 Protocol 是 `cast` 非校验（非 bool 真值会原样进 context）⇒ 触发 = 触碰 `shadow.py` 的刀；④ **F11**——`assemble_feature_authority` 仍收手工 bool（合同保留）⇒ 触发 = 装配层接线刀（p8-4）；⑤ **两条窗常数**（1800s/3600s）+ `fatigue_signal` 恒 `None` + 策略预算词表缺席（两条 remaining 恒 `None`）⇒ 触发 = BF-03 v1.2 / IP §13 校准 / p8-5 门槛刀；⑥ **`ConversationPriorityView` 生产者仍缺席**（RA §4 步 3 的对话腿）⇒ 当前自动路径在该事实上一律 fail-open ⇒ 触发 = 对话腿落地刀；⑦ **`sh2` 之后任何读数移动须同法 bump + 同步四处**（含 `tests/phase7/test_p7_4_stress_suite.py` 的**散文**姐妹列——它非机器校验、下次会静默过期）；⑧ **同 turn 集成未接线**（`runtime/controller.py` 不 import `automatic_teaching`，有正钉）⇒ 触发 = p8-4。
⑯ **p8-1（Gate 自动面）= COMPLETE（第十七会话）**：交付 `b43d4bc` + 处置 `325e7a6`；盖 `VR-OPI-64c88704-…23` PASS 于 `325e7a6`；处置 `DEC-OPI-64c88704-…21`（评审 ACCEPT-WITH-FINDINGS 无 HIGH；F1/F2 修 + F3–F7/F9/F10 修 + F8/F11/F12 登记；两处通道违规训诫留痕；四项总控起草缺陷自认）；**60 例 benchmark 仓内全量重放 60/60**；**两刀均零迁移**（head 仍 0015/v15）。**p8-1 DEFER（各有触发条件，勿静默丢）**：① **F11**——`ContinuationFacts` 无 `terminalizing_action` ⇒ 冻结参考 §8 的 terminalizing 豁免在 **USER_REQUESTED_CONTINUE 路不可达**（参考 `(hard_turn=True, HINT, terminalizing=True)`=ALLOW vs 仓内 DENY；60 例的 11 个该 context 例该字段全 False ⇒ 重放不覆盖）⇒ 触发 = 下次触碰该 profile 的刀（按 `AutoContinuationFacts` 同形补字段 + 补例）；② **F12**——`src/elc/teaching/store.py` 的「同 cycle 不同控制事实」路径把裸 `UNIQUE constraint failed…` 作 `Err(CONFLICT)` message（与 p6-1 F-2 同族）⇒ 触发 = 触碰 `teaching/store.py` 的刀（单元内已不可达并有断言）；③ **重放 `action_id=None`**（本单元声明面内无动作读面）⇒ 触发 = p8-4（用 `get_action_for_turn` 读）；④ **重放期 DEGRADED 不可重试**（F1 规则的字面延伸：同一 cycle 的 DEGRADED 也永久重放）⇒ 触发 = p8-4/p8-5 若需「DEGRADED 可重试」须新铸裁决；⑤ **同 turn 集成未接线**（`runtime/controller.py` 不 import `automatic_teaching`，有正钉）⇒ 触发 = p8-4（连带更新该钉）；⑥ **`decide_auto_continuation`/`AutoContinuationFacts`/`moment_consent_class_of` 无生产调用方**（换会话勿误读为已接线）。
- **对象索引**（全 id 可直接 design_get；OPI 前缀：Phase 3 早期 `2babb21e-bc72-47a2-9382-e773c92211d2`、P3-1B 起及 Phase 4 全在 `5ba74efc-9f26-483b-a7de-c833834275a4`、Phase 5 全在 `4d516e4f-9575-4cbf-8d08-6bf19a95bd25`、**Phase 5 收口系列 `a68fd9eb-7bf5-4ed6-a74b-68a38707177b`**、**Phase 6 p6-1 全在 `6259f6fd-c446-4e34-886c-946c3343af0c`**、**Phase 6 p6-2/p6-3 全在 `5a0be06d-5af8-4124-9550-88fda7b22587`**、**Phase 6 收口系列 `b99560d4-12a8-4ff5-8450-5049d9d97106`**、**Phase 8 开工系列 `0f76024b-4634-422f-ab0a-7f05bb786df0`**、**p8-0 系列 `64c88704-f160-4cae-820b-1c9380a203bd`**、**本会话全系列 `92170eff-604f-4c19-911a-d08ae3698d4e`（p8-4 / p8-5 / Phase 8 外评采纳 / Phase 9 开工 / p9-0）**；旧系列 `eaaa5a1d-7ac7-4746-bcdf-c02bc9147492`）：
  - **Phase 7 母计划 + 相位验收**：`PLAN-OPI-2394139d-…8`（四工作项 p7-1 kernel+trace / p7-2 candidates+scope+priority / p7-3 frontier+ledger+coverage / p7-4 shadow-mode+OQ-023A）/ `VAL-OPI-2394139d-…10`（八组；⑤ 记 OQ-023A 无载体 ⇒ 43 例为可执行替代）
  - **p7-2（候选供给侧）三件**：`TASK-OPI-fc9d8ca7-…13` / `VAL-OPI-fc9d8ca7-…11` / 盖 **`VR-OPI-fc9d8ca7-…20` PASS** 于 `53b330e`（交付 `4c6afb7`+`40ffa5c` + 处置修 `53b330e`；F1 MEDIUM 的前置边契约缺陷已修并自证；执行者 1 次通道违规＝训诫留痕；**评审侧 dmcp 全 `OBJECT_NOT_FOUND`** 已留痕）
  - **p7-3（frontier + ledger + coverage）三件**：`TASK-OPI-44647873-…20` / `VAL-OPI-44647873-…18` / 盖 **`VR-OPI-44647873-…23` PASS** 于 `b7b29f3`（交付 `821b247` + 处置修 `51a9868` + 收口 `b7b29f3`；评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM、3 LOW + 4 INFO、13 组变异全断言式 RED 无 0-RED；执行者 1 次通道违规＝训诫留痕、评审判 UNCERTAIN）
  - **p7-4（shadow mode + 冻结 43 例复现）三件**：`TASK-OPI-44647873-…33` / `VAL-OPI-44647873-…31` / 盖 **`VR-OPI-44647873-…35` PASS** 于 `5e33d0d`（交付 `f66cc2d`（首轮余额中断 → **第四次「执行者遗留先例」**：仓外快照 + 定位未还原变异 MUTATION-3 → 续接刀吸收）+ 处置修 `5e33d0d`；评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM、4 LOW + 4 INFO、**16 组变异 + 3 组篡改用例**、并由评审把**冻结参考实现逐例交叉验证 43/43 一致**）
  - **Phase 9 开工三件（第十八会话，新 OPI 系列 `92170eff-604f-4c19-911a-d08ae3698d4e`）**：`DEC-OPI-92170eff-…23`（开工 + 前沿裁决 R1–R8：迁移 0018 五表 / P9-0 carryover / GUARDED_STREAM 本地形态（测试注入块序列，不伪造网络流）/ ExposureEstimate 与台账单向 / PreDeliveryGuard 七条 hard invalidation / rollout 仍 HOLD / 挂账不变 / 不变量）/ `PLAN-OPI-92170eff-…25`（**六工作项** p9-0-carryover-trust-boundary / p9-1-canonical-delivery-records / p9-2-guarded-stream-normal-persona / p9-3-barge-in-cancel-supersede / p9-4-teaching-exposure-reconciliation / p9-5-stress-and-e2e；**八关系**：p9-1 → {p9-2,p9-3,p9-4} → p9-5、p9-2 MAY_PARALLEL p9-3、p9-0 MAY_PARALLEL p9-1；首值锚 = p9-0）/ `VAL-OPI-92170eff-…27`（**相位验收八组**：P9-0 carryover / Canonical Delivery Records（§22+§21.1 五表逐列）/ GUARDED_STREAM / barge-in·late callback / exposure 和解 / PreDeliveryGuard / 不回归与门 / 诚实性+rollout HOLD）
  - **p9-4（teaching-exposure-reconciliation）三件（第十九会话，同一 `e26b27a7-…` 系列；Phase 9 第五刀）**：`TASK-OPI-e26b27a7-…27`（任务书 A–E 五面：`exposure_reconciliation.py` 派生 + ACK 只升 ladder / 两交付面初估写入 / `accept_render_ack` / 两 crash 窗口和解；R1–R9 预裁 + 派生表 + 窗口映射）/ `VAL-OPI-e26b27a7-…29`（切片验收七组）/ 处置 `DEC-OPI-e26b27a7-…32`（评审 ACCEPT-WITH-FINDINGS 无 HIGH、**1 MEDIUM + 3 LOW + 3 INFO**、**9 RED / 2 NOT-RED**；**F2 MEDIUM 修** = 本刀改写既有测试造成的 append-only 钉丢失（以**父/子对照变异**确证：父 RED / 子 0-RED ⇒ 恢复钉 + 变异自证）+ F1（派生表 `FAILED` 拆两行 + barge-in 子例点名）· F4（新字段第五因 + 通道归属按实测）修 + F3 补强制世界一例（变异 RED）+ F5–F7 登记；执行者 7 异议（含「estimate 补救腿是应有补全」与「`exposure_estimate_id` 不落本刀」）逐条判定 + 总控起草缺陷自认 2 项；**执行者零通道违规**）；交付 `8df2dbd` + 处置修 `dabf8b2`；盖 **`VR-OPI-e26b27a7-…31` PASS** 于 `dabf8b2`；**零迁移（head 0018_delivery_records / v18）**
  - **p9-3（barge-in-cancel-supersede）三件（第十九会话，同一 `e26b27a7-…` 系列；Phase 9 第四刀）**：`TASK-OPI-e26b27a7-…20`（任务书 A–E 五面：`pre_delivery_guard.py` 七条件纯决策 / Guard 外 durable 入口 + Guard 内和解 / `InterruptAwareTransport` 包装缝 + 终态词映射 / 待决中断读面 + guard 行落盘 / tests/phase9；R1–R10 预裁 + **七条 hard invalidation 的 V1 载体表**）/ `VAL-OPI-e26b27a7-…22`（切片验收七组）/ 处置 `DEC-OPI-e26b27a7-…25`（评审 ACCEPT-WITH-FINDINGS 无 HIGH、**1 MEDIUM + 3 LOW + 6 INFO**、13 组变异 **11 RED / 1 等价 / 1 转 LOW**；**MEDIUM-1 当刀修**=教学/BUFFERED 面接 guard 与 reconcile（补 `test_p9_3_teaching_guard.py` 830 行，变异 4 组全 RED）+ LOW-1/2/3 修 + 两处诚实性同步；**两条 carrier 读法收窄经裁定采纳**（lineage 腿 ⇒ 比对其 moment 的 cycle（`STATE_MACHINES.md:391`）；lock 腿对 `PERSONA_RESUME` 读 False）；执行者 8 异议全成立 + 总控起草缺陷自认 2 项（carrier 表未验证教学两面；`request_interrupt` 指针 vs 活体面未点明）；**训诫两次**（交付刀 1 次仓内 `sed -i` <2 次不拒收；评审刀 2 次仓外 Bash））；交付 `90bb577` + 处置修 `6596da0`；盖 **`VR-OPI-e26b27a7-…24` PASS** 于 `6596da0`；**零迁移（head 0018_delivery_records / v18）**
  - **p9-2（guarded-stream-normal-persona）三件（第十九会话，同一 `e26b27a7-…` 系列；Phase 9 第三刀）**：`TASK-OPI-e26b27a7-…12`（任务书 A–E 五面：GUARDED_STREAM 本地形态（`StreamStep` 三形 / `StreamTransport` 两面 / 声明常数 / `run_guarded_stream` 顺序 take→guard→emit→record）/ controller 普通 persona 段改走 `finalize_streamed_delivery` + 两注入点 / 记录面与终态转移（`SENDING`→`SENT_PARTIAL`×n→终态；emit→record）/ tests/phase9 三文件；R1–R10 预裁）/ `VAL-OPI-e26b27a7-…14`（切片验收七组）/ 处置 `DEC-OPI-e26b27a7-…18`（**含首交整刀拒收全程留痕**：2 次同类通道违规 → 仓外快照 + `git reset --hard b0eb0c7` → 重交 `ab8f683`（as-found 纯吸收，提交面逐字节同）→ 评审 ACCEPT-WITH-FINDINGS 无 HIGH（**3 MEDIUM + 1 LOW + 4 INFO**；**16 组变异 13 RED / 1 等价 / 2 真缺口**）→ 处置修 F1（第四形状补 `refuse_at=2` 例）· F2（raise handler 补 `emit` 抛例 + reason 句收窄，原句「nothing … can be proven」**不真**）· F3（`GenerationContract.response_mode` 双载体漂移：`_contract` 改为从唯一模式表派生 + 两臂钉）；F4–F8 登记；执行者 7 异议**全成立**（含「任务书漏点 `delivery_records` 注入点」= 总控起草缺口）+ 总控起草缺陷自认 2 项）；交付 `ab8f683`（重交；被拒首交 `ce771e0`）+ 处置修 `cb254c0`；盖 **`VR-OPI-e26b27a7-…16` PASS** 于 `cb254c0`；**零迁移（head 0018_delivery_records / v18）**
  - **p9-1（canonical-delivery-records）三件（第十九会话，新 OPI 系列 `e26b27a7-2164-4a56-9017-2177ee1d00d9`；Phase 9 第二刀）**：`TASK-OPI-e26b27a7-…3`（任务书 A–E 五面：迁移 0018 五表逐列照 §22/§21.1 / SQL-free 端口 + SQLite 适配 / 删除面三清单同刀 / head·stamp 钉搬迁 8 文件 7 组 / tests/phase9 三文件；R1–R12 预裁）/ `VAL-OPI-e26b27a7-…5`（切片验收七组）/ 处置 `DEC-OPI-e26b27a7-…9`（评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM、**2 LOW + 6 INFO**、**12 组变异全 RED、零 NOT-RED**；F1 恒真断言·F2 同族过期散文·F3 名实相符 三条**修** + F4–F8 **登记**（各带触发条件）；执行者 8 异议逐条判定（漏钉两处 / 漏 `_conversation` 五腿 等两条成立）+ 总控起草缺陷 5 项自认（含**母计划与相位验收把 §21.1 两表列数写成 8/7，实为 7/6**）；A 面自写解析器 **FAILS:0**、头注引文抽查 15 处**零偏移**）；交付 `fc2e818` + 处置修 `26024e2`；盖 **`VR-OPI-e26b27a7-…7` PASS** 于 `26024e2`；**新增迁移 0018（head `0018_delivery_records` / v18）**
  - **p9-0（carryover + trust boundary）三件（第十八会话，同一 `92170eff-…` 系列；Phase 9 首刀）**：`TASK-OPI-92170eff-…29`（任务书 A–D 四裁决：`factor_trace` versioned structured JSON `ft1` / `ContinuationFacts.terminalizing_action` / 门三区域框架 / ReviewEvent writer contract + **anchor 裁置请求**）/ `VAL-OPI-92170eff-…31`（切片验收五组）/ 处置 **`DEC-OPI-92170eff-…34`**（评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM、3 LOW + 4 INFO、**12 组变异全 RED**、**零 0-RED**；LOW-1/2/3 全修（各带变异/实断言自证）+ INFO-1/2 措辞收窄 + INFO-3/4 登记；执行者 8 异议**全成立** + 总控起草缺陷 2 项自认；评审首派 600s inactive 中止后重派成功留痕）；交付 `77fa549`（含处置）；盖 **`VR-OPI-92170eff-…33` PASS** 于 `77fa549`；**零迁移（head 0017/v17）**
  - **Phase 8 外评采纳（第十八会话，同一 `92170eff-…` 系列）**：`DEC-OPI-92170eff-…21` —— 外评基于远程 `b098e8a` 复核确认 **`PHASE 8 COMPLETE = YES`（不需 reopen）+ `PHASE 9 IMPLEMENTATION = GO` + `REAL AUTOMATIC-TEACHING ROLLOUT = CONTINUE HOLD`**；总控逐条取证**五项全成立**（两个 MEDIUM = durable `factor_trace` 完整度 / `ContinuationFacts.terminalizing_action`（**与仓内 p8-1 DEFER F11 独立同击**）；ReviewEvent anchor 的 P7-0 冻结 **ANCHOR_ONLY** 与 P8-3 成败四词的紧张（**精确化为「有意冻结、需重裁」**）；SessionBudget 的 `remaining=None` 行为上等于「还有预算」（三分 `UNBOUNDED/KNOWN_REMAINING/UNKNOWN` 与 UNKNOWN 不得 fail-open = rollout 前置）；门三进 Phase 9）；六刀切片**采纳为提议**（位次由开工第 0 步定）；分维度评级逐条确认（SessionBudget 收窄为「PASS + rollout 前置」）
  - **p8-5（rollout-gate，RISK_SPIKE）+ Phase 8 相位收口（第十八会话，同一 `92170eff-…` 系列）**：`TASK-OPI-92170eff-…10` / `VAL-OPI-92170eff-…12` / 处置 `DEC-OPI-92170eff-…16` / **收口 `DEC-OPI-92170eff-…19`**；交付 `9506c56`（含处置）；盖切片 **`VR-OPI-92170eff-…14`** + **相位验收 `VR-OPI-92170eff-…18`** PASS 于 `9506c56` ⇒ **`PHASE 8 = COMPLETE`（六工作项 p8-0..p8-5 全毕）**；真实 rollout 仍 **HOLD**（门槛检查器对真语料 `VERDICT: HOLD / automatic: HOLD`，四行 usable targets 0、14/14 without a level）；**零迁移**
  - **p8-4（同 turn 集成 + exposure 生产者 + 0017 provenance 腿）三件（第十八会话，同一 `92170eff-…` 系列）**：`TASK-OPI-92170eff-…3`（任务书 A–H 八裁决）/ `VAL-OPI-92170eff-…5`（切片验收八组）/ 处置 `DEC-OPI-92170eff-…8`（评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM、10/11 变异 RED；F1–F8 处置；G7 先于本刀的删除面缺陷修复；通道 6+3 次训诫留痕 + 立规；总控 5 项缺陷自认）；交付 `08dda87`（含处置）；盖 **`VR-OPI-92170eff-…7` PASS** 于 `08dda87`；**本刀新增迁移 0017（head v16→v17）**
  - **Phase 8 开工三件（第十六会话）**：`DEC-OPI-0f76024b-…3`（开工 + 前沿裁决 R1–R7）/ `PLAN-OPI-0f76024b-…5`（六工作项 p8-0..p8-5 + 八关系）/ `VAL-OPI-0f76024b-…7`（相位验收八组；④ 记 `OQ-024A` 无载体 ⇒ 60 例 gate benchmark 为可执行替代）
  - **p8-0（CP2 durability）三件（第十六会话，新 OPI 系列 `64c88704-f160-4cae-820b-1c9380a203bd`）**：`TASK-OPI-64c88704-…7`（任务书：七裁决 + 迁移钉预写 6+2+登记类 + 预算带）/ `VAL-OPI-64c88704-…9`（切片验收七组）/ 处置 **`DEC-OPI-64c88704-…11`**（评审 ACCEPT-WITH-FINDINGS 无 HIGH；F1 修 + F2/F3/F4 修 + F5–F8 修 + **F9 DEFER**；两处总控起草缺陷自认）；交付 `42d1b16` + 处置修 `0479cbf`；盖 **`VR-OPI-64c88704-…13` PASS** 于 `0479cbf`
  - **p8-1（Gate 自动面）三件（第十七会话，同一 `64c88704-…` 系列）**：`TASK-OPI-64c88704-…17`（任务书：A–G 七裁决 + 单条预写钉 + 预算 50–130）/ `VAL-OPI-64c88704-…19`（切片验收七组）/ 处置 **`DEC-OPI-64c88704-…21`**（评审 ACCEPT-WITH-FINDINGS 无 HIGH、13/17 变异 RED、4 组 0-RED 全入 findings；**F1 重放短路（MEDIUM）** + **F2 权威推导点名（MEDIUM）** + F3–F7/F9/F10 修 + F8/F11/F12 登记；两处通道违规训诫留痕 + 口径收紧；四项总控起草缺陷自认）；交付 `b43d4bc`（60 例 benchmark 重放 60/60）+ 处置修 `325e7a6`；盖 **`VR-OPI-64c88704-…23` PASS** 于 `325e7a6`
  - **p8-3（台账落表 + 删除面 + ReviewEvent 语义）三件（第十七会话，同一 `6d40862d-…` 系列）**：`TASK-OPI-6d40862d-…9`（任务书：A–H 八裁决 + 迁移钉预写 + 预算 60–140）/ `VAL-OPI-6d40862d-…11`（切片验收七组）/ 处置 **`DEC-OPI-6d40862d-…13`**（评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM、**14/14 变异全 RED**、2 LOW + 5 INFO；F1/F2 修 + INFO-1/3 修 + INFO-2 确认 + INFO-4/5/6 登记；四项总控起草缺陷自认 + 四项执行者异议全判成立）；交付 `ff769e9`（+ 补 `280adc1`）+ 处置修 `ddbfd90`；盖 **`VR-OPI-6d40862d-…15` PASS** 于 `ddbfd90`
  - **p8-2（会话预算视图 + 权威化）三件（第十七会话，新 OPI 系列 `6d40862d-ead3-426b-9504-46206c9e0cf8`）**：`TASK-OPI-6d40862d-…1`（任务书：七裁决 + 逐字段派生裁定 + 预算 40–110）/ `VAL-OPI-6d40862d-…3`（切片验收七组）/ 处置 **`DEC-OPI-6d40862d-…5`**（评审 ACCEPT-WITH-FINDINGS 无 HIGH/MEDIUM、21 组变异 19 RED、1 组真 0-RED=F1、1 组等价；**F1 修 + F2–F6/F12 修 + F7–F11 登记**；两裁定 = `SHADOW_MODE_MODEL_VERSION` bump `sh2` / `gate.py` 散文授权（AST 同一）；三项总控起草缺陷自认 + 评审侧 1 次通道违规训诫留痕）；交付 `14041d0` + 处置修 `2f0b8e3`；盖 **`VR-OPI-6d40862d-…7` PASS** 于 `2f0b8e3`
  - **Phase 7 外评采纳（第十六会话，新 OPI 系列 `0f76024b-4634-422f-ab0a-7f05bb786df0`）**：`DEC-OPI-0f76024b-…1` —— 外评复核确认 **`PHASE 7 COMPLETE = YES`（不需 reopen）+ `PHASE 8 = GO` + 真实 rollout = HOLD**（原因 = 语料 readiness，非 Planner）；总控逐条取证含**两处修正/补强**（`NOT_SCHEDULED` 的计分取决于取行路径；`OQ-024A` 无载体 ⇒ `behavioral_baselines/gate/` 的 60 例 benchmark 为可执行替代）
  - **第十五会话铸出的 DEC（`OPI-44647873-…` 系列；此前拒收的五件 + 本刀两件）**：门二 `…4`（接受 4ce43ed + 三处有据收窄 + S41/S44 不可满足登记）/ 外评采纳 `…7`（P7-0 消费边界冻结为先决）/ P7-0 `…10`（首交整刀拒收 + 重交 as-found 吸收 + 通道合规 UNCERTAIN）/ p7-1 处置 `…12`（含 CRLF 指纹入册）/ p7-2 处置 `…14`（含评审侧 dmcp 全 `OBJECT_NOT_FOUND` 留痕）/ **p7-3 前沿裁决 `…16`**（R1 §19 位置且前沿=第 4 步幸存者集 / R3 台账纯核不落表 / R4–R6 计提与读数 / R7 UNKNOWN 不转义）/ **p7-3 处置 `…25`**（3 LOW 已修 + 4 INFO 入册 + 总控起草缺陷自认两项）/ **p7-4 前沿裁决 `…29`**（影子 = 纯记录零派发 / 43 例走**独立适配层** / 三分歧显式建模：S04·S42 由边界拒绝、S20 由运行器前置读）/ **p7-4 处置 `…36`**（4 LOW 已修 + F5/F6 留痕 + F7/F8 登记；含首轮中断与吸收、两条既有测试钉搬迁的定谳与纪律口径裁定）
  - **p7-1（Planner kernel + trace）三件**：`TASK-OPI-fc9d8ca7-…4` / `VAL-OPI-fc9d8ca7-…2` / 盖 **`VR-OPI-fc9d8ca7-…8` PASS** 于 `d876ed5`（交付 `f79a36d` + 处置修 `d876ed5`；执行者 1 次通道违规＝训诫留痕 + **新建文件 CRLF 指纹**入册；评审 F1/F2 两条 MEDIUM 的处置与留待项见「当前状态」）
  - **P7-0（消费边界冻结）四件**：`PLAN-…b99560d4.…32`（3 slice + 2 spike）/ `VAL-…b99560d4.…34`（验收六组）/ `TASK-…b99560d4.…36`（**外评八条取证 + 判定 + 裁置的权威载体**）/ 盖 **`VR-OPI-2394139d-…5` PASS** 于 `38ca076`（交付 = 重交 `e388c4c` + 处置修 `38ca076`；首交 `f4350ce`+`7e3b7e4` 因**同一刀两次通道违规**判**拒收**）——**门二 DEC 与外评采纳/P7-0 DEC 均待补铸（`decision_add` 契约故障，本会话 9 次拒收）**
  - **门二（BF-05 删除与墓碑）四件**：`PLAN-…b99560d4.…14` / `TASK-…b99560d4.…19`（**四查 + R1–R15 全文**）/ `VAL-…b99560d4.…17`（**已被取代**）→ `VAL-…b99560d4.…26`（相位验收，⑤ 记 profile 腿有据收窄）/ 盖 **`VR-…b99560d4.…28` PASS** 于 `4ce43ed`——**门二 DEC 未铸（`decision_add` 契约故障，本会话 6 次拒收）；补铸前以 TASK-…19 + 本文件为权威**
  - `DEC-OPI-b99560d4-…10` **Phase 6 收口**（四刀链 + **同题扩版相位 VAL `VAL-…b99560d4.…6` 取代 `VAL-…a68fd9eb.…44`** + 遗留账七项接管 + 门二四查预告 + 读数 1824/1158/666 + head 0013/v13 + 冻结面零改动；`VAL-…b99560d4.…6` 盖 **`VR-…b99560d4.…8` PASS** 于 `97b0dc7`）——**下一刀（门二）先读**
  - `DEC-OPI-5a0be06d-…23` **p6-3 处置**（评审 ACCEPT-WITH-FINDINGS 无 HIGH；L-1/L-2/INFO-1/INFO-2 修 `4a941a1`；L-3 预算留痕 + 计数口径教训；INFO-3 DEFER；流程书序修正；总控任务书笔误承认）+ `TASK-…5a0be06d.…20` / `VAL-…5a0be06d.…18` 盖 **`VR-…5a0be06d.…22` PASS** + `PLAN-…5a0be06d.…13`（p6-3 计划，已毕）——**相位收口前先读**
  - `DEC-OPI-5a0be06d-…15` **p6-3 前沿裁决 R1–R12**（所有权 user_config / 0013 / §9 九列·四类型·三 scope / active 独立转移面 / 读面窗口与目标腿 / 豁免面日志 / 22 处断钉预写 / 迁移常量收口）——历史（收口已毕）
  - `DEC-OPI-5a0be06d-…9` **p6-2 处置**（评审 ACCEPT-WITH-FINDINGS 无 HIGH；F-1/F-3/F-4 修 `63c8064`；**F-2 DEFER** 带触发条件 = 下次触碰 `scheduler/store.py` 的刀；F-5–F-10 留痕；执行者 6 项裁定 + 总控流程顺序偏离自报；**通道纪律第四段「仓内零写入含临时文件」自此立**）+ `TASK-…5a0be06d.…6` / `VAL-…5a0be06d.…4` 盖 **`VR-…5a0be06d.…8` PASS** + `DEC-…5a0be06d.…2`（p6-2 前沿裁决 R1–R15，见下）
  - `DEC-OPI-5a0be06d-…2` **p6-2 前沿裁决 R1–R15**（零迁移 / due 四步派生 + 实现声明常数 / urgency 四锚取自 BF-02 JSON / `event_type` 仍无词表 / watermark 十进制串 / **内容寻址 version** / ScheduleView 加 `as_of` / `SessionBudgetView` 留 Phase 8 / **`PlannerConstraint` 排位 p6-3 归 user_config**）
  - `DEC-OPI-6259f6fd-…16` **p6-1 处置** / `DEC-OPI-6259f6fd-…8` **p6-1 前沿裁决 R1–R18**（迁移 0012 / §5.2 逐字面 / 骨架面处置 / 16 处断钉预写）——历史
  - `DEC-OPI-a68fd9eb-…11` **PHASE 5 工程收口**（三刀链+切片三验收+相位验收六组+内容补课清单九项+metadata 三件+路径集判据+三扇硬门）——**其②③语义断言（R0–R4 层、linkage 批准）已由 `DEC-…15` 撤回，语义以 `DEC-…15` 为准**；相位验收 `VAL-…4d516e4f.34` 盖章 `VR-OPI-a68fd9eb.…9` PASS 零告警（首提 `VR-…a68fd9eb.6` INCONCLUSIVE 已留痕为机制事实）
  - `DEC-OPI-a68fd9eb-…57` **p6-0 处置**（评审 PASS 无 HIGH；F-1 任务书缺陷追认 14 处测试搬迁；F-2/F-3 已修；F-4/F-5/F-6/F-7/F-8 留痕；SessionFocus append-first 为派生判据）+ 交付 `TASK-…48`/`VAL-…50` 盖 `VR-…52` PASS——**p6-1 开工先读**
  - `DEC-OPI-a68fd9eb-…40` **Phase 6 开工 + 四条前沿裁决**（ReviewState 词表=NOT_SCHEDULED/UPCOMING/DUE/OVERDUE 等）+ `PLAN-…a68fd9eb.…42`（p6-0/p6-1/p6-2）+ `VAL-…a68fd9eb.…44`（相位验收六组）——**Phase 6 开工先读**
  - `DEC-OPI-a68fd9eb-…32` **P5-R 处置 + PHASE 5 语义验收重签**（独立评审 ACCEPT 无 HIGH：4 组变异全被测试捕获、12 句 shipped 探针、基线 1088 纯净副本复现；F-1 修 `8fa1eba`、F-2 修 `7039889`、F-3/F-4 口径校正、F-5/F-7/F-8 DEFER 留痕；**显式取代 `DEC-…a68fd9eb.11` 的「R1 解释性读法」句与「语料 14 target 全为 R1」句**，亦取代 `DEC-…4d516e4f.…51` 的解释性读法条款）
  - P5-R 三件链：`PLAN-…a68fd9eb.…19` / `TASK-…a68fd9eb.…22` / `VAL-…a68fd9eb.…24` 盖 `VR-…a68fd9eb.…29` PASS（切片）；**相位语义验收重签** `VAL-…a68fd9eb.…34` 盖 `VR-…a68fd9eb.…36` PASS
  - `DEC-OPI-a68fd9eb-…15` **外部评审采纳（Phase 5 复核 + P5-R 授权）**（三 blocker 取证 + P5-R 四工作项 + 过程教训：评审缺「实现 vs canonical 原文」逐字达标面）——历史
  - P5 三对交付：`TASK-…4d516e4f.38/.46/.55` + 切片盖章 `VR-…42/50/59`（全 PASS）+ 处置 `DEC-…43/51/60`；`PLAN-…4d516e4f.36` Phase 5 母计划（**全毕**）
  - `DEC-…4d516e4f.29` **P4-4 处置 + PHASE 4 COMPLETE 收口**（五刀链+五验收+**遗留清单七项**：derive_reply 启发式/prompt 转义归 Phase 9/Episode status/双证口径/history 期望/零候选边界/服务端 warning）——Phase 4 收口，未完项挂账
  - `DEC-…4d516e4f.22` P4-3 处置（其六项已由 P4-4 全兑现或转留痕）；`DEC-…4d516e4f.13` P4-2 处置；`DEC-…5ba74efc.115` P4-1 处置；`DEC-…5ba74efc.96` P4-0 处置；`DEC-…5ba74efc.68` **外部评审采纳（Phase 3 复核+P4-0 授权）**
  - `PLAN-…5ba74efc.82` Phase 4 母计划（p4-0..p4-4，**全毕**）；`VAL-…5ba74efc.70/.72/.80` 直盖 PASS；`VAL-…74/76` 路径集过窄 → 同题扩集 `VAL-…4d516e4f.7/.17` 代盖（取代关系见 `DEC-…4d516e4f.13/.22`）
  - P4 五对交付：`TASK-…5ba74efc.84/.100`+`VR-…91/.109`（P4-0/P4-1）；`TASK-…4d516e4f.9`+`VR-…12`（P4-2）；`TASK-…4d516e4f.19`+`VR-…21`（P4-3）；`TASK-…4d516e4f.26`+`VR-…28`（P4-4）
  - `DEC-…5ba74efc.65` **PHASE 3 COMPLETE 收口**（五刀链+遗留清单七项）
  - `DEC-…2babb21e.5` P3-1 开工基线；`DEC-…5ba74efc.43/.20`+`DEC-…2babb21e.34` P3-2/P3-1B/P3-1A 评审处置
  - `TASK-…5ba74efc.2/.24/.47`+`VR-…5ba74efc.13/.38/.57` P3-1B/P3-2/P3-3 任务书+盖章（PASS）
  - `VAL-…2babb21e.7/.9/.11/.13` P3 四验收（全 PASS）；`PLAN-…2babb21e.15` Phase 3 母计划；`TASK-…2babb21e.17`+`VR-…2babb21e.25` P3-1A 两件
  - `DEC-…eaaa5a1d.58` P3-0 评审处置；`DEC-…eaaa5a1d.51` 外部评审采纳（Phase 3 GO）；`DEC-…eaaa5a1d.48` PHASE 2 COMPLETE；`DEC-…eaaa5a1d.33` 拒收政策升格；`DEC-…eaaa5a1d.26` Phase 2 开工
  - `PLAN-…eaaa5a1d.19` Phase 2 PLAN；`VAL-…eaaa5a1d.17` 相位门；P2A/P2B 各系列 id 见旧交接（`git log` 可溯）
  - `DEC-…eaaa5a1d.2` 外部评审采纳（权威漂移）；`DEC-…d7937fd7.19`/`DEC-…d7937fd7.12` Phase 1 评审/COMPLETE
  - `DEC-…9dc4e77f.4` **契约载体**（跨会话重建先读它）；`DEC-…091f35c3.7/.13`；`DEC-…d5b616bf.3/.7`；`DEC-…7bf80972.2` PHASE 0

## dmcp 工作流协议（v2，2026-09-20 用户纠正后立——总控必守）

每刀（task）完整链：**四查+VAL+TASK 同铸 → 派执行者 → 总控独立复核（pytest/ruff/mypy/CI 亲跑）→ 独立评审（新上下文 executor-pro 只读评审，产 findings）→ 处置（修/DEFER/留痕）→ validation_submit 盖章（在最终验证过的提交上）→ design_sync REBUILD+CHECK 收口**。缺评审即违约（本仓 2026-09-20 已犯一次，用户点名纠正）。**顺序修正（2026-09-22 p6-2/p6-3 实测）**：原书写「复核 → 盖章 → 评审 → 处置」在实践中不可执行——VR 必须盖在**最终验证过的提交**上，而「处置」按定义会再改一次代码（p6-1/p6-2/p6-3 的 VR 实际都盖在处置修后的提交上）⇒ 自本行起以「处置 → 盖章」为书序（盖章仍是验收要件，只是位次挪到最终态）；因此「盖章后不得再改该切片代码」成为新硬约束。**迁移刀的成本（p6-1/p6-3 实测）**：新增一个迁移会使**所有** head/stamp/applied 钉变假（0012 断 16 个测试、0013 断 22 个），任务书必须**逐条预写**搬迁名单；自 p6-3 起这些值集中在 `tests/conftest.py` 的 `MIGRATION_IDS`/`SCHEMA_HEAD_FILE`/`SCHEMA_HEAD_VERSION`（改一处 + 该刀自己的迁移测试），并有一条自证钉校验常量 == 真实仓库。

- **纪律 ①（⟳ 刷基）**：连续两次 dmcp 变更之间必须 design_status 纯读刷新基再填 expected——REVISION_CONFLICT 即忘了刷基（错误面会给 current id 可直接重构）
- **Phase 开工第 0 步**：`plan_build` 铸 PLAN（VERTICAL_SLICE/RISK_SPIKE/VALIDATION_EXPERIMENT/ENABLER 工作项+relations）；任务从 plan 工作项展开。**readiness 更正（2026-09-20 第四会话查实）**：design_readiness 的 gate 行来自 gate policy 而非 plan_build——默认策略是 FINDING-only 单族行集（每条 finding 一行 BLOCKING），finding 册恒空 ⇒ 恒 READINESS_BASIS_INCOMPLETE；解锁要么服务端接线 preparer 让 findings 真实入册，要么宿主侧配 `DMCP_READINESS_POLICY_FILE`（宿主操作，不代执行）。**阶段门禁当前由本仓工作流承担**（VAL+独立评审+decision），readiness 仅作解锁后的复核面
- **评审面构建局限（当前服务端，已铁证）**：REVIEW_REQUIREMENT/REVIEW_PACKET 只能由宿主侧 preparer 铸，而 dist 无任何入口接线 `createHostReviewBasisPreparer`（D:\MCP apps/mcp-server，查实 0 wiring）→ review_submit 恒 OBJECT_NOT_FOUND、finding 册因构造恒空（finding_list=0 是构建事实不是「无问题」）。**降级路径**：评审照做（独立上下文评审员），findings 全文+证据+裁定入 decision（范式见 `DEC-…d7937fd7.19`/`DEC-…eaaa5a1d.9`），服务端升级接线后补录正式 finding 并迁移裁定
- **主体/能力（当前服务端 env）**：HUMAN_USER + DESIGN_STATE_READ/MUTATE 两族——FINDING_ADJUDICATION/DISPOSITION 族未授，裁决面即使有 finding 也会被诚实拒绝；升级服务端配置属宿主侧操作，skill 不代执行
- 契约=跨会话记忆：GOAL/RUNTIME/IN_SCOPE/OUT_OF_SCOPE/TECHNICAL_CONSTRAINTS/ASSUMPTIONS 在载体 `DEC-…9dc4e77f.4`（含六 canonical 文件 digest 绑定，漂移即假设失效；AGENTS.md 活文档不绑）；契约改写是整体替换（⑲），改前先 design_get 读回

## 派单协议（用户明令，2026-09-20）

- **执行者 = `executor-flash`（一般任务）/ `executor-pro`（难题）**——用户安装在 `~/.zcode/agents/`（model 钉 GLM-5.3-Flash / GLM-5.3，**须带 `thoughtLevel: max` 键**——2026-09-20 补，缺则新 harness 拒启，见 `DEC-…091f35c3.13`）。**禁止用内置 `general-purpose` 做执行任务**。
- 总控不亲自执行（写代码/测试/探针/部署一律派执行者，先 `task_build` 铸任务书）；设计类任务书先过 grilling 前沿；**任务书四查 + VAL 与任务书同铸**（前沿/实现设计条目/计划时效/预算算术；交付后补盖 VAL 视同无效验收）。红线全文 0–31 见旧仓 `D:\测试1\AGENTS.md`（红线 28/29/30/31 尤其要读）。

## 工程事实与红线（本仓特有）

- **远程**：github.com/ZJW11398925/english-learning-clean（public）；**CI 四门**：ruff → mypy → compileall → pytest（push+PR，Ubuntu，0 skipped 为准）
- **测试**：`python -m pytest`（当前 **2787/0/0**，0 skipped；**非 phase6/7 不含 deletion = 1581**；`tests/phase8` = **422**；`tests/phase3` = **280**；`tests/phase7` = **420**；`tests/deletion` = **120**；phase6 = 666；**collect 须写 `-o addopts=""` 才会打印总数**）；静态：ruff + mypy（**144** 文件 0 error）；migrations 到 **0016**（schema_version **16**；0011 = p6-0 三表、0012 = p6-1 的 `schedule_item`/`review_event`、0013 = p6-3 的 `planner_constraint`、0014 = 门二的 `deletion_tombstone`、0015 = p8-0 的 §14 四表 `planner_evaluation`/`planner_decision`/`planner_execution_status`/`runtime_decision_outcome`、**0016 = p8-3 的台账三表 `planning_ledger`/`coverage_obligation`/`planning_ledger_event`**；p8-1/p8-2 零迁移；**p6-2 零迁移**；head/stamp 值集中在 `tests/conftest.py` 的 `MIGRATION_IDS`/`SCHEMA_HEAD_FILE`/`SCHEMA_HEAD_VERSION`；新增迁移会使同族钉变假，搬迁面须**预先写明**——门二实测 **27 处**（25 实改 + 2 核过））；GenerationAction 权威在 runtime（DM §16）；DecisionCycle 权威在 Runtime（`DEC-…2babb21e.5`）；TeachingMoment/Attempt 全链已实装（P3-1/2/3）；**Phase 4 全毕（P4-0..P4-4）**：Relationship durable core（P4-1）+CP4 projection runtime（P4-2，`elc.runtime.projections` SQL-free + `elc.platform.db.projection_store`）+Episode 投影与 DisclosedUserProfile 与 PromptCompiler 八节（P4-3）+E2E/压力验收（P4-4：`tests/phase4/test_p4_4_{full_chain,stress}.py`；F-1 缺陷修复=分类器读失败不再被洗成空成功）；**Phase 5 COMPLETE（P5-0/P5-1/p5-2 + P5-R 语义重签已毕）**：`content_src/`+`curriculum/` 资产（14 validated target，P3 fixture 原件零改动）+ `elc/content`（确定性构建 content.db 2c617a95…bde96 / 只读读面写全拒）+ `elc/curriculum`（readiness R0–R4 + production `ContentBackedTeachingTargetProvider`）+ `elc/learning`（抽取式 target resolution + silent evidence 链，无 TeachingMoment/Gate/Planner）；`tests/phase5/` 327 新用例，全零 `_seed()` 真链路；**Phase 6 COMPLETE（四刀全毕 + 相位收口已毕，收口 `DEC-OPI-b99560d4-…10`；盖 `VR-OPI-b99560d4-…8` PASS 于 `97b0dc7`）**：`elc/user_config`（§5.1 Goal/Policy/Focus + 版本/append-first 纪律 + keying convention；§9 `PlannerConstraint` + 两 StrEnum + 5 面）与 `elc/scheduler`（§5.2 `ScheduleItem`/`ReviewEvent` 逐字 + `SqliteSchedulerStore` 当前投影/append-first/CONFLICT/epoch fence + `spacing.py` 纯策略 due 四步派生 + `recompute_schedule_item`/`get_schedule_view(as_of)`/`is_review_due` 三法 + 内容寻址 version）+ registry 换代；**门二 COMPLETE（BF-05 source-aware 删除与墓碑，独立一刀）**：`src/elc/deletion/`（七文件：scope 词表 / 删除计划 / store / controller / commands / queries / `__init__`）+ `migrations/0014_deletion_tombstone.sql`（v14）+ `tests/deletion/` **120 例** + **27 处既有迁移钉搬迁**；独立评审 ACCEPT-WITH-FINDINGS 无 HIGH、处置修 `4ce43ed`、盖 **`VR-OPI-b99560d4-…28` PASS**、sync SYNCED 117；**PHASE 7 COMPLETE（Planner shadow mode + Planner Behavioral Baseline V1）：四切片 p7-0..p7-4 全毕 + 相位验收 `VR-OPI-44647873-…38` PASS 于 `5395694` + 收口 `DEC-OPI-44647873-…39`；下一动 = Phase 8 开工第 0 步**：p7-1 kernel+trace（`f79a36d`+处置 `d876ed5`）+ p7-2 候选供给侧（`4c6afb7`+`40ffa5c`+处置 `53b330e`）+ p7-3 frontier+ledger+coverage（`821b247`+处置 `51a9868`+收口 `b7b29f3`）+ **p7-4 shadow mode + 冻结 43 例重放（`f66cc2d`+处置 `5e33d0d`；`src/elc/planner/{shadow,stress_suite}.py`；43/43 且与冻结参考实现逐例一致；**零迁移**）**
- **规范优先级执行**（P4-0 教训留痕）：canonical 与既有实现漂移时**实现服从 canonical**（列名/词表逐字照原文；无决策裁定不得以「Phase 0 拼写」为由倒置）；本仓已有两例（0007 列集、P4-0 relationship 列名/枚举）——任务书四查加「列名/词表照 canonical 原文」必答项
- **门是意图不是障碍**（P4-2 教训留痕）：实现**不得为过架构门而变形**——P4-2 首轮执行者把 `ProjectionExecutor.execute` 以「取方法值再调用」（`run = executor.execute; run(view)`）绕开 Gate 2 的 `.execute(` 调用形态扫描，总控判返工改**正向命名** `project()`（Gate 2 把 `.execute()/.commit()/.rollback()` 词表留给 runtime 不得触碰的 DB 机件；改名后结构性护栏不变、独立 AST 扫描 0 违规）。同轮总控把执行者自报的边界「RUNNING 残骸本刀不回收」升格为本刀内做（RA §22 明文把 pending projections 列入恢复扫描）——**边界自报是诚实的，但「RA 明文要求」级缺口不因自报而免做**
- **VAL 路径集纪律**（P4-2 教训留痕）：验收的 `applicability_contract.paths` 必须覆盖实际触及路径——VAL-…74 漏 `src/elc/platform/db`（Gate 2 规定 runtime 无 SQL，Runtime 拥有的投影适配器只能落此）与 `src/elc/teaching`（对账需 refs 扫描面），P4-2 铸同题扩集 `VAL-OPI-4d516e4f.…7` 盖章并在 `DEC-…4d516e4f.13` 显式留痕取代关系（**不双重声明同一观察**）；P4-3 同例（VAL-…76 漏 runtime/migrations → `VAL-…4d516e4f.17`）
- **落盘通道三段纪律**（P4-3 教训留痕，第六次违规后升级；**p6-2 追加第四段**）：①一切 `.py/.sql/.yml/.toml` **一律 Write/Edit 工具落盘**（Bash heredoc/`sed -i`/重定向/`ruff --fix` 等一律违规——`--fix` 属同族写操作）；②回执必附**「Bash 使用清单」**（逐条命令意图，非只读用途标红；同一刀内第二次违规=整刀拒收重交）；③**字节零差复写一律用 Edit 往返 + 四重验证**（H1==H2、`--no-filters` raw 相等、字节数/行数相等、分块 sha256 全等），**禁止**对超大文件（本仓 `runtime/controller.py` 195KB）做单次全量 Write 复写——该文件含历史 mojibake 字节（~3637 行），Read 显示与真实字节不一致，人工转录必带显示伪影；④**仓内零写入（含临时/诊断文件）**——「同一条命令内创建并随后删除」**不构成豁免**（红线约束的是钩子与审计面而非持久性，见 `DEC-…5a0be06d.…9`）；需要诊断脚本一律落仓外临时目录。**p8-1 追加第五段（口径收紧）**：**变异/诊断脚本（含 `.py`）一律 Write/Edit 工具落盘——仓外副本也一样**（处置刀曾以 `printf >> <仓外副本>.py` 追加变异，虽仓内零痕仍计一次违规）；Bash 只许只读命令、`git add/commit` 与不在禁列的构建命令（`pytest`/`ruff check`（无 `--fix`）/`mypy`/`compileall`）；**同一刀第 1 次＝训诫留痕、第二次＝整刀拒收重交**（p8-1 切片两刀各 1 次、均训诫留痕；本切片内第三次违规一律整刀拒收）
- **冷启动 import 闭环**（P4-3 教训留痕）：`elc.runtime.__init__` 聚合导出 + 域模块互相引用可造出「域模块 → runtime 包 → 新模块 → 域模块」的闭环，测试套件因导入顺序侥幸不报而 `import elc.conversation` 冷启动失败；解法＝本地窄 Protocol + `TYPE_CHECKING` 注解导入 + **subprocess 冷启动钉**（逐包 import）——凡新增跨包引用面必带冷启动钉
- **Mimosa 安全钩子**：Bash 直写源码/配置被拦——**.py/.sql/.yml/.toml 一律 Write/Edit 工具落盘**（P1B 执行者曾一次 Bash 直写 .py 未被钩子拦但违反明令，已在 `DEC-…d7937fd7.12` 训诫留痕——总控与执行者均不得再犯）；SQL 一律参数绑定或全字面量（标识符用白名单全字面量），禁任何拼接
- **基线冻结**：`behavioral_baselines/` 的 .py 一字节不改（例外须按 `DEC-…d5b616bf.3` 范式：显式决策+最小改动+manifest 重钉）；六 canonical 文档只许 `DECISION_REGISTER.md` 的版本条目
- **不做**（纪律）：未到位次的域逻辑/域表提前实现（位次见基线 §1.5：**Phase 5 工程 + 语义均已 COMPLETE（P5-R 已毕并重签）**；当前位次 = **Phase 8（Teaching Gate + 自动教学面）**——**p8-0/p8-1/p8-2/p8-3 均已 COMPLETE、p8-4（同 turn 集成 + exposure 生产者 + 崩溃窗口 Moment 半）可以开**；**真实 automatic-teaching rollout 本体仍 HOLD**（语料 14/14 `readiness=None` ⇒ R2/R3/R4 可用目标 0；BF-02 §10 逐字门槛——**不是 Planner 的问题**））；Redis/Kafka/分布式锁/TTL/heartbeat（Local V1 §24.1 永禁）；自动教学/Planner SELECT（Phase 8 才开）；`decision_cycle_id` 收紧已随 0007 兑现（`DEC-…091f35c3.7` 裁定 b 闭环）
- docs/run_post_bf_regression.py 的 ROOT 自相矛盾是原件冻结缺陷，永久不可修——repo-native 七套件是长期替代；勿再尝试修它
- Windows：core.autocrlf 与 blob 有字节差，哈希校验须 CRLF 归一；建议后续加 .gitattributes（勿扰动冻结文件行尾）

## 常用命令

```bash
python -m pytest          # 全量（含架构门与七套件行为基线）
ruff check . && mypy src  # 静态门
git push                  # CI 自动跑四门
```
