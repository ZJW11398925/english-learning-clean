# AGENTS.md — english-learning-clean（Clean Rewrite · Implementation Baseline V1）

## 项目一句话

成人英语学习应用「英语客厅」的 **Clean Rewrite**：以 Implementation Baseline V1（用户主导的更高智力讨论收敛，2026-09-20 交回）为唯一规范权威，从零实现。**规范优先级：本仓 `docs/` 六 canonical 文档 > `behavioral_baselines/` 行为基线资产 > 其他一切**（旧仓 `D:\测试1` 整体为 exploration archive，其全部设计仅作历史参考）。实现栈已裁：**Python 内核 + SQLite**（app.db 可变 / content.db 只读 / SecretStore；客户端边界已裁 `DEC-…d7937fd7.12`：进程内 Python API，交互面推迟 Phase 8 末后）。

## 🔴 dmcp 段（新会话续接第一入口）

- **workspace_id**：`ws-7585bd9c-f6ec-4c73-be14-d2e635a37033`（**schema 5**；旧工作区 `ws-db58afd2-…` 已随旧仓归档，别用）
- **续接顺序**：①读本文件 → ②`design_status` 刷基 → ③`design_get` 首要五件（**Phase 6 收口位**）：`DEC-OPI-5a0be06d-5af8-4124-9550-88fda7b22587.23`（**p6-3 处置**：评审 ACCEPT-WITH-FINDINGS 无 HIGH / L-1·L-2·INFO-1·INFO-2 已修（`4a941a1`）/ L-3 预算留痕 + 「今后任务书写预算带须写明计数口径」/ **流程书序修正**（处置→盖章）/ **总控任务书笔误承认**——**收口前先读**）＋`DEC-OPI-5a0be06d-…15`（**p6-3 前沿裁决 R1–R12**：所有权 user_config / 0013 / §9 九列·四类型·三 scope / **active 独立转移面** / 读面时间窗与目标腿 / **22 处断钉预写** / R10 迁移常量收口）＋`DEC-OPI-5a0be06d-…9`（**p6-2 处置**：due 四步派生的读法边界、F-2 DEFER 触发条件、通道纪律第四段）＋`DEC-OPI-5a0be06d-…2`（**p6-2 前沿裁决 R1–R15**：零迁移 / 内容寻址 version / `ScheduleView(as_of)` / `SessionBudgetView` 留 Phase 8 / **`PlannerConstraint` 排位 p6-3 归 user_config**（已兑现））＋`VAL-OPI-a68fd9eb-…44`（相位验收六组——**其 ⑤「migrations 增至 0011」句已假，收口须同题扩版取代**）；Phase 6 三份计划: `PLAN-OPI-a68fd9eb-…42`（p6-0/p6-1/p6-2，**全毕**）/`PLAN-OPI-5a0be06d-…13`（p6-3，**已毕**）；背靠 `DEC-OPI-4d516e4f-9575-4cbf-8d08-6bf19a95bd25.32`（**外评采纳：表述收窄 + 三重生产硬门 +「禁用 _seed()」**——仍生效）→ ④`git log --oneline` → ⑤按下一动读文档（**下一动 = ①相位收口 → ②门二（BF-05 删除与墓碑）独立一刀 → ③Phase 7 开工第 0 步**）：**收口**只需 `val-…44` 原文与四刀读数（本文件「当前状态」+ 工程事实已录全）；**门二**读 `behavioral_baselines/security/SECURITY_PRIVACY_DELETION_CONTRACT_v1.0.md` §20/§21 + IP §16 DoD #25 与 `DEC-OPI-4d516e4f-…32` 门二段；**Phase 7** 读 `docs/DOMAIN_MODEL.md` §10/§10.1/§11/§12/§13、`docs/DATA_MODEL.md` §14/§24.14、`docs/IMPLEMENTATION_PLAN.md` §8 与 `behavioral_baselines/planner/*`（BF-02 §5 的 `feature_assembly_status`/`snapshot_status`/missing_authorities 与 DEFERRED 路径）。派单协议同下：四查+VAL+TASK 同铸；**四查必答：列名/词表/判据照 canonical 原文逐字；本刀若新增迁移，stamp 钉搬迁必须预先写明**（p6-1 16 处 / p6-3 22 处的判决性实验先例；自 p6-3 起 head/stamp 值集中在 `tests/conftest.py` 常量，边际成本已降）；读数口径按 `DEC-…a68fd9eb.32`；内容补课未落地前不得假设任何 target readiness ≥ R2；禁 `_seed()`/fixture 供给；**VAL 一律带实际触及路径集**）
- **执行者遗留处理先例**（已发生三次：P3-1A/P3-2/P4-4 前均因 API 余额中断留下半成品）：开工前必查 git status——不干净时先取证（快照/sha256/差异分析），按「吸收+逐文件复核+修正+补齐+留痕」或「拒收重交」二选一，绝不静默覆盖；**P4-4 先例**：遗留半成品 3 条失败被判测试侧，评审以 as-found 原件复跑逐条复核成立
- **当前状态**（2026-09-22 第十二会话末；rev 114）：**当前位次 = Phase 6（Scheduler + Goal Portfolio）——工程四刀全毕，相位收口待做**——**p6-0/p6-1/p6-2/p6-3 均 COMPLETE**（见下三块与 p6-3 块）；**下一动 = ①Phase 6 相位收口**（按 `DEC-…6259f6fd.…8` R18 / `DEC-…5a0be06d.…14`：须以**同题扩版相位 VAL 取代** `VAL-OPI-a68fd9eb-…44`——其 ⑤「migrations 增至 0011」句已假（现为 0013/v13）——并在最终态盖章后重签）；**②其后 = 门二（BF-05 删除与墓碑）独立一刀**（位次 = Phase 6 收口后、Phase 7 开工前，`DEC-…6259f6fd.…8` R14）；**③再后 = Phase 7（Planner shadow mode + ActiveLearningFrontier）开工第 0 步**。**p6-3 已 COMPLETE**（`PLAN-OPI-5a0be06d-…13` 的 `p6-3-planner-constraint`）：交付 `ec825b0` + 处置修 `4a941a1`；切片验收 `VAL-OPI-5a0be06d-…18` 盖章 **`VR-OPI-5a0be06d-…22` PASS**；**独立评审（新上下文 executor-pro 只读）：ACCEPT-WITH-FINDINGS，A–H 全 PASS、无 HIGH**——自写解析器 6 项逐字复核、12 条迁移摘要双算全等、自建探针在真 app.db `FAILS: 0`、**7 组变异全断言式 RED**（并**再现**了执行者自报「首轮漏网」的半腿组：单形状逃逸、双形状捕获 ⇒ 扩例是承重的）；**L-1（scope 封闭性表述过强）/L-2（写读面不对称未说明）/INFO-1（引文范围）/INFO-2（后继钉写法）四条已修**（`4a941a1`：0013 头注释把 Types「钉住的列表」与 Scope「**例如**⇒封闭是本刀 schema 选择」拆开陈述 + 2 条反向钉；写面 docstring 写明「读出时校验 + 恢复路径 = flag 面」；引文 586–607→586–615；后继钉改字面量）、**L-3（预算带）留痕并裁定**（实测 +896/+753/+285 三口径 ⇒ 预算数字是估算、不返工，但今后任务书写预算带**须写明计数口径**）、INFO-3–INFO-7 留痕（含 **DEFER**：预探针在 fence 前，无写入故无正确性影响，归下次触碰 `user_config/store.py` 的刀）。**交付面**：`migrations/0013_planner_constraint.sql`（§9 九列逐字 + 四类型/三 scope 落 CHECK + FK + stamp 13，只 DDL）+ `elc/user_config`（两 StrEnum + `PlannerConstraint` + 5 面：`record_planner_constraint`/`set_planner_constraint_active`/`get_planner_constraint`/`active_constraints`/`active_constraints_for_target`）+ registry 条目 + **迁移代号钉 22 处 1:1 化入 `tests/conftest.py` 共享常量**（+1 自证钉）。读数（修后，总控亲跑）**1824/0/0**（非 phase6 = **1158** = 1157 + 1 条自证钉；phase6 = 666）、ruff clean、mypy **119**、compileall clean、迁移 head **0013_planner_constraint（v13）**、冻结面零改动；design_sync 已 SYNCED（111 对象）。**本刀教训（总控起草缺陷，已在 `DEC-…5a0be06d.…23` 承认）**：任务书 §6 把总数 1638 误写成「非 phase6 收集数」（正确基线 1157）⇒ **任务书里的数字面也要自洽**（与 p6-0 F-1 同族）。**p6-0 已 COMPLETE**（三对象按 canonical §5.1 落地 + migration 0011/v11；盖 `VR-OPI-a68fd9eb.…52` PASS；处置 `DEC-OPI-a68fd9eb.…57`）；**p6-1 已 COMPLETE**（`PLAN-…42` 的 `p6-1-scheduler-durable-core`）：交付 `0c19e36`+`ff58611`（§5.2 `ScheduleItem`/`ReviewEvent` 逐字 + migration **0012**/v12 + store/controller/接线 + F-4/F-7 兑现 + tests/phase6 净 142 例）+ 评审处置修 `491f691`（+13 例）；切片验收 `VAL-OPI-6259f6fd-…10` 盖章 **`VR-OPI-6259f6fd-…14` PASS**；**独立评审（新上下文 executor-pro 只读）：ACCEPT-WITH-FINDINGS，A–H 全 PASS、无 HIGH**——12 组变异全被测试捕获（均断言式 RED）、自建探针在真 app.db 33 条（32 PASS + 1 FAIL=F-1）、三层逐字比对（doc 块 ↔ `dataclasses.fields` 顺序 ↔ `PRAGMA table_info`）、collation 独立实验（Python max == SQLite `BINARY`，`NOCASE` 会给出相反结果）；**1 MEDIUM 已修**（F-1 `review_event` 静默 `created_at` 使同 id 同内容重试被判 `CONFLICT`——照 `ScheduleItem.updated_at` 对称做法改为「未声明时间戳在重放时是通配、已声明须逐字相符」，总控亲验修前反例复现/修后转绿且反向仍拒）+ **2 LOW 已修**（F-2 四外键统一前置探针→`NOT_FOUND` 且 DomainError 消息零裸 sqlite 文本；F-3 F-7 字节序边界入 docstring + 显式钉）+ **7 INFO 留痕**（F-4..F-10，含 ScheduleView 两桶改名 `due_*targets`→`due_*items`、第三处测试改名、R3 残留扫描范围声明、BF-05 登记载体、F-6 大小写扫描 DEFER、F-9 推送后 CI、F-10 typing-only 挡枚举）。**处置与全量留痕见 `DEC-OPI-6259f6fd-…16`**。读数（**p6-1 收口时**，总控亲跑）**1382/0/0**（非 phase6 = **1157 逐值不变**；phase6 = 225 = p6-0 的 70 + p6-1 的 155）、ruff clean、mypy **118** 文件 0 error、compileall clean、迁移 head **0012_schedule_review（v12）**、冻结面零改动；design_sync REBUILD+CHECK 已 SYNCED（102 对象）。**p6-2 已 COMPLETE**（`PLAN-…42` 的 `p6-2-due-and-schedule-view`）：交付 `c4a6415` + 处置修 `63c8064`；切片验收 `VAL-OPI-5a0be06d-…4` 盖章 **`VR-OPI-5a0be06d-…8` PASS**；**独立评审（新上下文 executor-pro 只读）：ACCEPT-WITH-FINDINGS，A–H 全 PASS、无 HIGH**——**5 组变异全被断言式 RED 捕获**（四步派生 / 内容寻址版本 / 视图桶 / 零时钟 / 共用尺子）、**8 个自建探针在真 app.db 全绿**、**真教学尝试链上产出真 freshness anchor**（证伪了执行者回执的过宽表述）、断钉面逐行审「无删函数、无断言放松」（仅 2 处真值翻转且带补偿断言）；**F-1/F-3/F-4 三条 LOW 已修**（桶序公开面写「stored spelling」+ 混合 offset 钉 / phase2 测试 1:1 改名 / `spacing.py` 节号 `§11`→`§13` + 短语钉）、**F-2 LOW DEFER**（严格解码抛裸异常的触发条件 = 下次触碰 `scheduler/store.py` 的刀或 Phase 7 DEGRADED 装配；总控亲验：同一行脏数据在两个读面抛**两种不同**裸异常 `AttributeError` vs `ValueError`）、F-5–F-10 INFO 逐条留痕（含 `0012` 头部 `event_type` 词表的**过期预言**、`is_review_due` 含 OVERDUE **已追认**、anchor 为 target 级 / 行为模态键级）。**交付面**：`src/elc/scheduler/spacing.py`（**纯策略**：四步派生 + 内容寻址 version + BF-02 四锚 urgency，零 SQL 零时钟）+ controller 三法实装（`recompute_schedule_item` / `get_schedule_view(as_of)` / `is_review_due`）与 **P6-2 指针删除** + `ScheduleView` 加 `as_of` + Learning query face 1:1 增量 `get_learning_watermark()`（`FreshnessView` 字段集不变）+ `store.list_schedule_items()`（零解释读）；**零迁移**。读数（修后，总控亲跑）**1638/0/0**（非 phase6 = **1157 逐值不变**；phase6 = 481 = 70 + 155 + 252 + 断钉净 +4）、ruff clean、mypy **119** 文件 0 error、compileall clean、迁移 head **0012_schedule_review（v12）**、冻结面零改动；design_sync REBUILD+CHECK 已 SYNCED（106 对象）。**下一动 = ①p6-3（`PlannerConstraint` / suppression scope —— IP §7 明列的 Phase 6 末项，按 `DEC-…5a0be06d.…2` R11 归 user_config；**按红线 31③ 先 `plan_build` 铸其计划工作项再铸任务书**）→ ②Phase 6 相位收口**（须以**同题扩版 VAL 取代 `VAL-…a68fd9eb.…44` ⑤「migrations 增至 0011」句**并重签；R18/`DEC-…9` 已预告）。**流程偏离留痕**：p6-2 实际按「复核 → 评审 → 处置修 → 盖章」走（书序是「复核 → 盖章 → 评审 → 处置」），已在 `DEC-…9` 自报，下刀回到书序。Phase 5 已工程 + 语义双 COMPLETE（以下为其记录）。**PHASE 5 COMPLETE（工程 + 语义均已重签）**——P5-R（Semantic Repair）已毕：交付 `f635ea2` + 处置修 `8fa1eba`（F-1 声明精度）/`7039889`（F-2 README 收窄）；切片验收 `VAL-OPI-a68fd9eb-…24` 盖章 **`VR-OPI-a68fd9eb-…29` PASS**；**相位语义验收重签** `VAL-OPI-a68fd9eb-…34` 盖章 **`VR-OPI-a68fd9eb-…36` PASS**（取代 `VAL-…4d516e4f.34` 中被证伪的②③表述）；处置与取代声明见 `DEC-OPI-a68fd9eb.…32`。读数 **1157/0/0**（761 既有零放松 + 396 phase5）、ruff clean、mypy 117、compileall clean、migrations 0010/v10 零新增、冻结面零改动。**P5-R 语义总结（引用读数前必读）**：①9 条 seed REALIZES 全 `CURRICULUM_MAPPED`（未经课程审核）且 **capability credit 仅在 `editorial_status=CANONICAL_APPROVED` 时成立** ⇒ 语料下教学侧 ALTERNATIVE_SUCCESS **只产 RESOURCE NEUTRAL 单条诚实 claim**；②静默证据两层化——**仅 RESOURCE + 整句 form 类命中**才铸证据，CAPABILITY/slot/嵌入/引用/转述/元语言/否定一律 **observation-only（零 claim 零 state）**；`evaluator_confidence=0.60`（BF-01 §10 门 0.50 之上、strong retrieval 门 0.70 之下）；③readiness 按 canonical 原文 **fail-closed**：R0 三项（含 assessment_membership）、R1 四 facet（pos/sense/basic_definition/forms），**语料 14/14 报 `level=None`**。**独立评审（executor-pro，只读）结论**：ACCEPT，A–H 全 PASS 无 HIGH；4 组变异全被测试捕获（14/13/15/4 FAILED）；自建 12 句 shipped 路径探针逐句符合准入门；基线 1088 已由评审用 `git archive` 纯净副本独立复现。**下一步 = Phase 6（Scheduler + Goal Portfolio）——开工第 0 步已完成**：**开工 DEC `DEC-OPI-a68fd9eb-…40`**（四条前沿裁决：ReviewState 词表取 canonical 四词 / §5.1 三件完整替换 / ScheduleItem·ReviewEvent 按 §5.2 新建 / 无 review 状态机⇒转移语义作**派生判据**入册；范围 p6-0 Goal·Policy·Focus、p6-1 Scheduler durable core、p6-2 due·ScheduleView；不变量含 freshness≠due、模态键、append-first、local-only、TEXT_PRODUCTION 不产 voice/audio debt）+ **母计划 `PLAN-OPI-a68fd9eb.…42`**（p6-0 MAY_PARALLEL p6-1，p6-1 MUST_PRECEDE p6-2）+ **相位验收 `VAL-OPI-a68fd9eb.…44`**（六组，路径集 `{src/elc, migrations, tests}`）已铸；**p6-0 已 COMPLETE**（按上方 `PLAN-…42` 的 p6-0 工作项）：交付 `e55a4a6`（三对象按 §5.1 完整替换 + migration 0011/v11 + 接线 + tests/phase6 70 例）+ 处置修 `da04ec2`（评审 F-2/F-3，仅 tests/phase6）；切片盖章 **`VR-OPI-a68fd9eb.…52` PASS**（首提即过）；处置 `DEC-OPI-a68fd9eb.…57`——**独立评审 A–H 全 PASS 无 HIGH**，4 组变异全被测试捕获，自建探针在真 app.db 复现版本纪律/拒绝路径/幂等/append-first/epoch fence；**F-1 是总控任务书缺陷并已追认**（14 处既有测试修改 = 12 迁移 stamp 钉 + 2 毕业钉，1:1 机械搬迁；教训：今后每刀涉及新迁移时，stamp 钉搬迁必须在该刀任务书里**预先写明**）。**下一刀四查必答（p6-1 开工前立）**：F-4（`effective_from` 的 `""` 哨兵 vs `str | None`，接线读书面前须裁定）+ F-7（SessionFocus 无 per-conversation 读面，接 Planner/Persona 视图的刀必须补增量读方法）+ SessionFocus **append-first 属派生判据**（§5.1 无版本列）——**三项均已由 p6-1 兑现**（F-4 登记 + F-7 三处读面 + 字节序判据）。开工取证：`learning/queries.py:36 get_freshness`（`last_strong_retrieval_at`/`stability_band`）是 Scheduler 的真读输入（非夹具）——**p6-2 的 due 决策读面即此**（p6-1 未消费它，属 p6-2）。**历史记录（其语义断言已被 `DEC-…a68fd9eb.32` 取代）**：**Phase 5 工程收口**（原）——**PHASE 5 = 工程 COMPLETE / 语义验收曾撤回、已由 P5-R 重签**（外评采纳 `DEC-OPI-a68fd9eb.…15`，三个 semantic blocker 经总控逐条取证成立：①**curriculum linkage 批准无据**——links.json 9/9 条 CANONICAL_APPROVED+primary_flag 而 rationale 自述依据是 P3 测试 fixture，且 credit 路径 `content/store.py:343-355` 不筛 editorial_status、capability 实体本身无功能定义（只有单一 exemplar+slots）⇒ REALIZES 在源头不可证伪；②**silent evidence 把「命中确定性」当「表现确定性」**——`evaluator_confidence=1.0` + resolver 接受嵌入跨度（引用/转述/元语言/否定可铸 SPONTANEOUS SUCCESS 四维强证据），canonical 依据 PRODUCT_CONTRACT §2.5「用户复述刚显示的完整答案」≠会了、BF-01 §10 evaluator_confidence 是判据置信；③**R0–R4 误报**——R1 要求 POS/sense/basic definition/forms（语料全无）、R0 第三项 assessment_membership 被实现列为 DECLARED_ABSENT，严格三件读法下连 R0 亦不满足）。**（历史记录）P5-R 四工作项（已全兑现）**：四工作项 = curriculum truth（降级未审 link + credit 加 CANONICAL_APPROVED 门 + 反例回归）/ silent evidence 两层化（匹配观察 vs 证据 claim；整句/准整句、非嵌入、非引用元语言才铸强证据；capability 不得凭 lexical token 铸）/ readiness 严格化（∧/∨ 读法显式裁决；补数据或走 `DECISION_REGISTER.md` 版本条目修订 canonical——**后者属用户裁决**）/ 回归（761 既有与 behavioral_baselines 零放松；tests/phase5 相位自建断言可按语义修正但须逐条留痕）。**（历史）Phase 6 grilling 曾可并行、implementation 曾 HOLD 至 P5-R PASS——该 HOLD 现已解除**。以下 Phase 5 段为工程记录（**语义以本段开头的新总结为准**）——**三刀链**：P5-0 `f912948`（`TASK-…4d516e4f.38` / 切片验收 `VAL-…40` 盖 `VR-…42` PASS / 处置 `DEC-…43`）+ P5-1 `0b0d072`（`TASK-…46` / `VAL-…48` 盖 `VR-…50` PASS / 处置 `DEC-…51`）+ p5-2 `d1ccbd2`（`TASK-…55` / `VAL-…57` 盖 `VR-…59` PASS / 处置 `DEC-…60`）；**相位验收** `VAL-…4d516e4f.34` 盖章 **`VR-OPI-a68fd9eb-…9` PASS 零告警**（六组：只读 content.db+registry loader / R0–R4+两视图+production provider 接真链路 / origin separation+candidate exclusion / 自然对话→target resolution→target-specific Performance Evidence→Estimator→LearnerState E2E / 既有 761 零放松+四门绿+0 skipped / §6 未验数字零转述）；收口 `DEC-OPI-a68fd9eb.11`。读数 **1088/0/0**（761 既有零放松 + 327 新：P5-0 138 / P5-1 83 / p5-2 106）、ruff clean、mypy 116、compileall clean、migrations 0010/v10 零新增、冻结面零改动。**Phase 5 交付面**：`content_src/`（14 实体=9 RESOURCE+5 CAPABILITY，§24.1 七列逐字，迁移自 P3 fixture 且原件零改动）+ `curriculum/`（5 capability / 9 CurriculumLink / prerequisites 空集）+ `elc/content/{build,store,queries,types}.py`（确定性构建：本会话两路独立构建 sha256 同值 `2c617a95…bde96`，与 P5-0 记录逐位相等；只读连接写全拒；unknown ID 全 NOT_FOUND）+ `elc/curriculum/{readiness,provider,store}.py`（R0–R4 三键集 + production `ContentBackedTeachingTargetProvider` + §24.11 origin/candidate exclusion）+ `elc/learning/{target_resolution,silent_claim,silent_evidence}.py`（抽取式 resolver + 静默证据链）；**Phase 3 遗留的 fixture 目标供给自此被真链路替换**（E2E 零 `_seed()`/零 fixture）。**机制事实留痕（本轮新增，勿再踩）**：①`validation_submit` 的 verdict 由 METRIC_COMPARATOR 按 **metric 名在 measurements 中命中**决定——未命中即 `INCONCLUSIVE`（不是 FAIL）；`==` 为严格相等（阈值 1 ⇒ 须提交数值 `1`，布尔 `true` 判不等）；`expected` 缺 `repository_basis_refs` 键报 `EVIDENCE_BASIS_REFS_ABSENT`（显式空数组亦静默）；②Write/Edit 按目标文件既有行尾复写（改行尾须「无信号 Write 两步法」）；③大文件禁单次全量 Write 复写（`runtime/controller.py` 含历史 mojibake 字节）。**Phase 4 段（历史记录，收口见 `DEC-…4d516e4f.29`）**：**PHASE 4 COMPLETE**（rev 76；sync 见收口记录）——五刀链 P4-0 `d8866b8`（`VR-…5ba74efc.91`）+P4-1 `2acaaca`（`VR-…109`）+P4-2 `ffcafcf`（`VR-…4d516e4f.12`）+P4-3 `6ef5ca1`（`VR-…21`）+P4-4 本刀（`VR-…4d516e4f.28` PASS，处置与收口 `DEC-OPI-4d516e4f-…29`）；五验收全 PASS（VAL-…70/72/80 直盖；VAL-…74/76 路径集过窄 → 同题扩集 VAL-…4d516e4f.7/.17 代盖并留痕取代）。**Phase 4 交付面**：Relationship durable core（Recorder P4-G1 门/validate 八规则/dedupe/append-first supersede/sensitive gate 四判定序/Persona×User 全隔离）+ **CP4 projection runtime**（确定性 pj- id/幂等 enqueue/五态 CAS/slice+based 双重重校验无盲重放/启动三连 reopen→ensure→drain/F-5 claim 护栏/构造期执行器完备性 fail-fast）+ **两投影类型**（RELATIONSHIP + EPISODE；episode §5.3 十列逐字、幂等版本感知重建）+ **DisclosedUserProfile**（§5.1 列集逐字+显式同意门+高敏仅 persona-specific ∧ RICH）+ **PromptCompiler 八节**（persona→profile→contract→history→relationship→episode→teaching→channel；byte-determinism；跨 persona 零泄漏双向）+ 启动恢复五步 + 全链三模式隔离。**P4-4 收官刀亮点**：压力套件抓到并修复真实缺陷 F-1（分类器读失败原被洗成「空成功」COMMITTED、烧掉确定 id 致该轮投影永久丢失 → 检出该 refusal 即 FAILED_RETRYABLE 可重试；评审两次独立反证）；E2E 四组（含 guard 释放/不重发 assistant 直证/durable 行逐字对账/两次重建编译逐字节相等）。读数 **761/0/0**、mypy 108、migrations 0010/v10、冻结面零改动。**遗留清单七项入册 `DEC-…29`**（Phase 5 接续用）。**外部评审（Phase 4 复核 + Phase 5 GO）已采纳入册 `DEC-…32`**：表述收窄=**Relationship/Episode infrastructure COMPLETE；automatic natural-language relationship extraction NOT production-wired**；三重生产硬门（均不阻塞 Phase 5、均不得因「PHASE 4 COMPLETE」被遗忘）：**门一** model proposal 的语义支撑与权威分离（受信 sensitivity authority + 真实 consent 记录 + 抽取式 evidence span/独立语义校验——现 gate 判定序已墙断「模型推断高敏」，但两字段仍是候选层声明）；**门二** BF-05 source-aware deletion + tombstone（**IP §16 DoD #25，V1 内必须排位次**；现状：src 零 delete/tombstone 实现）；**门三** untrusted durable memory → prompt 安全 framing（JSON framing/长度前缀/显式不可信分隔符 + section-spoofing 测试；触发条件=**真实 external provider 或 production candidate provider 任一启用前**，已由此句取代 DEC-…29 遗留③的「Phase 9」表述）；另 CP4 claim→domain write 的 base CAS 加固随下次触碰 CP4 带入。**Phase 5 三刀明细（历史记录）**：母计划 `PLAN-OPI-4d516e4f-…36`（p5-0 基底 / p5-1 readiness+production provider / p5-2 natural-chat 闭环，MUST_PRECEDE 链）三工作项全毕；相位验收 `VAL-OPI-4d516e4f.34` 已于本轮盖章 PASS。**P5-0 已 COMPLETE**（`TASK-…4d516e4f.38`，切片验收 `VAL-…4d516e4f.40` 盖 `VR-…4d516e4f.42` PASS，处置 `DEC-OPI-4d516e4f-…43`）：`content_src/`（14 实体迁移自 target_fixtures.py，原件与 P3 消费者零改动）+`curriculum/`（5 capability / 9 CurriculumLink）+`content/{build,store}.py`（确定性构建：四路 sha256 同值；只读连接写全拒；unknown ID 全 NOT_FOUND；§24.1 七列逐列）+`tests/phase5/` **138 用例**。评审 **A–H 全 PASS + 两轮处置**：F-1 写通道单一化并**发现机制事实**——Write/Edit 按目标文件既有行尾复写，改行尾须「无信号 Write 两步法」（**修正了此前锁定的「Edit 往返」表述**）；F-2 校验面四缺口补齐（format/format_version/实体 language 交叉校验）；F-3 §11 词表 canonical 抽取钉；F-5 措辞收窄；F-4/F-6 留痕（F-6 归 p5-1）；第 7 次 Bash 直写按先例处置。读数 **899/0/0**、mypy 110、零 migration、冻结面零改动。**p5-1 已 COMPLETE**（`TASK-…4d516e4f.46`，切片验收 `VAL-…4d516e4f.48` 盖 `VR-…4d516e4f.50` PASS，处置 `DEC-OPI-4d516e4f-…51`）：`curriculum/readiness.py`（§8.1/§24.10 五级判定，三键集；R4 要求可测 detection；**语料 14 target 全为 R1、无虚报 R4**）+`curriculum/provider.py`（production `ContentBackedTeachingTargetProvider` 接 content.db 只读面，语义照 P3 port）+`curriculum/store.py`（薄读适配+12 方法签名统一 = DEC-…43 F-6 收口）+origin/candidate exclusion（§24.11 判据）；**新 E2E 全走真供给链，零 `_seed()` 零 fixture**。读数 **982/0/0**、mypy 113。**两处留痕必读**：①**R1 读法是实现侧解释而非 canonical 裁决——严格读法（POS/sense/basic definition/forms 逐字）下本语料 14 个应是 R0**（前提「全实体皆 EXPRESSION」已由 pin 钉住）；②**全语料止步 R1 ⇒ 按 §8.1 默认表无一 target 满足 Planner 默认门槛**（内容侧补课：PedagogicalProfile/ResourceLabel/PackOverlay/explanation/example policy/contrast/TypicalError/detection fixtures）。**p5-2 已 COMPLETE**（`TASK-…4d516e4f.55`，切片验收 `VAL-…4d516e4f.57` 盖 `VR-…4d516e4f.59` PASS，处置 `DEC-OPI-4d516e4f-…60`）：确定性抽取式 resolver + 静默证据链（无 TeachingMoment/Gate/Planner）+ 供给降级不阻聊；**两条 MEDIUM 已修**——F-1 slots 假阳性面（3 个单 token 目标会让 \"I want to see that movie.\" 铸出 1.0 耐久 claim）：改「≥2 组或多 token 才走 slots」+ 把假阳性面写成 **§8.1 R4 detection policy 的第一批声明输入**（实测同构样本 5/23→1/23 且留存例为真阳性）；F-2 Moment 开启窗口错标：resolve 前查 durable 同 target moment，命中即**跳过**（fail-closed）+ 反序反例测试。读数 **1088/0/0**、mypy 116。**Phase 5 首轮收口（原；其语义断言已由 P5-R 修正并重签，见本段开头）**：`VAL-…4d516e4f.34` 曾盖章 `VR-OPI-a68fd9eb.…9`（六组「测试绿」事实仍成立，但 R0–R4 层与 linkage 批准两项语义已被证伪并修正）；内容侧补课清单九项、语义口径四项、metadata 三件、路径集判据与交接全部入册 `DEC-OPI-a68fd9eb.11`——其中「R1 是解释性读法」一条**已被外评否决、亦已被 `DEC-…a68fd9eb.32` 取代，不得再作为合规依据引用**。**🔴 新会话交接要点（Phase 6 开工前必读）**：①**续接首读对象**（全 id 可直接 design_get）：`DEC-OPI-a68fd9eb-…32`（**P5-R 处置 + PHASE 5 语义验收重签 + 取代声明**——**下一相位先读**）＋`DEC-OPI-a68fd9eb-…15`（外评采纳：三 blocker 取证 + P5-R 授权）＋`DEC-OPI-a68fd9eb-…11`（PHASE 5 工程收口：交付面/内容补课清单/metadata 三件；**其 R1 解释性读法与「语料全为 R1」两句已被 `DEC-…32` 取代**）＋`DEC-OPI-4d516e4f-…60`（p5-2 处置）＋`PLAN-OPI-4d516e4f-…36`（Phase 5 母计划，三工作项全毕）；背靠 `DEC-…4d516e4f.32`（三重硬门+禁 _seed()）与 `DEC-…4d516e4f.29`（Phase 4 收口+遗留七项）。②**下一动 = ①p6-3（`PlannerConstraint` / suppression scope，IP §7 明列的 Phase 6 末项；按 `DEC-…5a0be06d.…2` R11 归 user_config、消费方是 Planner/Gate）→ ②Phase 6 相位收口（须以同题扩版 VAL 取代 `VAL-…44` ⑤ 的「增至 0011」句并重签）**：Phase 6 开工第 0 步与 p6-0/p6-1/p6-2 **均已完成**（`DEC-…40` 四条前沿裁决 / `PLAN-…42` 三工作项**全毕** / `VAL-…44` 相位验收；p6-0 交付 `e55a4a6` + 处置修 `da04ec2`，盖章 `VR-…a68fd9eb.…52` PASS，处置 `DEC-…a68fd9eb.…57`；p6-1 交付 `0c19e36`+`ff58611` + 处置修 `491f691`，盖章 `VR-…6259f6fd.…14` PASS，前沿裁决 `DEC-…6259f6fd.…8`、处置 `DEC-…6259f6fd.…16`；p6-2 交付 `c4a6415` + 处置修 `63c8064`，盖章 `VR-…5a0be06d.…8` PASS，前沿裁决 `DEC-…5a0be06d.…2`、处置 `DEC-…5a0be06d.…9`）；四查必答含 (a) canonical 逐字达标面（不得以自建语义+绿测试背书）、(b) **`SessionBudgetView` 归属**（p6-1 R10 登记：若不在 p6-2 范围则显式留痕至 Phase 7/8）、(c) **`source_learning_watermark` 语义** + `learning/queries.py:36 get_freshness` 真端口为 due 决策输入、(d) **`event_type` 词表**（首个按值分支消费者）+ **spacing 阶梯**（`SpacingStage` revisit 条件已就位）+(e) **`ScheduleView` 产出形状**（现名 `due_items`/`overdue_items`）与 `PlannerConstraint`（DATA_MODEL §9）、(f) **BF-02 §5「missing Scheduler → schedule_urgency = 0」禁用项** + R4 的 urgency 数值图属 Phase 7 Planner 特征装配、(g) 读数口径沿用 `DEC-…a68fd9eb.…32`、内容补课未落地前不得假设 readiness ≥ R2、(h) **相位收口时须以同题扩版 VAL 取代 `VAL-…44` ⑤ 的「migrations 增至 0011」句**（p6-1 R18 预告）。③**Phase 5 交付面**：`content_src/`+`curriculum/`（14 validated target 迁移自 P3 fixture，原件零改动；content.db 由 `elc.content.build` 确定性生成、只读读面）+`elc/curriculum/{readiness,provider,store}.py`（R0–R4 判定+production TeachingTargetProvider+薄读适配）+`elc/learning/{target_resolution,silent_claim,silent_evidence}.py`（抽取式 resolver+静默证据链）+`elc/content/{store,queries,types,build}.py`；**Phase 3 遗留的 fixture 目标供给自此被真链路替换**。④**仍未闭的生产硬门**（DEC-…32，各有触发条件，不得因相位推进而遗忘）：候选 provider 的语义支撑与权威分离 / BF-05 删除与墓碑（**IP §16 DoD #25，V1 内须排位次**）/ 不可信记忆进 prompt 的 framing（触发=真实 external provider 或 production candidate provider 任一启用前）。⑤**通道纪律现状**：「Bash 使用清单」机制已捕获多次违规（最近一次 = p6-2 执行者以 Bash 在**仓内** `cp` 一个临时 `.py` 再于同命令删除——已自报、未入提交、无损留，按「同一刀第 1 次＝训诫留痕」处置并在 `DEC-…5a0be06d.…9` 立规：**仓内零写入（含临时文件）；「同一命令内创建并删除」不豁免**，诊断一律用仓外临时目录）；字节零差复写=Edit 往返仅保内容不变、**改行尾须「无信号 Write 两步法」**；大文件禁单次全量 Write 复写（`runtime/controller.py` 含历史 mojibake 字节）；⑥**Phase 6 grilling 必裁前沿（开工前实证，已逐字比对 canonical；①③④ 已由 p6-1 前沿裁决 `DEC-…6259f6fd.…8` 裁定、②已由 p6-0 `DEC-…a68fd9eb.…40` 裁定——下列保留为取证记录）**：`src/elc/scheduler/types.py:19-26` 的 ReviewState 词表（NEW/LEARNING/REVIEW/LAPSED/SUSPENDED）与 canonical `DATA_MODEL.md` §5.2 的 `review_state`（NOT_SCHEDULED/UPCOMING/DUE/OVERDUE）**disjoint**、字段名亦不一（`state` vs `review_state`）；`src/elc/user_config/types.py:193-220` 三件与 `DATA_MODEL.md` §5.1 差距（LearningGoalPortfolio 缺 5 字段 / TeachingPolicyProfile 缺 10 字段且多 1 字段 / SessionFocus 零字段吻合）；`ScheduleItem`/`ReviewEvent` 在 `src/` **零实现**、`docs/STATE_MACHINES.md` 无 review 状态机（grep "review" 0 命中）⇒ 形状与词表须按 canonical **完整替换**（不得在 Phase 0 skeleton 上小修）；派生钉：freshness ≠ review_due（IP §7 原文）、EvidenceModality 分离（canonical）、TEXT_PRODUCTION 不产 voice/audio debt（IP §16 #22 / DECISION_REGISTER:20 派生，非原文）
- **对象索引**（全 id 可直接 design_get；OPI 前缀：Phase 3 早期 `2babb21e-bc72-47a2-9382-e773c92211d2`、P3-1B 起及 Phase 4 全在 `5ba74efc-9f26-483b-a7de-c833834275a4`、Phase 5 全在 `4d516e4f-9575-4cbf-8d08-6bf19a95bd25`、**Phase 5 收口系列 `a68fd9eb-7bf5-4ed6-a74b-68a38707177b`**、**Phase 6 p6-1 全在 `6259f6fd-c446-4e34-886c-946c3343af0c`**、**Phase 6 p6-2 全在 `5a0be06d-5af8-4124-9550-88fda7b22587`**、**Phase 6 p6-2/p6-3 全在 `5a0be06d-5af8-4124-9550-88fda7b22587`**；旧系列 `eaaa5a1d-7ac7-4746-bcdf-c02bc9147492`）：
  - `DEC-OPI-5a0be06d-…23` **p6-3 处置**（评审 ACCEPT-WITH-FINDINGS 无 HIGH；L-1/L-2/INFO-1/INFO-2 修 `4a941a1`；L-3 预算留痕 + 计数口径教训；INFO-3 DEFER；流程书序修正；总控任务书笔误承认）+ `TASK-…5a0be06d.…20` / `VAL-…5a0be06d.…18` 盖 **`VR-…5a0be06d.…22` PASS** + `PLAN-…5a0be06d.…13`（p6-3 计划，已毕）——**相位收口前先读**
  - `DEC-OPI-5a0be06d-…15` **p6-3 前沿裁决 R1–R12**（所有权 user_config / 0013 / §9 九列·四类型·三 scope / active 独立转移面 / 读面窗口与目标腿 / 豁免面日志 / 22 处断钉预写 / 迁移常量收口）——**相位收口前先读**（评审 ACCEPT-WITH-FINDINGS 无 HIGH；F-1/F-3/F-4 修 `63c8064`；F-2 DEFER 带触发条件；F-5–F-10 留痕；执行者 6 项裁定 + 总控流程顺序偏离自报）+ `TASK-…5a0be06d.…6` / `VAL-…5a0be06d.…4` 盖 **`VR-…5a0be06d.…8` PASS**——**p6-3 开工先读**
  - `DEC-OPI-5a0be06d-…9` **p6-2 处置**（评审 ACCEPT-WITH-FINDINGS 无 HIGH；F-1/F-3/F-4 修 `63c8064`；**F-2 DEFER** 带触发条件 = 下次触碰 `scheduler/store.py` 的刀；F-5–F-10 留痕；执行者 6 项裁定 + 总控流程顺序偏离自报）+ `TASK-…5a0be06d.…6` / `VAL-…5a0be06d.…4` 盖 **`VR-…5a0be06d.…8` PASS** + `DEC-…5a0be06d.…2`（p6-2 前沿裁决 R1–R15，见下）
  - `DEC-OPI-5a0be06d-…2` **p6-2 前沿裁决 R1–R15**（零迁移 / due 四步派生 + 实现声明常数 / urgency 四锚取自 BF-02 JSON / `event_type` 仍无词表 / watermark 十进制串 / **内容寻址 version** / ScheduleView 加 `as_of` / `SessionBudgetView` 留 Phase 8 / **`PlannerConstraint` 排位 p6-3 归 user_config**）
  - `DEC-OPI-6259f6fd-…16` **p6-1 处置** / `DEC-OPI-6259f6fd-…8` **p6-1 前沿裁决 R1–R18**（迁移 0012 / §5.2 逐字面 / 骨架面处置 / 16 处断钉预写）——历史
  - `DEC-OPI-a68fd9eb-…11` **PHASE 5 工程收口**（三刀链+切片三验收+相位验收六组+内容补课清单九项+metadata 三件+路径集判据+三扇硬门）——**其②③语义断言（R0–R4 层、linkage 批准）已由 `DEC-…15` 撤回，语义以 `DEC-…15` 为准**；相位验收 `VAL-…4d516e4f.34` 盖章 `VR-OPI-a68fd9eb.…9` PASS 零告警（首提 `VR-…a68fd9eb.6` INCONCLUSIVE 已留痕为机制事实）
  - `DEC-OPI-a68fd9eb-…57` **p6-0 处置**（评审 PASS 无 HIGH；F-1 任务书缺陷追认 14 处测试搬迁；F-2/F-3 已修；F-4/F-5/F-6/F-7/F-8 留痕；SessionFocus append-first 为派生判据）+ 交付 `TASK-…48`/`VAL-…50` 盖 `VR-…52` PASS——**p6-1 开工先读**
  - `DEC-OPI-a68fd9eb-…40` **Phase 6 开工 + 四条前沿裁决**（ReviewState 词表=NOT_SCHEDULED/UPCOMING/DUE/OVERDUE 等）+ `PLAN-…a68fd9eb.…42`（p6-0/p6-1/p6-2）+ `VAL-…a68fd9eb.…44`（相位验收六组）——**Phase 6 开工先读**
  - `DEC-OPI-a68fd9eb-…32` **P5-R 处置 + PHASE 5 语义验收重签**（独立评审 ACCEPT 无 HIGH：4 组变异全被测试捕获、12 句 shipped 探针、基线 1088 纯净副本复现；F-1 修 `8fa1eba`、F-2 修 `7039889`、F-3/F-4 口径校正、F-5/F-7/F-8 DEFER 留痕；**显式取代 `DEC-…a68fd9eb.11` 的「R1 解释性读法」句与「语料 14 target 全为 R1」句**，亦取代 `DEC-…4d516e4f.…51` 的解释性读法条款）
  - P5-R 三件链：`PLAN-…a68fd9eb.…19` / `TASK-…a68fd9eb.…22` / `VAL-…a68fd9eb.…24` 盖 `VR-…a68fd9eb.…29` PASS（切片）；**相位语义验收重签** `VAL-…a68fd9eb.…34` 盖 `VR-…a68fd9eb.…36` PASS
  - `DEC-OPI-a68fd9eb-…15` **外部评审采纳（Phase 5 复核 + P5-R 授权）**（三 blocker 取证 + P5-R 四工作项 + 过程教训：评审缺「实现 vs canonical 原文」逐字达标面）——历史
  - P5 三对交付：`TASK-…4d516e4f.38/.46/.55` + 切片盖章 `VR-…42/50/59`（全 PASS）+ 处置 `DEC-…43/51/60`；`PLAN-…4d516e4f.36` Phase 5 母计划（**全毕**）
  - `DEC-…4d516e4f.29` **P4-4 处置 + PHASE 4 COMPLETE 收口**（五刀链+五验收+**遗留清单七项**：derive_reply 启发式/prompt 转义归 Phase 9/Episode status/双证口径/history 期望/零候选边界/服务端 warning）——Phase 4 收口，未完项挂账
  - `DEC-…4d516e4f.22` P4-3 处置（其六项已由 P4-4 全兑现或转留痕）；`DEC-…4d516e4f.13` P4-2 处置；`DEC-…5ba74efc.115` P4-1 处置；`DEC-…5ba74efc.96` P4-0 处置；`DEC-…5ba74efc.68` **外部评审采纳（Phase 3 复核+P4-0 授权）**
  - `PLAN-…5ba74efc.82` Phase 4 母计划（p4-0..p4-4，**全毕**）；`VAL-…5ba74efc.70/.72/.80` 直盖 PASS；`VAL-…74/76` 路径集过窄 → 同题扩集 `VAL-…4d516e4f.7/.17` 代盖（取代关系见 `DEC-…4d516e4f.13/.22`）
  - P4 五对交付：`TASK-…5ba74efc.84/.100`+`VR-…91/.109`（P4-0/P4-1）；`TASK-…4d516e4f.9`+`VR-…12`（P4-2）；`TASK-…4d516e4f.19`+`VR-…21`（P4-3）；`TASK-…4d516e4f.26`+`VR-…28`（P4-4）
  - `DEC-…5ba74efc.65` **PHASE 3 COMPLETE 收口**（五刀链+遗留清单七项）
  - `DEC-…2babb21e.5` P3-1 开工基线；`DEC-…5ba74efc.43/.20`+`DEC-…2babb21e.34` P3-2/P3-1B/P3-1A 评审处置
  - `TASK-…5ba74efc.2/.24/.47`+`VR-…5ba74efc.13/.38/.57` P3-1B/P3-2/P3-3 任务书+盖章（PASS）
  - `VAL-…2babb21e.7/.9/.11/.13` P3 四验收（全 PASS）；`PLAN-…2babb21e.15` Phase 3 母计划；`TASK-…2babb21e.17`+`VR-…2babb21e.25` P3-1A 两件
  - `DEC-…eaaa5a1d.58` P3-0 评审处置；`DEC-…eaaa5a1d.51` 外部评审采纳（Phase 3 GO）；`DEC-…eaaa5a1d.48` PHASE 2 COMPLETE；`DEC-…eaaa5a1d.33` 拒收政策升格；`DEC-…eaaa5a1d.26` Phase 2 开工
  - `PLAN-…eaaa5a1d.19` Phase 2 PLAN；`VAL-…eaaa5a1d.17` 相位门；P2A/P2B 各系列 id 见旧交接（`git log` 可溯）
  - `DEC-…eaaa5a1d.2` 外部评审采纳（权威漂移）；`DEC-…d7937fd7.19`/`DEC-…d7937fd7.12` Phase 1 评审/COMPLETE
  - `DEC-…9dc4e77f.4` **契约载体**（跨会话重建先读它）；`DEC-…091f35c3.7/.13`；`DEC-…d5b616bf.3/.7`；`DEC-…7bf80972.2` PHASE 0

## dmcp 工作流协议（v2，2026-09-20 用户纠正后立——总控必守）

每刀（task）完整链：**四查+VAL+TASK 同铸 → 派执行者 → 总控独立复核（pytest/ruff/mypy/CI 亲跑）→ 独立评审（新上下文 executor-pro 只读评审，产 findings）→ 处置（修/DEFER/留痕）→ validation_submit 盖章（在最终验证过的提交上）→ design_sync REBUILD+CHECK 收口**。缺评审即违约（本仓 2026-09-20 已犯一次，用户点名纠正）。**顺序修正（2026-09-22 p6-2/p6-3 实测）**：原书写「复核 → 盖章 → 评审 → 处置」在实践中不可执行——VR 必须盖在**最终验证过的提交**上，而「处置」按定义会再改一次代码（p6-1/p6-2/p6-3 的 VR 实际都盖在处置修后的提交上）⇒ 自本行起以「处置 → 盖章」为书序（盖章仍是验收要件，只是位次挪到最终态）；因此「盖章后不得再改该切片代码」成为新硬约束。**迁移刀的成本（p6-1/p6-3 实测）**：新增一个迁移会使**所有** head/stamp/applied 钉变假（0012 断 16 个测试、0013 断 22 个），任务书必须**逐条预写**搬迁名单；自 p6-3 起这些值集中在 `tests/conftest.py` 的 `MIGRATION_IDS`/`SCHEMA_HEAD_FILE`/`SCHEMA_HEAD_VERSION`（改一处 + 该刀自己的迁移测试），并有一条自证钉校验常量 == 真实仓库。

- **纪律 ①（⟳ 刷基）**：连续两次 dmcp 变更之间必须 design_status 纯读刷新基再填 expected——REVISION_CONFLICT 即忘了刷基（错误面会给 current id 可直接重构）
- **Phase 开工第 0 步**：`plan_build` 铸 PLAN（VERTICAL_SLICE/RISK_SPIKE/VALIDATION_EXPERIMENT/ENABLER 工作项+relations）；任务从 plan 工作项展开。**readiness 更正（2026-09-20 第四会话查实）**：design_readiness 的 gate 行来自 gate policy 而非 plan_build——默认策略是 FINDING-only 单族行集（每条 finding 一行 BLOCKING），finding 册恒空 ⇒ 恒 READINESS_BASIS_INCOMPLETE；解锁要么服务端接线 preparer 让 findings 真实入册，要么宿主侧配 `DMCP_READINESS_POLICY_FILE`（宿主操作，不代执行）。**阶段门禁当前由本仓工作流承担**（VAL+独立评审+decision），readiness 仅作解锁后的复核面
- **评审面构建局限（当前服务端，已铁证）**：REVIEW_REQUIREMENT/REVIEW_PACKET 只能由宿主侧 preparer 铸，而 dist 无任何入口接线 `createHostReviewBasisPreparer`（D:\MCP apps/mcp-server，查实 0 wiring）→ review_submit 恒 OBJECT_NOT_FOUND、finding 册因构造恒空（finding_list=0 是构建事实不是「无问题」）。**降级路径**：评审照做（独立上下文评审员），findings 全文+证据+裁定入 decision（范式见 `DEC-…d7937fd7.19`/`DEC-…eaaa5a1d.9`），服务端升级接线后补录正式 finding 并迁移裁定
- **主体/能力（当前服务端 env）**：HUMAN_USER + DESIGN_STATE_READ/MUTATE 两族——FINDING_ADJUDICATION/DISPOSITION 族未授，裁决面即使有 finding 也会被诚实拒绝；升级服务端配置属宿主侧操作，skill 不代执行
- 契约=跨会话记忆：GOAL/RUNTIME/IN_SCOPE/OUT_OF_SCOPE/TECHNICAL_CONSTRAINTS/ASSUMPTIONS 在载体 `DEC-…9dc4e77f.4`（含六 canonical 文件 digest 绑定，漂移即假设失效；AGENTS.md 活文档不绑）；契约改写是整体替换（⑲），改前先 design_get 读回

## 派单协议（用户明令，2026-09-20）

- **执行者 = `executor-flash`（一般任务）/ `executor-pro`（难题）**——用户安装在 `~/.zcode/agents/`（model 钉 GLM-5.3-Flash / GLM-5.3，**须带 `thoughtLevel: max` 键**——2026-09-20 补，缺则新 harness 拒启，见 `DEC-…091f35c3.13`）。**禁止用内置 `general-purpose` 做执行任务**。
- 总控不亲自执行（写代码/测试/探针/部署一律派执行者，先 `task_build` 铸任务书）；设计类任务书先过 grilling 前沿；**任务书四查 + VAL 与任务书同铸**（前沿/实现设计条目/计划时效/预算算术；交付后补盖 VAL 视同无效验收）。红线全文 0–31 见旧仓 `D:\测试1\AGENTS.md`（红线 28/29/30/31 尤其要读）。

## 工程事实与红线（本仓特有）

- **远程**：github.com/ZJW11398925/english-learning-clean（public）；**CI 四门**：ruff → mypy → compileall → pytest（push+PR，Ubuntu，0 skipped 为准）
- **测试**：`python -m pytest`（当前 **1824/0/0**，0 skipped；非 phase6 = **1158**（= 1157 + 1 条迁移血缘自证钉））；静态：ruff + mypy（**119** 文件 0 error）；migrations 到 **0013**（schema_version **13**；0011 = p6-0 三表、0012 = p6-1 的 `schedule_item`/`review_event`、0013 = p6-3 的 `planner_constraint`；**p6-2 零迁移**；head/stamp 值集中在 `tests/conftest.py` 的 `MIGRATION_IDS`/`SCHEMA_HEAD_FILE`/`SCHEMA_HEAD_VERSION`）；GenerationAction 权威在 runtime（DM §16）；DecisionCycle 权威在 Runtime（`DEC-…2babb21e.5`）；TeachingMoment/Attempt 全链已实装（P3-1/2/3）；**Phase 4 全毕（P4-0..P4-4）**：Relationship durable core（P4-1）+CP4 projection runtime（P4-2，`elc.runtime.projections` SQL-free + `elc.platform.db.projection_store`）+Episode 投影与 DisclosedUserProfile 与 PromptCompiler 八节（P4-3）+E2E/压力验收（P4-4：`tests/phase4/test_p4_4_{full_chain,stress}.py`；F-1 缺陷修复=分类器读失败不再被洗成空成功）；**Phase 5 工程全毕（P5-0/P5-1/p5-2；语义验收待 P5-R 重签）**：`content_src/`+`curriculum/` 资产（14 validated target，P3 fixture 原件零改动）+ `elc/content`（确定性构建 content.db 2c617a95…bde96 / 只读读面写全拒）+ `elc/curriculum`（readiness R0–R4 + production `ContentBackedTeachingTargetProvider`）+ `elc/learning`（抽取式 target resolution + silent evidence 链，无 TeachingMoment/Gate/Planner）；`tests/phase5/` 327 新用例，全零 `_seed()` 真链路；**Phase 6 四刀全毕（p6-0/p6-1/p6-2/p6-3，相位收口待做）**：`elc/user_config`（§5.1 Goal/Policy/Focus + 版本/append-first 纪律 + keying convention；§9 `PlannerConstraint` + 两 StrEnum + 5 面）与 `elc/scheduler`（§5.2 `ScheduleItem`/`ReviewEvent` 逐字 + `SqliteSchedulerStore` 当前投影/append-first/CONFLICT/epoch fence + `spacing.py` 纯策略 due 四步派生 + `recompute_schedule_item`/`get_schedule_view(as_of)`/`is_review_due` 三法 + 内容寻址 version）+ registry 换代；**下一步 = 相位收口（同题扩版 VAL 取代 `VAL-…44`）**
- **规范优先级执行**（P4-0 教训留痕）：canonical 与既有实现漂移时**实现服从 canonical**（列名/词表逐字照原文；无决策裁定不得以「Phase 0 拼写」为由倒置）；本仓已有两例（0007 列集、P4-0 relationship 列名/枚举）——任务书四查加「列名/词表照 canonical 原文」必答项
- **门是意图不是障碍**（P4-2 教训留痕）：实现**不得为过架构门而变形**——P4-2 首轮执行者把 `ProjectionExecutor.execute` 以「取方法值再调用」（`run = executor.execute; run(view)`）绕开 Gate 2 的 `.execute(` 调用形态扫描，总控判返工改**正向命名** `project()`（Gate 2 把 `.execute()/.commit()/.rollback()` 词表留给 runtime 不得触碰的 DB 机件；改名后结构性护栏不变、独立 AST 扫描 0 违规）。同轮总控把执行者自报的边界「RUNNING 残骸本刀不回收」升格为本刀内做（RA §22 明文把 pending projections 列入恢复扫描）——**边界自报是诚实的，但「RA 明文要求」级缺口不因自报而免做**
- **VAL 路径集纪律**（P4-2 教训留痕）：验收的 `applicability_contract.paths` 必须覆盖实际触及路径——VAL-…74 漏 `src/elc/platform/db`（Gate 2 规定 runtime 无 SQL，Runtime 拥有的投影适配器只能落此）与 `src/elc/teaching`（对账需 refs 扫描面），P4-2 铸同题扩集 `VAL-OPI-4d516e4f.…7` 盖章并在 `DEC-…4d516e4f.13` 显式留痕取代关系（**不双重声明同一观察**）；P4-3 同例（VAL-…76 漏 runtime/migrations → `VAL-…4d516e4f.17`）
- **落盘通道三段纪律**（P4-3 教训留痕，第六次违规后升级；**p6-2 追加第四段**）：①一切 `.py/.sql/.yml/.toml` **一律 Write/Edit 工具落盘**（Bash heredoc/`sed -i`/重定向/`ruff --fix` 等一律违规——`--fix` 属同族写操作）；②回执必附**「Bash 使用清单」**（逐条命令意图，非只读用途标红；同一刀内第二次违规=整刀拒收重交）；③**字节零差复写一律用 Edit 往返 + 四重验证**（H1==H2、`--no-filters` raw 相等、字节数/行数相等、分块 sha256 全等），**禁止**对超大文件（本仓 `runtime/controller.py` 195KB）做单次全量 Write 复写——该文件含历史 mojibake 字节（~3637 行），Read 显示与真实字节不一致，人工转录必带显示伪影；④**仓内零写入（含临时/诊断文件）**——「同一条命令内创建并随后删除」**不构成豁免**（红线约束的是钩子与审计面而非持久性，见 `DEC-…5a0be06d.…9`）；需要诊断脚本一律落仓外临时目录
- **冷启动 import 闭环**（P4-3 教训留痕）：`elc.runtime.__init__` 聚合导出 + 域模块互相引用可造出「域模块 → runtime 包 → 新模块 → 域模块」的闭环，测试套件因导入顺序侥幸不报而 `import elc.conversation` 冷启动失败；解法＝本地窄 Protocol + `TYPE_CHECKING` 注解导入 + **subprocess 冷启动钉**（逐包 import）——凡新增跨包引用面必带冷启动钉
- **Mimosa 安全钩子**：Bash 直写源码/配置被拦——**.py/.sql/.yml/.toml 一律 Write/Edit 工具落盘**（P1B 执行者曾一次 Bash 直写 .py 未被钩子拦但违反明令，已在 `DEC-…d7937fd7.12` 训诫留痕——总控与执行者均不得再犯）；SQL 一律参数绑定或全字面量（标识符用白名单全字面量），禁任何拼接
- **基线冻结**：`behavioral_baselines/` 的 .py 一字节不改（例外须按 `DEC-…d5b616bf.3` 范式：显式决策+最小改动+manifest 重钉）；六 canonical 文档只许 `DECISION_REGISTER.md` 的版本条目
- **不做**（纪律）：未到位次的域逻辑/域表提前实现（位次见基线 §1.5：**Phase 5 工程 + 语义均已 COMPLETE（P5-R 已毕并重签）**；当前位次 = **Phase 6（Scheduler + Goal Portfolio）**——**implementation 可以开**；其余域（Planner/Teaching 自动面）仍禁）；Redis/Kafka/分布式锁/TTL/heartbeat（Local V1 §24.1 永禁）；自动教学/Planner SELECT（Phase 8 才开）；`decision_cycle_id` 收紧已随 0007 兑现（`DEC-…091f35c3.7` 裁定 b 闭环）
- docs/run_post_bf_regression.py 的 ROOT 自相矛盾是原件冻结缺陷，永久不可修——repo-native 七套件是长期替代；勿再尝试修它
- Windows：core.autocrlf 与 blob 有字节差，哈希校验须 CRLF 归一；建议后续加 .gitattributes（勿扰动冻结文件行尾）

## 常用命令

```bash
python -m pytest          # 全量（含架构门与七套件行为基线）
ruff check . && mypy src  # 静态门
git push                  # CI 自动跑四门
```
