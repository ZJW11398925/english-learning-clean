"""Platform package: canonical base types, registry, and app.db infrastructure.

Kept import-light on purpose: submodules are imported explicitly by
consumers (`elc.platform.types`, `elc.platform.db.migrations`, ...).
"""
